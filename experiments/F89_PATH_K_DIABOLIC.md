# The Path-k Diabolic at the N=4→N=5 Transition: integrability keeps the crossings, the self-fold placed them on the real axis

**Status:** Tier 1 for the integrability mechanism (now Δ-verified, see "The Δ-test" below); Tier 2 for completeness (the full complex-q diabolic count is not claimed). The instrument reproduces the known path-3 diabolic structure exactly; the path-4 result (diabolics exist but sit at complex q, none at physical real q) is decisive and confirmed by a fine real-axis scan plus a tight zoom. All 15 in-region coalescences classify consistently across three signals (monodromy loop, gap-scaling exponent, EpCharacter): 11 diabolics, 4 defective EPs. The earlier character-vs-loop disagreement on two near-axis pairs was resolved (loop contamination by a neighbour EP at the dense path-4 spectrum, fixed by reading the small intrinsic loop radius). The central claim of the synthesis (integrability = existence) is no longer a grounded reading but a Δ-verified result: breaking integrability with an XXZ ZZ-anisotropy Δ≠0 kills the path-4 diabolics (they flip defective or lift), exactly as it flips the N=4 diabolic, while leaving a defective control untouched. The structure now extends to path-5 (N=6): with a residual-only instrument (the full-block scan floods on AT-locked degeneracies there), the same box gives 16 diabolics (up from 11, the count growing with N), all at complex q, none at a physical real q (see the path-5 section below). It extends to path-6 (N=7) only with an **exact-residual** instrument (the residual-only *tracker* itself floods at the F_53 strand density; the fix reads the residual roots as the eigenvalues of the block compressed onto the complement of the q-independent AT invariant subspace, no partition and no tracking, re-gated to reproduce 11 at N=5 and 16 at N=6): the box gives 37 diabolics, and for the first time since N=4 several sit at **physical real q** (two of the four confirmed on-axis by a tight zoom, all four absent at N=5/N=6), so "complex-q only for N ≥ 5" is too strong, the mechanism of the N=7 real-q onset open (see the path-6 section). These real-q diabolics are now **Δ-verified to be the same integrability-protected species** as the complex-q ones: porting the exact-residual treatment to the XXZ block and turning on an anisotropy Δ kills all four the instant Δ ≠ 0 (three flip defective, geo 2→1; the fourth lifts), exactly as it kills the complex-q diabolics, so the N=7 onset is a *placement* mechanism, not a new protection (see the Δ-test section). The placement itself is then resolved by extending the (rebuild-free, already N-general) exact instrument to N=8 and N=9: the real-q onset is **parity-gated, odd N ≥ 7** (N=8 even has none; N=9 odd has ≥ 3, each a clean isolated on-axis real-λ diabolic), refuting both the threshold and the one-off readings; its *cause* is now grounded from below (the dimension-mismatch / sector-swap: at odd N the reflection-fixed central site makes dim(R-even) ≠ dim(R-odd), forcing the realness antiunitarity Σ L Σ = L† to act within R-even, so two real eigenvalues can cross there; at even N the sectors are conjugate-paired and carry no real RESIDUAL eigenvalues, at most one frozen window-edge AT mode each, so no sector holds a real pair to collide), see the "odd-N effect" section. The even-N real-q defective check (2026-08-10, the sideways-ladder arc's even-N half) then answers the complementary defective question: at N=6 no real-q defective (1,2) EP exists at all (the same-day gcd certificate closed it window-free, a theorem, see the verdict), at N=8 the SAME certificate closed the same statement window-free on 2026-08-12 (no repeated Λ-root of the residual at any real q ≠ 0, defective or diabolic; the scan record's on-axis reading of the diabolic near q = 0.6165 was the cell-0.002 grid's, refuted exactly, the coalescence itself lying off the axis, see the verdict); the real-λ species is excluded on the measured 16-point q-grid 0.001..51.2 octic (at N=8 the literal real population is a constant pair pinned at the −6 window edge, one per R-sector, semisimple by the window-edge lemma, at every grid point), the N=6 clean defective loci keep |im q| ≥ 0.2, and the mechanism is sector disc-reality (real at N=4 via the self-fold, genuinely complex at N=6; the full-block disc is always real by the bipartite gauge, see the (1,3) section), leaving N=4 the one even N with recorded real-q defective *(1,2)* loci (the complex-λ half is unscanned above N=8). The quartet's mechanism itself did not stop at N=4: the self-fold is block-scoped and fixes (p, N/2) blocks at every even N, and the follow-up census on the **(1,3) block at N=6** (2026-08-10 evening) found the sibling population, closed 2026-08-11 by an exact Sturm count at **fifteen real-q defective loci** (9 R-even + 6 R-odd; by species ten fold pairs on the line and five conjugate pairs on the axis; the census's thirteen were a floor, one fold-line locus short in each sector, both channels blind to both misses), the sectors there being conjugation-closed real families by the bipartite gauge's parity ((p+q)(N−1) even, a criterion that subsumes the odd-N effect; the (1,2) sectors at even N sit on the other parity), and the real-λ species reaching beyond (1,3) into every p+q-even block probed (see "The (1,3) block at N=6" below).

**Date:** 2026-06-27 (path-3/4); extended to path-5 (N=6) 2026-06-29; extended to path-6 (N=7) with the exact-residual instrument 2026-06-30; the N=7 real-q diabolics Δ-verified integrability-protected (exact-residual XXZ port) 2026-06-30; the real-q onset resolved as parity-gated (odd N ≥ 7) via the N=8 / N=9 discriminator 2026-06-30; its mechanism grounded from below (the dimension-mismatch / sector-swap) and landed as a live C# witness (`inspect --root diabolicparity`) 2026-06-30; the within-odd threshold grounded on the residual (even N: zero real residual eigenvalues; odd N: a real population growing with N) and the onset's two geometries (real-real crossing vs conjugate-pair tangency) identified, both added to the witness 2026-06-30; Move 4 (the cross-fold) answered 2026-06-30: the (SE,DE)↔(SE,w_{N−2}) fold is an EXACT antiunitary similarity (machine-zero residual, N=4..9, all q), so every diabolic pairs across it with character and gap preserved, landed as a live C# witness (`inspect --root crossfold`) on a shared Core block builder; the cross-fold then shown **integrability-independent** 2026-06-30: it survives XXZ anisotropy at every Δ (the Δ·ZZ term is bit-flip-even, zz(b̄)=zz(b)), so it holds for the full interacting block even as the diabolics die under Δ, the discriminant being bit-flip parity (a longitudinal Z-field, odd, breaks it), via the `Build(n,wKet,wBra,q,Δ)` overload + the witness's Δ children; and finally DOCKED onto the F1 palindrome trunk 2026-06-30: the cross-fold generalizes to all (wKet,wBra) and is one of two antiunitary legs of the existing spine V₄ ⊂ D₄ block-resolved (bra leg P = ρ·F a factor of Π = R·D, ket leg Q = Π²·R, full flip QP = Π² = XGlobalChargeConjugationPairing), so F89d is the F1 palindrome's bra leg per block (`KetComplementPermutation` + the 3-leg `WeightCoherenceBlockTests` + the witness's Klein/dock children); the R-ODD sector given its own census 2026-07-02 (the sectorbraid arc's deep-loci probe: the exact R-odd AT/residual split, at N=5 24 = 7 ⊎ 17, and the scan; see "The R-odd census" below); the REAL SEED CENSUS landed the same day (the PT-break count scan, immune to the closest-pair masking: seeds 4/6/7/9 at N=5/7/9/11 across both parities, a fourth N=5 seed unmasked at q≈0.6430 R-odd, and the containment corollary's per-N input extended through N=11; see "The real seed census" below); the Route-B open item superseded by the fold-resultant certificate's exact mod-p disc layer profile 2026-07-03 (see the open-item UPDATE below); the EVEN-N REAL-Q DEFECTIVE CHECK 2026-08-10 (the sideways-ladder arc's even-N half: N=6 strips both parities all diabolic, the N=8 on-axis q=0.6165 classified DIABOLIC, the N=6 defective positions recorded, the literal real population gated by simulations/even_n_literal_real_count.py, and the mechanism read off disc-reality; see "The even-N real-q defective check" below); the N=6 closing certificate the same day (`FoldResultantCertificate.CertifyDiscReImGcd`: gcd(Re D, Im D) certified trivial, no real q ≠ 0 with any repeated Λ-root of the (1,2) residual, window-free, both parities; see the verdict); the (1,3)@N=6 CENSUS 2026-08-10 evening (sector disc-reality gated real, at least thirteen real-q defective loci across two detector channels, all doubled; [`disc13_n6_census.py`](../simulations/disc13_n6_census.py); see "The (1,3) block at N=6" below); the WALK from those seeds 2026-08-11 (both freight values through both chains at every one of the thirteen loci; the ladders' two-valued singular structure {√2 ×105, √6 ×15} separates family facts from locus facts, the per-locus content being the pins, the doubled truncation-split pairs, the defective-grade non-normality and lowest-weight membership; an η-side chain carries freight for the first time; [`eta_ladder_chain_n6.py`](../simulations/eta_ladder_chain_n6.py); see "The walk from a (1,3) seed" below); the EXACT (1,3) LAYER READ the same morning (the certificate machinery generalized with the rate-literal trap removed and validated on the committed (1,2) pins; layers [0,108,0,120] / [0,132,0,168] at two primes, no odd multiplicity, disc real mod p, F_res even in (Λ+12)², the μ=0 stratum a perfect square; `Disc13LayerScoutTests`; see "The exact (1,3) layer read" below); the EXACT REAL-ROOT COUNT 2026-08-11 afternoon (disc_M(G) and its simple layer landed exactly over ℤ, the census closed at 9 + 6 = fifteen with two new fold-line loci and the species split 7+2 / 3+3 read exactly, the mult-4 hideouts closed by the exact psc device; `Disc13SturmTests`; see "The exact real-root count" below); the WALK RE-RUN AT FIFTEEN 2026-08-12 (the two Sturm-only fold loci's λ read with the census's sector + pair-filter device at the Sturm-exact q\*, gated in the verifier: each locus's own R-sector holds exactly two truncation-split pairs, the conjugate-doubled fold pair, λ = −6 ± 1.7171734i at q\* = 0.6133162 R-even and −6 ± 3.0292680i at q\* = 0.4627729 R-odd; both seeds pass the full per-locus gate set, 662 gates total, exit 0); the N=8 (1,2) VERDICT CLOSED AS A THEOREM 2026-08-12 (the N=6 gcd certificate run at N=8 after a design pivot pared the planned ℚ(i) simple-layer split down to committed machinery: gcd(Re D, Im D) trivial, no repeated Λ-root of the residual at any real q ≠ 0, the on-axis reading of the 0.6165 diabolic refuted exactly with the coalescence off-axis; certified layer read 6086/3730/[376, 990]/max-mult 2 beside it, and the conj-antiparity conj-coeff(D)(q) = D(−q) pinned the parity pattern before the run, Re's 6086/3730 exact given the certified deg/v; gates `N8_Certificate_DiscMultiplicity` + `N8_Certificate_NoRealNonzeroCoalescence`; see the verdict)
**Authors:** Thomas Wicht, Claude (Opus 4.8)

## What this is about

**The system, in one paragraph (for a first read).** This is the R=CΨ² project's open spin chain: N spins with exchange coupling J, under local Z-dephasing at rate γ (the light falling on the chain; γ sits at the sending end, not at an eye). Its evolution is generated by a *Liouvillian* (the dissipative analogue of a Hamiltonian), whose eigenvalues λ are complex: Re λ is a relaxation rate (how fast a mode fades), Im λ an oscillation frequency. One dimensionless knob, **q = J/γ** (coupling vs arriving light; the octic-normalization book H = J·(XX+YY), factor-2 q/Q pin: [GLOSSARY.md, "The coupling ratio q and Q"](../docs/GLOSSARY.md)), tunes everything; promoted to a complex variable it exposes the analytic structure. We zoom into one invariant piece of that spectrum, the **(SE,DE) coherence block**: the density-matrix coherences linking the single-excitation sector (SE, one spin flipped) to the double-excitation sector (DE, two flipped). As q is tuned, two eigenvalues of this block can collide, and the *kind* of collision is the whole story.

**Two kinds of collision.** A **diabolic** point is a genuine crossing: the two eigenvalues coincide but their eigenvectors stay independent (*semisimple*), and it is *silent* (no square-root singularity). A **defective** collision, an *exceptional point* (EP), is the opposite: the eigenvectors coalesce into a Jordan block (a √-branch), and it is *loud* (amplified). Telling these apart at every collision is what the instrument below does.

**Terms used here** (project labels, one line each): *path-k* is the N=(k+1)-site chain (path-3 = N=4, path-4 = N=5); *the AT* is the [Absorption Theorem](../docs/proofs/PROOF_ABSORPTION_THEOREM.md) Re λ = −2γ⟨n_XY⟩, which fixes every relaxation rate from a mode's "light content" ⟨n_XY⟩ (how many of its sites carry an X or Y rather than an I or Z; the X/Y letters are the ones Z-dephasing actually damps, so more of them means faster fade); *the residual* is the part of the block left after removing the AT-locked closed-form strands (the part carrying the interesting structure), with *F_d* its polynomial factor of degree d and a *strand* one eigenvalue branch of the block as q is tuned; *the self-fold* is an antiunitary mirror symmetry the block has only at N=4 (the cause of the N=4 diabolic's specialness, below); *Tier 1/2* are the project's confidence labels (1 = proven or machine-verified, 2 = computed or plausible); *an arc* is an open research thread in the ledger; *R-1/R-2* are the two risk arguments from the reviewed investigation plan, summarized inline where used.

At N=4 (the path-3 chain) the (SE,DE) block has a single silent **diabolic** at a physical coupling q = q_EP ≈ 0.659, at λ_EP = −4 + 2iJ (rate −4 in units of γ, frequency 2J). The [zeros_connecting_structure](../compute/RCPsiSquared.Core/OpenArcs/OpenArcsRegistry.cs) arc traced it from below ([`gmscan --trace`](../compute/RCPsiSquared.Diagnostics/Foundation/GaloisMonodromyWitness.cs)): a mirror pair of modes slides together and merges on the symmetry line (the *fold*). That picture is N=4-only, because the within-block self-fold exists only at N=4 (the half-filling coincidence where the DE two-excitation sector is its own bit-flip complement, which happens only when the 2 excitations equal the N−2 empties, i.e. N=4). The forward question: **does an analogue survive to N=5 (path-4), or does removing the self-fold kill the diabolic?**

The honest prior (the reviewed investigation plan's R-1/R-2): a diabolic is a *node* of the spectral curve, codim-3-complex = 6 real conditions, overdetermined by 4 in a 2-real-DOF complex-q scan, so **generically absent unless a symmetry auto-satisfies it**. At N=4 the self-fold (plus the reality of q_EP) did exactly that. Remove it at N≥5 and the generic expectation is: no diabolic. The answer turned out richer than either "yes" or "no", and it reconciles the codimension argument with integrability: the diabolics **survive at every N** (free-fermion integrability keeps the crossings), but **where they sit in q moves**: all at complex q for N=5 and N=6, then several **returning to the physical real axis by N=7** (see the path-5 and path-6 sections).

## The instrument, validated at path-3

[`pkmono --diabolic`](../compute/RCPsiSquared.Cli/Commands/PathKMonodromyScanCommand.cs) (engine: [`PathKMonodromyScout.FindDiabolics`](../compute/RCPsiSquared.Diagnostics/Foundation/PathKMonodromyScout.cs)) hunts the residual block's coalescences: it scans the complex-q plane for full-block min-gap minima (no per-q AT stripping, which is valid only at q=2; the residual SET is tracked from q=2 by continuity, since F_AT·F_residual are distinct factors), then classifies each candidate by three independent signals:

- the **monodromy loop** (a small q-loop: identity ⟹ no √-branch ⟹ diabolic candidate; transposition ⟹ defective EP),
- the **gap-scaling exponent** (gap ∝ |q−q\*|^p: p ≈ 1 linear ⟹ two sheets crossing ⟹ diabolic; p ≈ ½ ⟹ defective √-branch),
- the **EpCharacter** verdict (Riesz projector: geo = alg, departure ≈ 0 ⟹ semisimple/diabolic; geo < alg ⟹ defective Jordan block).

At path-3 the three agree and reproduce the **known** diabolic structure of the squared discriminant factor (3q⁴+q²−1)² exactly: the real pair q ≈ ±0.659 (λ = −4 ± 1.318i) **and** the imaginary pair q ≈ ±0.876i (λ = −2.248 / −5.752), all identity-loop, exponent 1.00; the genuine EPs are correctly transposition-loop, exponent ½. This is the [`PathKDiabolicTests`](../compute/RCPsiSquared.Diagnostics.Tests/Foundation/PathKDiabolicTests.cs) gate (the path-k path reproduces `OcticRootsAt`; the classifier re-finds q_EP as diabolic and a generic eigenvalue as not).

## The path-4 (N=5) result

Diabolics **exist** at path-4, and there are several. But the decisive observation is *where in q*: **none sits at real (physical) q.** The clean diabolics (all three signals agreeing) are conjugate pairs at complex q:

| q | merged λ | signals |
|---|---|---|
| ± 0.879 i | −6.662 | identity loop, exp 1.00 (imaginary-q, analytic continuation, like path-3's ±0.876i) |
| ± 1.695 i | −0.383 | identity loop, exp 1.00 (imaginary-q) |
| 0.6407 ± 0.180 i | −4.077 ± 1.115 i | identity loop, exp 0.98 (clean) |
| 0.7654 ± 0.024 i | −4.371 ∓ 2.056 i | identity loop, exp 0.98 (clean) |
| 1.9447 ± 1.217 i | −2.455 ∓ 3.473 i | identity loop, exp 1.02 (clean) |

(This table shows the clean complex-q families as representatives, NOT the full list. The Status line's census, **11 diabolics + 4 defective EPs = 15 coalescences**, is the authoritative count from the broad scan re[0.2,3] × im[−1.5,1.5], reproducible verbatim via the §Reproduce command; the table is a readable subset of it, plus the near-axis pairs discussed below.)

A fine real-axis strip scan (`--re 0.3,2.5 --im -0.25,0.25 --cell 0.01`) finds **no diabolic at im(q) = 0**; the real-q coalescences in this strip are **defective** EPs (q ≈ 1.0776, λ ≈ −3.792, plus the seed locus q ≈ 0.6209, λ ≈ −4.6189 of the multi-sector census; a third real defective EP lives in the R-ODD residual at q ≈ 2.80489, λ ≈ −4.4882, outside this strip's octic story). The closest diabolic approach to the physical axis is a conjugate pair at q = 0.6118 ± 0.012i, and a tight zoom (`--cell 0.002`) confirms it stays **split** at ± 0.012i rather than merging onto the axis. So at every physical (real) q the N=5 chain has only loud defective EPs; the silent diabolics have retreated into complex q.

## The path-5 (N=6) result, and the residual-only instrument

Pushing the same hunt to path-5 (N=6) first broke the instrument, then confirmed the pattern. The naive `pkmono --diabolic --k 5` over the path-4 box floods: 528 coalescences, every one with gap exactly zero and residual=False (the scan's per-coalescence flag: the coalescing pair is AT-locked, not part of the residual). These are not diabolics but the AT-locked strands crossing each other. At N=6 the double-excitation sector carries enough repeated light-content ⟨n_XY⟩ that many AT strands share an identical relaxation rate Re λ = −2γ⟨n_XY⟩, so a pair coincides (Re and Im both equal) wherever their frequencies cross, which happens on dense curves throughout complex q. The full-block min-gap scan that was clean at N=5 (zero such crossings in the box) drowns at N=6, and the genuine residual coalescences, sitting at a small but finite gap, are never even local minima of a field that is zero around them.

The fix is to scan the residual strands only. [`ResidualRootsTracked`](../compute/RCPsiSquared.Diagnostics/Foundation/PathKMonodromyScout.cs) follows the residual SET by nearest-neighbour continuity from the base q0 = 2 (where it is identified exactly against the reconstructed AT factor); the SET is monodromy-invariant because F_AT and F_residual are distinct factors, so it survives any label permutations on the path. With the AT strands gone from the root set, the gap field, the refinement, the monodromy loop and the gap-scaling exponent all see only the H_B-mixed residual. The locator is exposed as `pkmono --diabolic --residual --k N`. Two rounds of a from-q0 re-tracking nest had to be removed for it to scale: `GapRefineResidualLocal` and `ResidualLoopIsIdentity` track locally from the candidate instead of globally per probe, dropping the [path-k diabolic test suite](../compute/RCPsiSquared.Diagnostics.Tests/Foundation/PathKDiabolicTests.cs) from 11 minutes to 15 seconds.

With the residual-only scan, the same box re[0.2,3] × im[−1.5,1.5] at cell 0.05 gives 21 clean coalescences: **16 semisimple residual diabolics and 5 defective EPs** (path-4 gave 11 + 4). Two things hold, exactly as at path-4:

- **The count grows with N**: 11 (N=5) → 16 (N=6), consistent with the integrable level-crossing density rising as the residual degree F_d climbs (18 → 32).
- **Complex-q only**: the diabolics at re(q) = 0 sit on the imaginary axis (an analytic continuation, like path-3's ±0.876i); the rest are at complex q; the ones nearest the physical axis (im(q) ≈ 0.01 to 0.05) are the ghosts of the path-4 kind (q = 0.6118 ± 0.012i there), split off the axis rather than on it. None sits at a physical real q. The self-fold that put one diabolic on the real axis was N=4-only, and nothing brings a diabolic back to the physical axis at N=6.

**The Δ-test confirms integrability-protection at N=6.** The mechanism question (does XXZ anisotropy kill the diabolics, as it does at N=4?) needed the same residual treatment: the full-block Δ-test box scan is captured by the AT crossings at N=6, so q* jumps to an exact-zero AT degeneracy and the verdict turns unreliable (it reads "integrability-protected" for the wrong reason). With a residual-aware tracker ([`ResidualRootsTrackedXxz`](../compute/RCPsiSquared.Diagnostics/Foundation/XxzCoherenceBlock.cs); the residual SET is Δ-stable because the ZZ term is Hermitian, so the AT rate Re λ = −2γ⟨n_XY⟩ is Δ-independent), each path-5 diabolic reads cleanly: diabolic (geometric multiplicity = algebraic multiplicity = 2, the two eigenvectors stay independent) at Δ = 0 with q* pinned at the seed and a finite gap, then DEFECTIVE (geometric drops to 1 < algebraic 2, a Jordan block) the instant Δ ≠ 0, the departure-from-normality growing with Δ. The rung-near diabolic at q = 0.709 − 0.219i (which the full-block test captured entirely, reading false LIFTs at a wandering q*) now flips clean: departure 0.028 → 0.073 → 0.160 across Δ = 0.02, 0.05, 0.10, q* never leaving the seed. The free-fermion integrability that creates the diabolics protects them at N=6 exactly as at N=4, via `pkmono --delta-flip --residual`.

Honest limits. The 16 is the EpCharacter count (the load-bearing R-3 discriminant) in this box at this resolution; completeness is not claimed (obstacle 1, as at path-4). A few coalescences read diabolic by EpCharacter and the identity loop but show a gap-scaling exponent near ½ rather than 1 (for example q = 0.6508 + 0.052i, exponent 0.49); these need the per-point three-signal reconciliation the path-4 near-axis pairs needed, and are not yet individually settled. Pushing to path-6 (N=7) then broke the residual-only tracker itself, which is the next section.

## The path-6 (N=7) result: the exact-residual instrument, and real-q diabolics appear

Pushing the hunt to path-6 (N=7) first exposed that the residual-only **tracker** does not survive the strand density of N=7's residual factor (F_53: 53 residual strands, up from 18 and 32 at N=5 and N=6), then, once that was fixed, turned up something the lower N did not have.

**Why the tracked instrument broke (not "just slow").** The residual-only path of path-5 identifies the residual strands by a nearest-match removal of the reconstructed AT factor from the numerical block spectrum, then follows that SET by nearest-neighbour continuity from q0 = 2. At N=7 the **S₂-symmetric (SE,DE) block** (the symmetry-reduced block the scout works on, not the full 147-dimensional coherence block) is 75-dimensional (= 22 AT + 53 residual) and carries exact rate-degeneracies: many strands sit at exactly Re λ = −6 (the AT rate for ⟨n_XY⟩ = 3, the maximally-damped sector, where the coherence's bra and ket share no excited site), several coinciding. The 22-root AT factor cannot tell which of these degenerate strands are AT, so a chunk of genuine AT strands (and exact duplicates) leak into the "residual" SET already at the base q0 = 2. The tell-tale: three strands all at exactly −6.000 + 0i, i.e. a triply-repeated root. The true residual factor is a degree-53 polynomial whose roots are all distinct (it is *separable*, the condition that makes it a clean Galois factor), so it *cannot* have a repeated root; three coincident strands therefore prove that at least two of them are leaked-in AT strands wearing a residual label, not genuine residual roots. Their exact same-rate degeneracies make the residual min-gap zero across the whole box, so a broad scan reports hundreds of spurious "gap = 0, exponent = NaN" coalescences (293 in the re[0.2,3] × im[−1.5,1.5] box) and isolates no genuine ones. This is *not* the per-evaluation-cost / wall-clock obstacle the path-5 notes anticipated; the per-evaluation result is corrupt, and a smaller box or seed pre-filter would have produced the same artifact.

**The fix: the residual roots EXACTLY, with no partition and no tracking.** In plain words: instead of guessing strand-by-strand which are AT and chasing them across the plane, carve off the entire AT-carrying subspace once (it is the same at every q) and read the eigenvalues of what is left. The algebra makes that exact. Write the block matrix as **M(q)** (the (SE,DE) block at coupling q); it splits as M(q) = D/2 + i·q·K/2, where **D** is the q-independent dephasing rate diagonal (the relaxation rates, fixed by the arriving light) and **K** is the real-symmetric hopping (the Hamiltonian coupling). The AT-locked strands span an invariant subspace W_AT of M(q), and that subspace is **q-independent**: it is built from the *rate sectors* (groups of strands sharing one D-rate) and the K-invariant structure of the hopping, and K-invariance does not change when K is scaled by q. So the residual roots at any q are exactly the eigenvalues of M(q) compressed onto the **orthogonal complement** of W_AT: with an orthonormal split [U_AT | U_res] (a full QR of the AT invariant-subspace basis), the compression is block-triangular (U_resᴴ M(q) U_AT = 0 because W_AT is M(q)-invariant), so eig(U_resᴴ M(q) U_res) = the full spectrum minus the AT part = the F_d residual roots, AT-free by construction. No nearest-match, no continuity tracking, a single F_d × F_d eigendecomposition per q. The AT invariant subspace is exactly the one whose characteristic polynomial [`F89AtFactorReconstruction.ForPathK`](../compute/RCPsiSquared.Core/F89PathK/F89AtFactorReconstruction.cs) already returns as the validated AT factor (the full-D reconstruction test asserts it equals the committed oracle for k = 4,5,6), so eig(U_resᴴ M U_res) are the genuine oracle F_d roots. Exposed as [`PathKMonodromyScout.ResidualRootsExact` / `FindDiabolicsExact`](../compute/RCPsiSquared.Diagnostics/Foundation/PathKMonodromyScout.cs) and the CLI flag `pkmono --diabolic --exact`. It re-gates the established science exactly: the same boxes give **11 diabolics at N=5 and 16 at N=6** (matching the tracked path), and the residual roots are distinct (min gap ≫ 0) where the tracked path flooded with duplicates. As a bonus it is several times faster (one small EVD per cell instead of a 160-step continuity track of the full block).

**The count grows, and grows fast.** The same broad box re[0.2,3] × im[−1.5,1.5] at cell 0.05 gives **37 semisimple residual diabolics** at N=7, all with finite refined gaps (~10⁻⁸ to 10⁻¹⁰), gap-scaling exponent ≈ 1, identity loop, and varied λ, mostly in q-conjugate pairs:

| N | residual degree F_d | in-region diabolics (cell 0.05) |
|---|---|---|
| 5 | 18 | 11 |
| 6 | 32 | 16 |
| 7 | 53 | 37 |

(In-region EpCharacter counts at this resolution; completeness is not claimed, as at every N. A finer real-axis strip at cell 0.01 finds more, the count being resolution-dependent.)

**The new thing: diabolics appear at PHYSICAL REAL q at N=7.** At N=4 a diabolic sat at real q (the self-fold's doing); at N=5 and N=6 none did (defective EPs live on the real axis at N=5; the N=6 axis carries neither species, since 2026-08-10 a theorem, see the even-N verdict below). At N=7 the exact scan finds **semisimple diabolics at real q and real λ**: q = 1.1264, 1.3038, 2.6280 in the cell-0.05 broad box (λ = −4.942, −5.171, −4.343), plus a fourth, q = 0.6788 (λ = −4.557), in a finer cell-0.01 real-axis strip. These are verified genuine: a tight zoom (cell 0.002) on two of them (q = 1.1264 and q = 2.6280) keeps each a single on-axis point with refined gap ~10⁻⁹ that does **not** split into a conjugate pair (unlike the path-4 ghost at 0.6118 ± 0.012i, which the same fine scan resolves as off-axis); the other two read as single on-axis points at the cell-0.01 strip but were not separately tight-zoomed. And it is a real N=7 onset, not a tracked-method miss: the exact instrument run on the N=5 and N=6 real-axis strips finds **no** real-q diabolic there either (N=5 keeps only defective EPs on the real axis, q ≈ 0.6209 and q ≈ 1.0776 in the octic story), reproducing the established lower-N result. So the simple reading "the self-fold is N=4-only ⟹ complex-q only for all N ≥ 5" is too strong: a different mechanism returns diabolics to the physical axis by N=7. Whether that return is a threshold, an even/odd parity effect, or a one-off is now **settled by the N=8 / N=9 discriminator** (next section): it is **parity-gated, odd N ≥ 7** (N=8 even has none, N=9 odd has them again). The *cause* of that parity is grounded from below (the dimension-mismatch / sector-swap, next section). The integrability character of these real-q diabolics, by contrast, is now settled: the Δ-test (does an XXZ anisotropy flip them defective, as it does the complex-q ones?) is run by porting the exact-residual treatment to the [`XxzCoherenceBlock`](../compute/RCPsiSquared.Diagnostics/Foundation/XxzCoherenceBlock.cs), whose tracked residual path breaks at k = 6 for the same reason (the F_53 AT-degeneracy flood). The answer is clean and it is reported in the Δ-test section below: all four real-q diabolics flip defective or lift the instant Δ ≠ 0, the same integrability-protected death as the complex-q ones. The real-q diabolics are a **characterized species**: integrable level-crossings, placed on the physical axis by the N=7 mechanism but protected by the same free-fermion structure as every other diabolic in this story.

## The real-q onset is an odd-N effect: the N=8 / N=9 discriminator

The N=7 onset left one sharp question: is the return of diabolics to the physical axis a **threshold** (every N ≥ 7 has them), an **even/odd** parity effect, or a **one-off** quirk of N=7? Two more N settle it, and the answer is parity.

**No rebuild was needed.** The exact-residual instrument is already N-general: the AT invariant subspace is reconstructed from the rate sectors at any block size ([`AtInvariantSubspaceBasis`](../compute/RCPsiSquared.Core/F89PathK/F89AtFactorReconstruction.cs)), and `pkmono --diabolic --exact --k N` builds the (SE,DE) block at nBlock = k+1 for any k. (Only the *test* oracle [`F89PathKFdOracle`](../compute/RCPsiSquared.Core/F89PathK/F89PathKFdOracle.cs), the committed F_d literals used to cross-check the residual count, stops at k = 6; the scan itself does not read it.) So N=8 (k=7) and N=9 (k=8) are a **run**, not a build: the same matched real-axis strip (re[0.2,3], a thin im band, cell 0.05 then cell 0.01) and the same tight-zoom split-test that confirmed the N=7 real-q diabolics.

**N=8 (even): none.** The strip's near-axis coalescences all sit *off* the real axis. The closest real-λ candidate, q = 0.6904, refines to im(q) = −0.004 (not 0) inside a dense cluster of partial strand-crossings (gap-scaling exponents scattered 0.1–0.7, none the clean linear ≈ 1 of a true crossing; best gap stuck at ~10⁻⁸). A tight zoom (cell 0.002) over the whole candidate band re[0.58, 0.73] finds **no** isolated on-axis point with real λ: the only exact-im(q)=0 point there carries a complex λ (q = 0.6165, λ = −4.617 + 1.497i; "exact" at the zoom's cell resolution — the 2026-08-12 gcd certificate later refuted the on-axis reading exactly, the coalescence lying off the axis, see the verdict), and the real-λ near-candidates never reach the axis. The genuine N=7-type diabolic (real q **and** real λ, isolated, gap → 10⁻⁹) is absent.

**N=9 (odd): present, ≥ 3.** The same finer strip turns up isolated on-axis points that refine to **exactly** im(q) = 0 and im(λ) = 0, each with its off-axis conjugate-pair neighbours split cleanly away, the N=7 signature. Tight zooms (cell 0.002) confirm three:

| q | λ | refined gap | exponent |
|---|---|---|---|
| 0.4755 | −5.424 | 6.3·10⁻⁹ | 1.06 |
| 1.1144 | −4.611 | 5.0·10⁻⁹ | 0.83 |
| 1.4994 | −4.381 | 6.5·10⁻⁹ | 0.70 |

Each is a single point at im(q) = im(λ) = 0 that does **not** split into a conjugate pair under the zoom; its nearest neighbours (for example q = 0.4837 ± 0.011i beside q = 0.4755, and q = 1.1018 ± 0.030i beside q = 1.1144) are the off-axis ghosts, with complex λ and a ± im(q) partner. This is the same on-axis survival that confirmed N=7's four; N=8 produced nothing like it. (The q = 0.4755 point is the cleanest, exponent 1.06; the other two read a softer 0.70–0.83, the gap-scaling exponent pulled below 1 by neighbouring strands in the dense N=9 field, but all three are isolated, on-axis, real-λ, gap → 10⁻⁹, the load-bearing signature.)

**The discriminator, sharply.** At *both* N=8 and N=9 the strip carries on-axis points with **complex** λ (analytic-continuation crossings: q = 0.6165 at N=8, q = 0.6688 at N=9). What only N=9 has is on-axis points with **real** λ, the genuine real-q diabolics. So the parity gate sits on the real-λ, self-conjugate coalescence specifically, not on any on-axis crossing.

**The count vs N, and the verdict.**

| N | parity | real-q diabolics (real q, real λ, isolated, gap → 10⁻⁹) |
|---|---|---|
| 4 | even | 1 (via the N=4-only self-fold, a *different* mechanism) |
| 5 | odd | 0 |
| 6 | even | 0 |
| 7 | odd | 4 |
| 8 | even | 0 |
| 9 | odd | ≥ 3 |

This refutes both simple readings: not a **threshold** (N=8, the very next N, returns to zero), and not a **one-off** (N=9 has them again). The onset is **parity-gated: real-q diabolics appear only at odd N, and only from N=7** (odd N=5 is still empty, a secondary "enough DE states" threshold *within* the odd branch). Excluding N=4's special self-fold, the real-q onset is an **odd-N ≥ 7** phenomenon. (Coverage, as always: the strip is bounded re[0.2,3] and completeness is not claimed; the verdict rests on the *qualitative* contrast (clean isolated on-axis real-λ survivors at odd N=7, 9; none at even N=6, 8), which is robust to the box.)

### The mechanism, grounded from below: the dimension-mismatch / sector-swap

The reflection R: i ↦ N−1−i commutes with the block (it is the S₂ the block is built on, in [`F89PathKSeDeBlock`](../compute/RCPsiSquared.Core/F89PathK/F89PathKSeDeBlock.cs)) and splits the full (SE,DE) coherence block into an R-even and an R-odd sector. The realness symmetry at real q (the chiral antiunitarity Σ L Σ = L†, which forces the spectrum real-or-conjugate-paired) has a fixed relation to R, and that relation flips with the parity of N. (Scope: this section reads the (SE,DE) block, p+q = 3. The block-general criterion, gated in the (1,3) census section below, is that the realness acts within each sector iff **(p+q)(N−1) is even**; at p+q = 3 that reads on the parity of N, which is this section's statement, and the sector-dimension mismatch below is a correlate of the character at p+q odd, not its cause: (1,3)@N=6 has equal sector dimensions and is nonetheless self-conjugate.) This is computed from below by building the full (SE,DE) block, splitting it by R, and measuring whether the antiunitarity acts within each sector (self-conjugate) or across them:

- **At even N** the two sectors have equal dimension and the antiunitarity maps one onto the other: σ(R-even) = conj(σ(R-odd)) **exactly** (the cross-sector conjugacy defect is ~10⁻¹³, while each sector's self-conjugacy defect is O(20): neither sector is self-conjugate). The realness lives *across* the two sectors, so a within-sector collision of two REAL eigenvalues, the pseudo-Hermitian PT-breaking route to a **defective** EP, has nothing to act on: neither sector carries real RESIDUAL eigenvalues in the window (the within-odd threshold below), and the full block's only real modes are frozen window-edge AT strands, at N=8 one per sector at λ = −6 at every measured q (the even-N check's literal-population verifier), so no sector holds a real pair to collide. The loud defective EPs an even N does show at real q are N=4's, the self-fold's doing; even N = 6 and 8 keep only complex-λ analytic-continuation crossings on the axis in the scanned strips, measured semisimple (q = 0.6165 at N=8, classified in the even-N check below).
- **At odd N** the reflection-fixed central site (i = (N−1)/2, an integer only at odd N) makes dim(R-even) − dim(R-odd) = (N−1)/2 ≠ 0 (3 at N=7, 4 at N=9; 0 at even N=6, 8). The dimension mismatch *forbids* the antiunitarity from pairing the sectors across, forcing it to act within each: each sector becomes **self-conjugate** (self-conjugacy defect ~10⁻¹⁴). The R-even block then carries genuine **real** eigenvalues, and two of them can cross at real q as a **semisimple (diabolic)** coalescence. The diabolic eigenvectors confirm the tie to the centre: they carry ~0.18 to 0.20 of their weight on the central-SE-site states (~0.05 to 0.06 on the reflection-fixed singletons).

So the realness and the parity are grounded in one stroke: the same dimension mismatch the fixed central site creates is what lets a real eigenvalue exist inside R-even and cross there. The fixed-site object is the reflection-fixed Jordan-Wigner (JW) band-centre zero mode (the free-fermion single-particle mode k = (N+1)/2, R-self-fixed, an integer mode only at odd N), the same structure [SLOW_MODE_R_PARITY](SLOW_MODE_R_PARITY.md) found splits a *different* coherence block's exceptional point at odd N. This is genuinely distinct from the N=4 self-fold (the antiunitary half-filling T = P·K, N=4-only, which fixes Re λ = −4): the odd-N diabolics sit at real λ but *not* at −4 or −N, because they are placed by the sector-self-conjugacy, not by T.

**The within-odd threshold (grounded on the residual; its onset is a density effect with two geometries).** The mechanism *permits* real-q diabolics at every odd N (R-even is self-conjugate at N=5 too), yet odd N=5 shows none. Reading the parity directly on the AT-stripped **residual** (the F_d strands the diabolics actually live in, via [`PathKMonodromyScout.ResidualRootsExact`](../compute/RCPsiSquared.Diagnostics/Foundation/PathKMonodromyScout.cs) at real q) sharpens the gate: at **even N the R-even residual carries NO real eigenvalues anywhere** in the physical window q ∈ [0.2, 3] (the realness is entirely cross-sector), so even N has *no real strands at all* to host a real-q diabolic; at **odd N the residual carries a real population that grows with N** (up to 4 / 5 / 8 real residual roots at N = 5 / 7 / 9, residual degrees F_18 / F_53 / F_116). The real-q diabolics are real-λ coalescences *among these odd-N real residual strands*, and the within-odd onset tracks the residual degree/population growth: F_18 (N=5) is too sparse to host a real-q coalescence in the window, F_53 (N=7) is the first that does. **No closed form** for the exact threshold, as the count-vs-N caveat notes.

The recorded "two real residual strands crossing" picture turned out to be *one* of two geometries: the onset coalescences are sometimes a **real-real crossing** (two real residual strands meeting, the count-preserving semisimple crossing, e.g. N=7 at q ≈ 2.628, where the global closest approach of two real strands → 0) and sometimes a **conjugate pair tangent to the real axis** (a (λ, λ̄) pair whose Im → 0 only at q*, e.g. N=9 at q ≈ 0.4755, where near q* the pair sits at λ = −5.43 ± O(10⁻³)i with the imaginary part shrinking to zero). The first odd N with a real-real *crossing* is N=7 (N=5's real strands stay ≈ 0.028 apart at their closest); N=9's onset diabolic, by contrast, is a conjugate-pair tangency (so its real strands never cross, yet it has real-q diabolics); this is why counting real-real crossings is *not* a proxy for the real-q diabolic count, and the count remains the scout's (`FindDiabolicsExact`) job. The persistent evidence for the mechanism is a live C# witness, [`DiabolicReflectionParityWitness`](../compute/RCPsiSquared.Diagnostics/Foundation/DiabolicReflectionParityWitness.cs) (`inspect --root diabolicparity`): it builds the full (SE,DE) block at inspect time, splits it by R, measures the self- vs cross-sector conjugacy across N=5..9 (self-conjugacy defect ~10⁻¹⁴ in both sectors at odd N; σ_even = conj σ_odd to ~10⁻¹³ at even N), reads the real R-even residual population vs N (even N: 0; odd N: growing), and reproduces the N=7 (λ=−4.942) and N=9 (λ=−5.424) diabolics in the R-even sector, all from below.

### The R-odd census (2026-07-02, the sectorbraid arc's deep-loci probe)

Everything above scans the **R-even** residual (the S₂-symmetric block is the R-even sector; `FindDiabolicsExact` reads its exact AT-complement compression). The codim-1 proof's fold-resultant program ([PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) §6) needed the **R-odd** enumeration too, and the probe built it with the same exact machinery mirrored to the other parity (`F89PathKSeDeBlock.ROddBasis` + `F89AtFactorReconstruction.ROddAtInvariantSubspaceBasis` + `PathKMonodromyScout.FindDiabolicsExactROdd`; CLI `pkmono --diabolic --rodd`; gate [`ROddDeepLociProbeTests`](../compute/RCPsiSquared.Diagnostics.Tests/Foundation/ROddDeepLociProbeTests.cs), Category RODDPROBE). At N=5 the R-odd sector (dim 24 = 7 rate-locked AT strands, q-linear λ = r0 + 2isq on the rungs r0 ∈ {−2,−6}, ⊎ a **degree-17 residual**) gives, over the standard window (0.2, 3.0) × (−1.5, 1.5):

- **7 diabolics** (gap exponent ≈ 1.0, identity loop), among them the pure-imaginary-q family with real λ (±0.5819i at λ = −7.2534, ±0.8460i at λ = −4.8561), semisimple by Hermiticity exactly as on the R-even side (the block is real-symmetric at Re q = 0), the two-mechanism split of the proof's §8 repeating verbatim in the other parity. One of these sits at λ < −6; it is silent (semisimple), so it does not touch the deep-loci story.
- **7 defective EPs** (gap exponent ≈ 0.5, transposition loop), the known third real N=5 EP among them, reproduced from the R-odd residual at q = 2.804888, λ = −4.4882.
- **The probe verdict:** of the 6 complex defective loci, NONE is deep (no Re λ_A < −6); 4 are closed by the moved corner window; but the pair **q\* = 1.7701 ± 1.2189i** (λ_A = −3.7562 ∓ 0.5065i) escapes the window although Re λ_A > −6 (|Im q|·‖K‖₂ = 8.4 swamps the margin 2.24, and the R-parity-refined window, the corner's own R-odd sector, does not close it either), so it joins the resultant deliverable alongside the R-even deep pair. The details and the consequence for the certificate live in the proof's §6 and the `zeros_connecting_structure` ledger entry.

### The real seed census (2026-07-02, the PT-break count scan)

The containment corollary of [PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) has exactly one per-N input: a real defective EP on (1,2) must EXIST at each odd N (the seed). The gap-field scans cannot answer that reliably at growing strand density (the closest-pair masking: today's R-even N=7 scan misses the known seed q\* = 1.5148 outright). The census instrument that can is the **PT-breaking count change** (`PathKMonodromyScout.FindRealDefectiveByCountChange`, gate [`RealSeedCensusTests`](../compute/RCPsiSquared.Diagnostics.Tests/Foundation/RealSeedCensusTests.cs), Category SEEDCENSUS): at odd N both reflection sectors are self-conjugate, so a real defective EP is where two REAL residual strands merge and leave the axis as a conjugate pair, and the sector's real-root count jumps by 2. Counting is global, so no density masks it; each bisected jump is then classified AT-aware on the raw block (`SectorEpProbe.ProbeDefectiveAnywhere`). Two blind spots, both harmless for the seed question: a tangency (a pair grazing the axis, e.g. the N=9 onset at q ≈ 0.4755) makes no net count change, and real-real semisimple crossings make none; both are diabolic, not seeds.

Results over the standard window q ∈ [0.2, 3], both parities:

| N | R-even seeds | R-odd seeds | total | notes |
|---|---|---|---|---|
| 5 | 0.620878 (λ=−4.6189), 1.077615 (λ=−3.7917) | **0.643037 (λ=−3.8196, NEW)**, 2.804888 (λ=−4.4882) | **4** | the fourth seed was unknown; confirmed independently by a tight local gap scan (`pkmono --rodd`, cell 0.005: defective at 0.6430, λ=−3.820) |
| 7 | 0.538696 (−3.9816), 0.924107 (−4.6615), **1.514833 (−4.8846)** | 0.600379 (−4.9228), 0.723553 (−4.8609), 1.590852 (−3.8998) | **6** | 1.5148 = the proof's known N=7 seed, which the gap scan masks; the count scan recovers it |
| 9 | 0.591760 (−5.1206), 0.659767 (−5.1101), 0.849011 (−4.8415), 2.137549 (−4.0264) | 0.511958 (−4.1581), 0.675374 (−4.4978), 2.761985 (−5.0640) | **7** | **the seed input EXTENDS to N=9**: the 12-set diamond membership at N=9 follows from the corollary with zero new compute |
| 11 | 0.502123 (−4.3111), 0.598253 (−4.5660), 0.812457 (−4.8639), 1.902993 (−5.2355) | 0.587252 (−5.2588), 0.631587 (−5.2563), 0.727968 (−5.2098), 1.010462 (−4.7397), 2.700712 (−4.1353) | **9** | **extends to N=11** (Category SLOW_SEEDCENSUS, 2 h 31 m; the F_d BigRational setup dominates) |

The real-axis seed population grows with N (4/6/7/9 at N = 5/7/9/11) and both parities carry it. Coverage, as always: the window; the counts are lower bounds only in the sense that seeds outside [0.2, 3] would not show.

The *existence* of at least one seed at each odd N (what the corollary actually needs, above the count) is reduced from this census to an exact nullity identity in [F89_SEED_EXISTENCE_REDUCTION.md](F89_SEED_EXISTENCE_REDUCTION.md): r(0⁺) − r(∞) = N − 1, carried by the odd-N −2-rung kernel (a path count), with r(∞) the free-fermion fusion-resonance count. That reduction leaves one lemma and a codimension-2 genericity open, but it is the reason not to grow the scan past N = 11 to "confirm".

### The even-N real-q defective check (2026-08-10, the sideways ladder's even-N half)

The sideways-ladder arc (`sideways_spin_ladder`, [SidewaysSpinLadderClaim](../compute/RCPsiSquared.Core/Symmetry/SidewaysSpinLadderClaim.cs)) left one open half: does the chain walk have a real-q defective (1,2) locus to stand on at even N? One word first, because the repo reserves it: a **seed** is the real-λ census object (`RealDefectiveSeeds`, MirrorWorld's `Seed.cs`, the reduction's corollary input). What the walk needs is wider, any real-q defective EP of the block, complex λ included; this section says *locus* for the wide object. The sweep for it went to [PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) (the window-edge and shell lemmas: parity-free constraints, no existence content; §7's fold lattice, which supplies the mechanism below), [F89_SEED_EXISTENCE_REDUCTION](F89_SEED_EXISTENCE_REDUCTION.md) (zero surplus at even N, r(0⁺) = r(∞) = 3·Z₃; its even-N parenthesis credits "the census's even-N null result", which overstates the store it points at, since no even-N census exists, only the measured window population below), MirrorWorld's `Seed.cs` (the store that scopes the zero correctly: "a forced lower bound; whether an accidental seed exists off it is a separate question this count does not settle"), the multi-sector monodromy design spec (local, `docs/superpowers/specs/2026-07-01-multi-sector-monodromy-design.md`) §B3 (the decisive prior: disc-reality is antiunitary-forced, real at N=4 via the self-fold, **genuinely complex at N=6** — sector-level statements throughout, the (1,3) census section's gauge caveat: every full-block disc is real), `docs/CAUGHT_ERRORS.md` (the F86a shape: a scan's silence once passed for "no EP"), the F-registry's F125 entry in `docs/ANALYTICAL_FORMULAS.md` (the fold partner −λ̄−2N and the recorded "at N=4 the locus eigenvalue is complex and the 4-orbit is accordingly conjugate-related, not byte-identical", the walk's value-level prior), and the mechanism section above. N=4 answers the existence question for even N as a class: `ReferenceDefectiveLoci` holds **four real-q defective EPs at N=4** (q ≈ 0.460212, 0.854438, 0.857458, 1.738181; their λ, recomputed at those seeds for the walk below, all sit on Re λ = −4 to 2·10⁻¹⁴, the fixed line of the fold-partner map −λ̄−2N, and derivably so, since the holomorphic fold s and conjugation both permute the block's defective set, fixing a single pair under their composite: λ = −4−1.633i, −4−1.239i, −4−1.566i, −4−9.136i with conjugate partners; the R-even strip prints one member per locus, at q* = 1.7382 the + member). So the open case is even N ≥ 6, and it splits by species.

**The real-λ species (the seed proper) is excluded at N = 6 and N = 8 on the measured 16-point q-grid, 0.001..51.2 in octic q, far beyond the window on both sides.** The mechanism: neither reflection sector is self-conjugate at even N (the sector-swap above), so realness is not sector-protected. The counting endpoints leave a loophole the window scans never probed: at N=8, r(0⁺) = r(∞) = 6 (Z₃ = 2, the 2cos(π/9) family, the smallest even N with resonances), so six kernel modes *could* have been literally real near the endpoints and forced finite-q collisions outside [0.2, 3]. Measured ([`even_n_literal_real_count.py`](../simulations/even_n_literal_real_count.py), gated): they are not. At N=6 the literal real population is **zero at every q** (first-order lift, ratio 2.000 per q-doubling). At N=8 it is the **constant two**: the two zero-frequency fusion-resonance AT strands, both pinned exactly at λ = −6, the rate-window *edge*, where the window-edge lemma proves semisimplicity, so this pair can never carry a Jordan block; the other four resonance modes lift at third order (|Im| ∝ q³, ratio 8.0 per doubling across three doublings, the resonant-N=11 mechanism) and return only as c/q asymptotics. A constant count has no real-to-complex transition at any probed q: no PT-breaking event on the grid (16 points, the doubling ladders at both ends). The count-blind tangency (a conjugate pair grazing the axis, the census's stated blind spot) is gated separately: the near-axis floor's minimum over the grid sits at the smallest probed q, the small-q lift envelope, with no interior grid point below it (the floor is not monotone inside, the large-q c/q descent begins there; the gate is the envelope minimum, nothing more), and every tangency measured in this story is diabolic (the N=9 onset geometry). At N=8 the pair splits one per R-sector at every grid point (rank 1 in each R-projection, gated), so no sector holds a real pair to collide even at the edge. This also settles the census question on the measured grid: `FindRealDefectiveByCountChange` counts real-root jumps, and the count never changes there, so the census would return silence uninformatively; correctly, `RealSeedCensusTests` instantiates odd N only.

**The complex-λ species is absent from the physical axis at N = 6 and N = 8 in the scanned strips, both R-parities.** The matched strip protocol of the N=8/N=9 discriminator, never before run at N=6 and never in R-odd:

- **N=6 R-even strip** (re[0.2,3], im ±0.025 commanded, header displays ±0.03, cell 0.01): 12 coalescences, all DIABOLIC, none at im(q) = 0. Tight zooms (cell 0.002) on the three near-axis members and the soft-exponent one: q = 0.6667−0.004i, 0.7778+0.007i, 2.5459+0.007i stay semisimple at their off-axis positions; 0.6471−0.018i stays diabolic; the zooms also surface two further in-box near-axis diabolics the cell-0.01 strip does not resolve, 0.6437−0.010i and 0.6616−0.022i. (The strip's 12 vs the broad box's 16: resolution- and band-dependent counts, as the path-6 section already notes; the strip band excludes the box's higher-|im| members and resolves extra near-axis ones.)
- **N=6 R-odd strip** (same box, `--rodd`): 12 coalescences, all DIABOLIC, and they are the exact q- and λ-conjugates of the R-even twelve, the cross-sector conjugacy of the mechanism section measured at the locus level.
- **N=8, both parities**: the R-even discriminator band's one exact-on-axis point, q = 0.6165 (λ = −4.617+1.497i, listed above with its defective/diabolic character unstated), classifies **DIABOLIC** under a cell-0.001 zoom (gap 1.5·10⁻⁸, exponent 0.95, identity loop): an analytic-continuation crossing. The new R-odd strip (re[0.2,3], im ±0.025, cell 0.01) gives 30 coalescences, the 24 in-box all DIABOLIC by EpCharacter; half of them carry soft exponents (0.29–0.60) and two a non-identity loop, the path-4-style per-point three-signal reconciliation not done, while the on-axis member, the conjugate partner q = 0.6165 (λ = −4.617−1.497i), is three-signal clean (exponent 0.95, identity loop); the strip's one defective label sits out of box at 1.7806−0.191i (the marginalia below). (Scope kept honest: the N=8 on-axis uniqueness statement is the discriminator band's, re[0.58,0.73] at cell 0.002; the strips at cell 0.01 add no on-axis point.)
- **N=6 defective positions** (the broad box re-run; the count above said "16 + 5" and never recorded the 5): counts reproduce exactly, and the four clean in-box defective EPs sit at q = 1.4159−0.217i (λ = −3.159+7.187i), 1.7226−0.580i (−4.477−0.971i), 1.9274+0.582i (−7.331+9.202i), 2.1288+0.323i (−4.415−1.897i), all |im(q)| ≥ 0.2; the fifth entry is a refinement drifted to the imaginary axis (0−1.602i, λ = −6.351), out of box, its identity loop and exponent 1.00 contradicting the defective label. One further candidate the zooms surfaced and the record must carry: q = 0.7764−0.011i prints "defective EP" in-box, and its non-identity loop alone would support that label, but its gap refuses to refine (2.63·10⁻⁴ unchanged from cell 0.002 to cell 0.001, four orders above every genuine coalescence here) and its exponent 0.09 fits neither species: a non-converging gap-field candidate, not a coalescence. It is off-axis regardless.

**Why the axis is empty at even N ≥ 6, said with the right mechanism.** Defective loci are the zeros in q of the discriminant of the R-sector that carries them — the sector's, not the block's (every full-block discriminant is real by the bipartite gauge, the (1,3) census section below, so block-level reality discriminates nothing), and the *residual* sector disc where κ=0 clusters sit (the raw sector disc is identically zero there, the (1,2)@N=6 sectors included with three exact pairs each; the certificate in the verdict already runs on the AT-stripped residual). A real zero of a *real* discriminant is codimension 1, generic; a real zero of a *genuinely complex* discriminant needs Re and Im to vanish at the same real q, codimension 2, generically absent. The spec's §B3 measured which case each N is: sector disc-reality holds exactly where a within-sector antiunitary acts, so N=4 (the self-fold T = P·K) has a real sector discriminant and four real defective loci, while the **N=6 (1,2) sector residual discriminant is genuinely complex** (the sector's, which is where the loci live; the census section below shows every full-block disc is real by the gauge), and no within-sector antiunitary exists in the *(1,2)* block at even N ≥ 6 to change that (the (1,3) block at N=6 carries one, the census section below). The spec is local; the numbers are re-gated in the committed verifier ([`even_n_literal_real_count.py`](../simulations/even_n_literal_real_count.py), the disc-reality block): on the R-even sector, arg-from-real at machine floor for the controls N=4 and N=5 (10⁻¹⁴/10⁻¹³) against O(1) at N=6 across q = 0.4/1.0/2.0 (the O(1) side's specific digits later measured to be a noise angle, the sector carrying exact κ=0 degeneracies; the (1,3) census section holds the correction and the filtered, similarity-invariant read). The self-fold is block-scoped, not N-scoped: fold legs fix any (p, N/2) block (§7(b), even N), and it is the *(1,2)* block that is self-folded only at N=4. Where the quartet's mechanism goes at N=6 is therefore not "nowhere" but *one block over*, (1,3); that follow-up ran the same evening and found the sibling population, at least thirteen real-q defective loci, and found the real-λ half of it reaching beyond (1,3) into every p+q-even block probed, so "one block over" names the fold-fixed census block, not an exclusive home (see "The (1,3) block at N=6" below; the ladder walks (1,2), so what a walk from a (1,3) seed reads is the section's own follow-up).

**Verdict, scoped.** At even N = 6 and N = 8 the (1,2) block has no real-q defective EP: the real-λ species excluded on the measured 16-point q-grid 0.001..51.2 octic (gated verifier; the one real population is a constant window-edge pair, one per R-sector, that the window-edge lemma keeps semisimple; the tangency blind spot gated by the floor-minimum envelope), the complex-λ species absent from the axis in the scanned strips (both parities, window re[0.2,3], |im q| ≤ 0.025, cells 0.01/0.002/0.001, completeness not claimed, and the gap-field seeder can mask, as the N=7 census recovery above showed). At N=6 this is no longer a scan result: the gcd route (the disc's real and imaginary parts, two polynomials over ℚ, certified coprime mod a degree- and valuation-preserving prime) ran on the fold-resultant machinery on 2026-08-10 and closed it **window-free**, stronger than the [0.2, 3] Sturm count it was named as: `FoldResultantCertificate.CertifyDiscReImGcd` certifies that no real q ≠ 0 carries *any* repeated Λ-root of the (1,2) residual, both parities (certifying prime 1073741833, deg Re 926 / v 536, deg Im 925 / v 537, gcd degree 0, gate `N6_Certificate_NoRealNonzeroCoalescence`, run `dotnet test compute/RCPsiSquared.Core.Tests -c Release --filter "Category=SLOW_EVEN_DISC"`; the N=4 real-disc control fails the certificate closed, as it must, four real loci existing there). With the AT-seam block-diagonality (no Jordan chain crosses), the AT side itself semisimple at every q (per rate sector the dephasing diagonal is one scalar, so the restriction is r₀·I + iq·K|_W with K diagonal-similar to a real symmetric matrix, diagonalizable on every invariant subspace, so disc_Λ(F_res) misses no defect), and the q = 0 block semisimple, the N=6 statement is a theorem: **no real-q defective EP of the (1,2) block at N=6 exists at all**. On 2026-08-12 the SAME certificate ran at N=8 and the statement is a theorem there too, resting on the same three ingredients, all N-general (the gcd certificate on F_res; the AT-seam block-diagonality with the AT side semisimple at every q, so disc_Λ(F_res) misses no defect of the block; the q = 0 block semisimple, the certificate being silent at q = 0 where D carries its multiplicity-3730 root): **no real-q defective EP of the (1,2) block at N=8 exists at all** — and, within the residual, stronger: no real q ≠ 0 carries *any* repeated Λ-root of F_res, so no exactly-on-axis diabolic *of the residual* either (the AT factor keeps its own exact degeneracies, the window-edge strands among them: semisimple, diabolic points of the block by construction, untouched by this statement). Certifying prime 1073741833, deg Re 6086 / v 3730, deg Im 6085 / v 3731, gcd degree 0, 3166 primes past the lc-divisor cap 3165; gate `N8_Certificate_NoRealNonzeroCoalescence`, both parities (run `dotnet test compute/RCPsiSquared.Core.Tests -c Release --filter "Category=SLOW_EVEN_DISC8_DEVICE"`, ~2 h for the category: the layer-certificate gate plus the two gcd parities). The expected blocker never materialized: the design fork had assumed the "on-axis diabolic q = 0.6165" is a real root of the doubled disc layer, making the full-D gcd legitimately nonzero and forcing a simple-layer split over ℚ(i) — but the exact-im(q)=0 reading was the cell-0.002 band zoom's grid read, never an exact fact, and the certificate excludes it exactly: the coalescence the scan classified (diabolic, exponent 0.95, identity loop) lies off the axis, the full-D gcd is trivial, and the split (and the derivative-gcd count designed as its replacement) are retired unbuilt (the design note behind this is local, not in the repo; its review rounds' load-bearing findings live in the arc entry). Two exact by-products of the day stand on their own: the certified layer read at N=8 (deg_q D = 6086 and v_q = 3730 certified past the cap, layers [376, 990] at a both-attaining prime, MaxDiscMultiplicity = 2, which by the one-way lift closes every multiplicity-≥3 hideout at (1,2)@N=8 outright; gate `N8_Certificate_DiscMultiplicity`, ~37 min), and the conj-antiparity identity conj-coeff(D)(q) = D(−q) within each R-sector (L = A + iqK with real ingredients, so even D-coefficients are real and odd ones purely imaginary, Re D even in q and Im D odd, real roots pairing ±q; it forces the parity PATTERN, and with the certified deg/v it pins Re's 6086/3730 exactly, the Im values 6085/3731 adding only the adjacent coefficients' non-vanishing; the committed N=6 pins 926/536/925/537 instantiate the same pattern). The consequence for the sideways ladder: at N=6 no starting *(1,2)* locus exists at physical coupling (the theorem above; the chain reading from a (1,2) seed there is input-free by theorem, not untested for want of an input, while the (1,3) census below restores an input at least thirteen loci strong one block over), at N=8 none exists (the same theorem, 2026-08-12), and N=4 is the even N where the walk RAN (same day, [`eta_ladder_chain_n4.py`](../simulations/eta_ladder_chain_n4.py), gated, all four loci; ported to a C# gate the same day, `SidewaysSpinLadderChain.RunN4` behind `Gate_N4` in `SidewaysSpinLadderGateTests` and a live witness node, with the port's calibration facts recorded in the arc entry): **band and fold freight share the four sectors**. The freight is the same band + fold pair as at odd N (§7's holomorphic fold −λ−2N), and on the line Re λ = −4 the fold value is conj(λ*), so foldset = bandset = the confined 4-orbit by the walk's own definitions: four ℓ = 1/2 doublets (two S⁺ chains × two conjugate values) on the four shared sectors, each transporting its one interior rung at the CG norm 1.000000000, dying at highest weight at the eigensolver floor (ratio ~10⁻¹⁵, survivors = 4 = dim target), CONTROL exactly 0.0. The odd-N seat identity 4(N−2) = |chains| × interior does not re-instantiate: the two η/Φ chain interiors are the same four blocks at N=4 (η, as at odd N, not separately norm-measured), and the block-level sharing is §7's overlap-4 fact already; what the walk adds is the value-level λ-sharing across all four sectors and the transport of both conjugate members (the twin's *presence* per block is free, block spectra being conjugation-closed; the sharing and the transport are the measured content). Block-local T-conjugacy (T diagonal, so the reduction's bipartite similarity runs per block, every N) corroborates and is not even-N-specific; what even N adds is that its loci are complex-λ (the real-λ species excluded above), so an even-N walk always carries the pair split open, while the odd-N seeds are real and the pair is invisible there. Instrument marginalia worth keeping: in tight zooms, points OUTSIDE the commanded box occasionally flip their diabolic/defective label (0.6120+0.025i, 0.8098−0.033i, and the N=8 R-odd out-of-strip 1.7806−0.191i read "defective" with identity loops or exponents ≈ 1 contradicting it); trust in-box classifications only, and treat a gap that will not refine as no coalescence at all.

### The (1,3) block at N=6 (2026-08-10 evening): the quartet's mechanism found one block over

The mechanism paragraph above ends on an offered follow-up: the self-fold fixes (p, N/2) blocks at every even N, so where the N=4 quartet's mechanism goes at N=6 is the (1,3) block. This section runs it. The sweep first, stores by name: [PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) §7 holds the fold lattice and its consequence (b), a block fixed by a fold leg (q̃ = N/2, even N) has an s-symmetric spectrum, gated for every (p,3) block at N=6 at one complex coupling, tolerance 10⁻⁷ (`BlockLatticeFoldGroupTests`); the antiunitary leg itself is `F89CrossFoldSimilarityClaim`, machine-zero N=4..9, general in both weights; the disc-reality criterion (real exactly where a within-block antiunitary acts) is the multi-sector monodromy spec's §B3 prior, asserted across the stores (this file's mechanism paragraph included) and derived in none; the measuring device is [`even_n_literal_real_count.py`](../simulations/even_n_literal_real_count.py)'s arg-from-real; the ground truth for validation is `ReferenceDefectiveLoci` / [`eta_ladder_chain_n4.py`](../simulations/eta_ladder_chain_n4.py) (the N=4 quartet); `docs/CAUGHT_ERRORS.md` holds the detector lesson used below (B1, 2026-07-01); the F-registry's F125/F142 entries contain no "(1,3)" at all, [F89_MULTI_SECTOR_MONODROMY](F89_MULTI_SECTOR_MONODROMY.md) holds (1,3) only as an N=5 braid member at (1,2)'s loci, and (1,3) is absent from [PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md)'s N=6 12-set (which is containment-only, the proof's own scope note: absence there is not an exclusion); the open-arcs ledger holds the one forward pointer; `docs/GLOSSARY.md` and `fw.Confirmations` (24 entries) return nothing on (1,3), fold-line loci, or defective censuses. No census, scan, or discriminant of (1,3) at N=6 existed anywhere in the repo. Every number below is gated by one verifier, [`disc13_n6_census.py`](../simulations/disc13_n6_census.py) (the block builder is [`eta_ladder_blocks.py`](../simulations/eta_ladder_blocks.py)'s weight-general `build_L`; γ = 1 per site, octic q, J = 2q; run `python simulations/disc13_n6_census.py`, ~9 min quiet), except where a sentence is marked as a review-round measurement.

**The §B3 prior gets its one-line derivation, and it is gated entry-wise.** At real q the self-fold on (1,3)@N=6 is the exact rearrangement L = −P_f·conj(L)·P_fᵀ − 2N·I with P_f the bra-complement permutation b ↦ b̄ (gated `== 0.0`, no eigensolver: both sides hold identical floats). So the spectrum is closed under λ ↦ −λ̄ − 2N, every eigenvalue difference maps to the negated conjugate of a difference, every squared difference to a conjugated one, and the discriminant to its own conjugate: **a self-folded block's discriminant is real at real coupling**, which is §B3's criterion with the antiunitary named. P_f commutes with the site reflection (gated `== 0.0`), so the fold acts within each R-sector and the *sector* discriminants inherit the reality: measured at machine floor (arg-from-real ≤ 5·10⁻¹² across octic q = 0.4/1.0/2.0, both sectors) against the failing control, the (1,2)@N=6 sectors at 0.40–1.36 rad, an eleven-order split. Same verdict as the committed (1,2) gate's, and a correction to that gate's numbers found while checking: [`even_n_literal_real_count.py`](../simulations/even_n_literal_real_count.py) probes the (2,1) block (the conjugate twin of (1,2): sector spectra conjugate, the read equivalent) at the carrier knob (its q = J, the factor-2 book) with **no pair filter**, and its (2,1)@N=6 sector carries three κ=0 degenerate pairs of its own, so its raw disc is identically zero and its quoted 0.50–1.13 rad are the angle of rounding noise, not a value: they move under a plain orthogonal similarity of the same matrix (0.575 → 0.447 in one draw; the moving is the content, the landing value is draw-dependent), while the filtered read is invariant (1.14 there). The committed gate's floor side (N=4/N=5 real) is untouched and its O(1) side still fails as it must, but for the noise's reason; retrofitting the pair filter there is a recorded follow-up. One scoping caveat the gauge (next paragraph) forces: 𝒟∘K makes every FULL block's disc real at any (p, q̃), so §B3's criterion carries content only at *sector* level, where two antiunitaries can pay for the reality: the gauge (whenever (p+q)(N−1) is even) and the fold (whenever the block is fold-fixed, P_f commuting with R). At (1,3)@N=6 either suffices; at (1,2)@N=4 only the fold acts within the sector, which is why N=4's quartet lives in a sector that is *not* a real family.

**The (1,3) sectors are conjugation-closed, and the cause is the bipartite gauge, not the fold.** Each R-sector spectrum of (1,3)@N=6 equals its own conjugate as a multiset (nearest-neighbour residual ≤ 2·10⁻¹³): the sectors are **real matrix families**, while the (1,2) sectors at N=4 and N=6 are conjugate-*swapped* (residual O(1), the cross-sector conjugacy above). The cause has a one-line derivation, gated entry-wise: the staggered diagonal metric 𝒟 = diag((−1)^(σ(a)+σ(b))), σ the sum of occupied site indices, satisfies 𝒟·L·𝒟 = conj(L) **exactly on every block** (at real J the composite of [PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) §7's ingredients (iii) conj L(q) = L(−q̄) and (iv) 𝒟L(q)𝒟 = L(−q); the β-exotic exclusion already leans on it as its metric at p+q = 3, calling it T there, [`BetaExoticPerNExclusionClaim`](../compute/RCPsiSquared.Core/Symmetry/BetaExoticPerNExclusionClaim.cs), not to be confused with this file's fold antiunitary T = P·K), and 𝒟 commutes with the site reflection iff **(p+q)(N−1) is even**, anticommutes otherwise (both gated `== 0.0`; §7's grading clause already carried "𝒟 commutes with R up to a block-global sign" without saying when the sign is −1 — the criterion completes it). So a block's R-sectors are conjugation-closed whenever p+q is even or N is odd (derived; on the other parity the gauge swaps the sectors, and the measured non-closure of the four probed blocks follows — a second closing antiunitary is excluded by nothing on the page, only unobserved): the (1,3) closure is the even parity of p+q = 4, and the criterion subsumes this file's odd-N effect (at p+q = 3 it reads "N odd") in one character. Self-foldedness is neither necessary nor sufficient for it: (0,3) and (2,3) at N=6 are fold-fixed like (1,3) yet conjugate-swapped (p+q odd; gated), and (1,3)@N=4 is closed without being self-folded. The division of labour: the **gauge** makes the sector a real family; the **fold** pins loci to the line and s-doubles the ones off it.

With both acting within the sector, write μ = λ + N: the sector spectrum is closed under μ ↦ μ̄ (gauge) and μ ↦ −μ (the holomorphic s = fold∘conj), a Klein four-group, and its orbits fix the locus multiplicity: μ = iy gives the conjugate pair (λ, λ̄) on the fold-fixed line Re λ = −6, μ = x real gives the s-pair (λ, −2N−λ) symmetric about it, and only μ = 0 (λ = −6 exactly) is a singleton. So away from λ = −6 every defective locus is **doubled**, every disc zero the census meets has even order, and the odd-order sign-change detector (the Sturm-style read a real disc invites, and the one that finds all four N=4 loci as sign flips) is structurally blind here: the verifier gates **zero sign changes** over [0.02, 8.0) in both sectors despite the thirteen loci, against exactly **four** at (1,2)@N=4 R-even, one per known locus, where the fold's antiunitary alone acts within the sector, its line is pointwise fixed, and loci are single odd-order zeros (the conjugate partner living in the other sector). The singleton escape hatch λ = −6 is named, and on the scanned grid channel B closes its dangerous half: a *defective* locus exactly there has μ = ±√(a(q−q\*)) with a real (conjugation- and s-closure leave no other local form), the pair real on one side of q\* and a conjugate pair on the other, so the exactly-real count flips by 2 and the fully-enumerated transition scan would see it. What actually sits at −6 are diabolic crossings, harmless to the even-order claim (transversal crossings are order-2 zeros anyway): three per sector in the printed inventory (q\* ≈ 0.475, 0.700, 3.455 / 0.458, 0.747, 1.205) plus one in the printed rejected bucket (q ≈ 0.544, real-real, exponent 1, classified by a review round), all exponent 1. One more instrument fact the verifier gates around: the (1,3) R-even sector carries two persistent κ=0 AT triples (λ ≡ −4 and λ ≡ −8 at every q, ×3 each, physical units γ = 1), so the raw sector disc is identically zero and every disc read here filters pairs below 10⁻⁸.

**The detectors, and the lesson their validation taught.** Channel A: candidates are local minima of the collapsed min-gap UNION local dips of log|disc|; each candidate is trisection-refined, kept only below gap 10⁻⁶, classified by the gap-scaling exponent (½ = defective √-branch, 1 = diabolic crossing; the verifier's exponents read 0.500 on every locus against a 1.0000..1.0006 diabolic band, so any threshold between decides identically), and its partner pair checked to coalesce at the same q\* (given the exactly-gated closure the partner's coalescence is derived; the float check confirms it at the refinement floor, gated below 10⁻⁶). Gate: on (1,2)@N=4 R-even, channel A finds **exactly** the four known loci 0.460212 / 0.854438 / 0.857458 / 1.738181, all exponent 0.500, and no other √-branch locus. The lesson, caught by a referee round of this section's own review: **a detector's validation transfers only to the species its ground truth contains.** N=4's quartet is all fold-line complex loci, and channel A, validated complete on them, missed four of the five real-λ loci on (1,3)@N=6 *inside* the validated grid (one of them sat in its rejection bucket, a dip candidate whose trisection walked to a neighbouring gap and discarded it; every rejection is printed now, and a per-sector gate pins that no kept coalescence goes unclassified). The rejection bucket has a second, structural feeder: `collapse` merges an *exactly-converged* coalescing pair, `features` then reads the next gap, and the refiner rejects the point — the known N=4 diabolic q ≈ 0.6590 lands there (printed), as does the λ = −6 real-real crossing q ≈ 0.544; so the diabolic inventory is an inventory, not a census, and the counts everywhere in this section are floors. The instrument that closes the gap was already in this file: **channel B, the PT-break real-count scan** of the real seed census ("immune to the closest-pair masking"): count the exactly-real sector eigenvalues along the grid, bisect every count transition, classify the merging pairs. Channel B is complete for its species up to an exact in-cell cancellation (the count-blind tangency the committed gate already names); channel A's fold-line completeness has no independent check beyond grid density and the N=4 validation, which is why the totals below are a **floor**; the exact layer read (the section after the walk) has since pinned the structure those loci live in (its measured layer degrees are floors too, see there), and the real-root count still stays on the list. One more instrument lemma the verifier now gates so the filter hides nothing: the R-even κ=0 AT triples are semisimple (geometric = algebraic = 3 at λ = −4 and −8), and R-odd carries no exact degeneracy at all.

**The census.** Octic q, [0.02, 8.0): channel A at step 0.001, channel B transitions bisected (45 halvings of a 10⁻³ cell); edges [0.002, 0.02) and [8.0, 25.0) gated empty on both channels. (The floor below has since become an exact count: the real-root-count section after the layer read closes it at **9 R-even + 6 R-odd**, the two loci both channels missed located and classified there.) **At least thirteen real-q defective loci** (13 found: 8 R-even + 5 R-odd by sector; by species, eight fold pairs on the line (6 R-even, 2 R-odd) and five conjugate pairs on the axis (2 R-even, 3 R-odd) — two different 8/5 splits, coincidentally equal; a floor, not a count — channel A's rejections are printed, not exhaustively re-classified, its collapse threshold rejects exactly-converged coalescences, and no upper bound exists short of an exact layer split; the mod-p layer read gives floors only), every found q\* pinned in the verifier (channel-B λ values are the merging-pair positions near q\*, read at q\* or q\* ∓ 10⁻⁶ with ~2·10⁻⁶ drift between the two; their gated content is the transition, the √ exponent, and the pair sum −12):

| sector | q\* | λ | species |
|---|---|---|---|
| R-even | 0.4630013 | −6 ± 0.3864821i | fold pair on the line, conj-doubled |
| R-even | 0.6400121 | −6 ± 1.2687899i | fold pair on the line, conj-doubled |
| R-even | 0.7026159 | −6.5624176 / −5.4375824 (both real) | conj pair on the axis, s-doubled, PT 12→8 |
| R-even | 0.7450837 | −6 ± 1.4621529i | fold pair on the line, conj-doubled |
| R-even | 0.9184132 | −6 ± 7.7721940i | fold pair on the line, conj-doubled |
| R-even | 1.5109958 | −6 ± 0.9288088i | fold pair on the line, conj-doubled |
| R-even | 3.0922209 | −6 ± 1.5169893i | fold pair on the line, conj-doubled |
| R-even | 3.3536850 | −6.0787877 / −5.9212123 (both real) | conj pair on the axis, s-doubled, PT 8→12 |
| R-odd | 0.4635508 | −6.2680671 / −5.7319329 (both real) | conj pair on the axis, s-doubled, PT 12→8 |
| R-odd | 0.8029541 | −6.6051577 / −5.3948423 (both real) | conj pair on the axis, s-doubled, PT 8→4 |
| R-odd | 1.2273677 | −6.3760926 / −5.6239074 (both real) | conj pair on the axis, s-doubled, PT 4→0 |
| R-odd | 1.9567664 | −6 ± 7.6439946i | fold pair on the line, conj-doubled |
| R-odd | 3.2608458 | −6 ± 25.1047962i | fold pair on the line, conj-doubled |

Diabolic crossings are abundant beside them (10 R-even, 12 R-odd on channel A's grid, exponent 1, inventoried, printed, and count-gated by the verifier, not gated as loci; two of R-even's ten sit at real λ off the line): the fold line is where fold-partner strands cross, some sliding along it and some passing through, the N=4 picture of the mechanism section with the line moved from Re = −4 to Re = −6, the fixed line of the fold-partner map −λ̄ − 2N (at N=4 the fold's own antiunitary did the pinning, the sector there not being a real family). The λ geometry is the two-species geometry the two antiunitaries force, not a repeat of N=4's all-on-the-line table. A **fold pair** (λ, −λ̄−2N) meets where Re λ₁ = Re λ₂ = −6, ONE real condition, codim 1: fold-symmetry breaking, the merging partners approaching as exact mirrors about the line, conjugation doubling the locus with the twin pair at the conjugate position. A **conjugate pair** (λ, λ̄) meets where Im λ = 0, the other single real condition: conjugation-symmetry breaking, the pseudo-Hermitian PT species proper (the word earned here because the sector really does carry a within-sector antiunitary squaring to +1), s doubling each such locus with its mirror pair, the two measured pair means summing to −2N = −12, the R-odd real population walking 12 → 8 → 4 → 0 down its three loci. A generic complex coalescence off both lines would be a size-4 Klein orbit, TWO real conditions, codim 2 in the one real knob, and none shows. Five of the thirteen then-found loci (still five of the exact fifteen: both later-found loci are fold-line) sit on the real axis where all four of N=4's stayed on the fold line.

**Scope, kept honest.** This is a float census, the (1,3) sibling of the *strip-scan* stage of the (1,2) story, not of its certificate stage: coverage is the scanned grid (loci outside [0.002, 25) octic would not show, and the R-odd member at λ = −6 ± 25.1i says loci at large |Im λ| do occur at real q); the exact structure the loci live in landed the next morning (the layer-read section below), the loci themselves stay float-pinned. Two more scope facts from the review rounds (measured there, not gated by the verifier, which touches only (1,3)/(1,2)/(2,3)/(0,3)). The real-λ species is **not (1,3)-own**: every p+q-even sibling probed at N=6 carries real-count transitions in at least one sector ((2,4) and its fold image (2,2): 8 R-even / 11 R-odd on [0.02, 4); (1,5): 3 R-odd, none R-even), several at exactly (1,3)'s q\* (0.703, 0.464, 0.803, 1.227, 3.354 recur, the ladder's spectral containment at work), so "the quartet's mechanism moves to (1,3)" is right as *the fold-fixed block's census* and wrong as a placement-exclusivity: what is (1,3)-specific is the fold-line geometry, not the species. And the arg-from-real device is **one-sided**: the floor side is exact (the fold/gauge argument), the O(1) side a mod-π residue, suggestive only. The exact machinery was (1,2)-hardwired at exactly three joints (`BlockPencil`, `ClearedAtSectors`'s literal ×2-cleared rate list {−4, −12}, and the `AssertHoppingSymmetry` guard's `SymOrbitSizes` weights) when this census landed; the generalization ran the next morning and its exact layer read is the section after the walk ("The exact (1,3) layer read" below; the SCOUT read costs ~2.3 s per parity — the ~9 min guess was for a certificate-grade run, which remains unpriced). And because the disc here is REAL, `CertifyDiscReImGcd` is structurally the wrong tool for it (it must decline on a real disc, as its N=4 control does); the exact reality read (both mod-p embeddings agreeing) is part of the layer scout. The exact object the census called for was the **multiplicity-2 layer with the λ = −6 stratum handled separately**: with the doubling, a defective locus is a *double* root of the sector disc while a doubled diabolic crossing is a *quadruple* one, so away from μ = 0 the separation between them IS the multiplicity — but at λ = −6 an undoubled transversal crossing is also order 2 (the six inventoried λ = −6 diabolics would land in the same layer), so the stratum needs its own read, exactly as at N=4 (disc(F₈) = c·q²⁴·(3q⁴+q²−1)²·P₁₀(q²): squared layer diabolic, simple layer defective; an exact real-root run on P₁₀ itself does not exist in the repo); a squarefree part or a bare sign count would erase precisely that separation, the lesson `docs/CAUGHT_ERRORS.md` recorded 2026-07-01 (B1: a discriminant-zero detector answers the weaker question). Both reads have since run; what they returned is the layer-read section.

**What it restores.** The arc's even-N chain walk stalled at N=6 for want of an input: the (1,2) axis is empty by theorem. The census restored the walk's inputs one block over, thirteen by its own channels, fifteen after the exact count (and the cross-block recurrence of the real-λ positions says more sit in the other p+q-even blocks). The walk from a (1,3) seed was new territory (the N=4 walk seeded at (1,2); from (1,3) the S⁺ ladder reads into (2,2) and Φ into (2,4)), with the doubled-pair structure predicting the even-N split-open pair transport of the verdict paragraph in both freight values at once; it ran the next morning and is the next section.

### The walk from a (1,3) seed (2026-08-11, at fifteen seeds 2026-08-12): both freights, both chains, one norm

The prediction the census section closes on ran the next morning. The sweep first, stores by name: the input table is this file's census section (the thirteen (q\*, λ) pins, joined 2026-08-12 by the exact count's two Sturm-only fold loci, their λ read with the census's sector + pair-filter device at the Sturm-exact q\* (not the census's detection chain: no trisection refinement, no exponent classification) and gated in the verifier at sector level: each locus's own R-sector holds exactly two truncation-split pairs, the conjugate-doubled fold pair, λ = −6 ± 1.7171734i at q\* = 0.6133162 R-even and −6 ± 3.0292680i at q\* = 0.4627729 R-odd, fifteen seeds in all); the chain bookkeeping is the arc entry (`sideways_spin_ladder` in `OpenArcsRegistry.cs`); the never-measured-Φ flag sits in four stores, all scoped to the two *canonical* η chains of the odd-N accounting (the arc entry's "the two eta chains carry their spin by that count alone, their transport norms untested", `docs/ANALYTICAL_FORMULAS.md` F142, `SidewaysSpinLadderClaim` at three sites, and its Runtime registration), so those stores stay correct as scoped and what this walk measures is an η-side chain those flags never covered; the multiplet reading is [PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) §6 (highest-weight annihilation) and the S⁺ operator [PROOF_FROZEN_BAND_SO4](../docs/proofs/PROOF_FROZEN_BAND_SO4.md) Lemma 2.3; the template is [`eta_ladder_chain_n4.py`](../simulations/eta_ladder_chain_n4.py); `docs/GLOSSARY.md`, `fw.Confirmations` and `docs/CAUGHT_ERRORS.md` return nothing on a (1,3)-seeded walk; no such walk existed anywhere in the repo. Every number below is gated by one verifier, [`eta_ladder_chain_n6.py`](../simulations/eta_ladder_chain_n6.py) (662 gates at the fifteen seeds, ~1 min; exit 0 iff all pass; the count is gate lines, the earlier 566 counted the same way), except two marked as run measurements. Three of this section's first-draft claims died in its own review rounds and are recorded at the end.

**The chains are new, and the fold maps one onto the other.** From (1,3) the S⁺ chain runs (0,4) → (1,3) → (2,2) → (3,1) → (4,0) along p+q = 4 = N−2 and the Φ chain runs (0,2) → (1,3) → (2,4) → (3,5) → (4,6) along p−q = −2: neither is one of the four canonical chains of the odd-N accounting (p+q = N∓1, d = ±1), so the seat identity 4(N−2) does not apply and is not claimed. The two share only the seed block, and the bra-complement fold f: (p,q) ↦ (p, N−q) carries the S⁺ seats onto the Φ seats exactly ((2,2) ↦ (2,4), (3,1) ↦ (3,5), (1,3) fixed, boundaries (0,4) ↦ (0,2) and (4,0) ↦ (4,6)): the Φ chain is the fold image of the S⁺ chain through the self-folded seed. The two su(2) weights, m = (p−q)/2 on the S⁺ side and m = (p+q−N)/2 on the η side, both read (1,3) as an m = −1 seat; a weight fixes m, never ℓ, and the block's ℓ content is a measurement, not a prediction: **each ladder on (1,3) has exactly two singular values, √2 on a 105-dim family and √6 on a 15-dim family** (gated as exact multiplicities), so the block decomposes under each chain's su(2) as 105 × ℓ=1 ⊕ 15 × ℓ=2 at m = −1, and across the whole five-seat chain the seat dimensions 15+120+225+120+15 = 495 = 15·5 + 105·3 + 105·1 close the bookkeeping with 105 ℓ=0 singlets at the middle seat. A first draft asserted ℓ = (interior−1)/2 as a rule; the ℓ=2 family on the same interior-3 chain refutes it, and what actually selects ℓ = 1 for the freight is **lowest-weight membership**: the seed eigenvectors have entering-rung projection at the floor (≤ 6·10⁻¹⁵ against the image of (0,4) → (1,3) resp. (0,2) → (1,3), gated per seed and per chain), while the AT strands project at 1.000 exactly where they carry ℓ=2 (gated). Death at m = +1 then follows from ℓ = 1, two rungs up.

**The freight and its twin, one formula for both species.** The N=4 script's twin is conj(λ\*), valid there because every N=4 locus sits on the fold line. The census's two species need the holomorphic s uniformly: twin = s(λ\*) = −2N − λ\* = −12 − λ\*, which on the line Re λ = −6 equals conj(λ\*) (a one-line identity given the gated fold-line pin, not an independent check) and on the real axis is the distinct s-partner (the other member of the census pair). What the verifier gates about the twin is substantive and lives elsewhere: the twin value sits in the (1,3) spectrum as a **second near-defective pair at the same coupling** (its own truncation-sized split, its own defective-grade non-normality, below), which is the census's doubling seen by the walk's instrument. The seed pair is found by census-targeted search, the two eigenvalues of (1,3) closest to the pinned λ: an untargeted global closest-pair search lands on the exact κ=0 AT degeneracies at λ = −4/−8 instead (a first-run measurement, not gated: split ~10⁻¹⁵ there against the truncation-band 1.8·10⁻⁵..1.6·10⁻³ of the defective pairs), the same flood the census's pair filter was born for, recurring one instrument over.

**What the ladder owns, and what the seeds own.** The walk's review rounds measured the split, and it is the N=4 σ_min caveat's stronger sibling (the N=4 entry's calibration fact (b): the full ladder passes at any generic q there too). The CG rung norm √2, the terminal death and survivors = 15 are **family properties, not locus properties**: with only two singular values available, every ℓ=1-family eigenvector rides √2, √2 and dies at the terminal rung, and the verifier gates 105 of 120 eigenvectors passing the full walk gate set at an off-locus, non-uniform-J control coupling with no locus anywhere near, on both chains (survivors = 15 is likewise the terminal rung's surjectivity onto the 15-dim boundary, rank 15 at singular value 2.0 exactly, gated; forced given CONTROL, not falsifiable by a locus). So transport-at-√2 certifies family membership, coupling-free, and the per-locus content is everything else: the pair sits at the census λ (≤ 5·10⁻⁶) with a split gated two-sided against the truncation model (2√(|a|·|δq|), |δq| ≤ 10⁻⁷: the census q\* carry 5·10⁻⁸ from their 7 decimals, the two Sturm-only q\* are isolated to 10⁻⁷; band 10⁻⁸..10⁻², measured 1.8·10⁻⁵..1.6·10⁻³, the lower edge seven orders above the exact AT degeneracies it excludes); the fold-line/axis pins hold at the eps·‖L‖ scale (gated relative, ≤ 100·eps·‖L‖∞); **both** freight values are simultaneously near-defective pairs and both are defective-grade non-normal, the departure from normality s of the restricted 2×2 measuring 1.4·10³..2.5·10⁴ × the split (gated > 100×; a semisimple degeneracy such as the AT triples has s ≈ 0, so this is the gate a mere near-crossing would fail); both freights are lowest-weight (the floor projections above); both are present in every seat spectrum of both chains (gated ≤ 2·10⁻³, the presence gate the N=4 template carried); member images are eigenvectors of the target blocks (Rayleigh residual ≤ 10⁻⁹ gated, 2.1·10⁻¹⁴..1.4·10⁻¹³ measured, quoted outward of the printed precision), which pins the transported eigenvalue to a pair member by exactness (implied, not separately gated); the mix rides the same CG norm (the in-plane robustness of the N=9 run; its Rayleigh residual is *not* gated against the split, which bounds nothing for a non-normal pair, the true in-plane worst being s itself); CONTROL (the intertwining residual over every rung of both chains, boundary rungs included) is exactly 0.0 at all fifteen couplings and at the control coupling. **The η-side norms land on the CG value**, measured here for the first time on any η-side chain, as a family fact of the (d = −2) chain: the canonical d = ±1 chains of the odd-N accounting remain unmeasured.

**The AT strands sort the death, and it is the family's, not the seed's.** A first draft read the negative control as "the highest-weight death is the defective seed's property"; the two cross-cases the draft never ran falsify that reading, and all four are gated now. Each rate value's exact 3-dim AT eigenspace (λ ≡ −8 and λ ≡ −4, dimensions gated) sits in the ℓ=2 family of one chain and the ℓ=1 family of the other: −8 rides S⁺ at √6, √6 and survives the boundary (ratio ~1) while the same eigenspace rides Φ at √2, √2 and dies at the floor; −4 exactly mirrored (Φ survives, S⁺ dies); all members of each eigenspace walked, all four case-gates per seed. Death sorts by multiplet family membership, nothing else: not by defectiveness, not by rate value. What stays curious and uninterpreted: which chain hosts which rate value's ℓ=2 seat (−8 ↔ S⁺, −4 ↔ Φ) is gated as a fact, unexplained.

**Scope.** Seed-targeted and float, like the census it consumes: the walk certifies per-locus structure (pins, splits, doubling, non-normality grade, lowest-weight membership) at the fifteen pinned loci and family structure everywhere; it detects nothing and adds no locus, and transport-at-CG alone would not distinguish a locus from any other ℓ=1 vector, which is why the off-locus control is part of the verifier. The chains walked are the (p+q = 4, d = −2) pair; the other p+q-even blocks the census's real-λ recurrence points at ((2,2), (2,4), (1,5) carry transitions at shared q\*) have their own chains and are not walked here. Errors of this section's first draft, caught by its review rounds and kept: the ℓ = (interior−1)/2 "rule" (refuted by the ℓ=2 family on the same chain), the "death is the seed's" reading of the negative control (falsified by the two unran cross-cases), and a mix-residual gate "bounded by the pair split" (no bound for a non-normal pair; the measured in-plane worst is 1.4·10³..2.5·10⁴ × the split, and the repaired verifier gates that ratio as the defective-grade signature instead; the fix-diff round then caught the repair misquoting this very range, its own minted number, as 2·10³..9·10³). The C# gate port of the walk itself (the `RunN4` pattern; distinct from the layer-read gate class of the next section) is the natural next brick and is not claimed done.

### The exact (1,3) layer read (2026-08-11): the census's structure theorem mod p

The census's named closing object ran the same morning. The sweep first, stores by name: the plan and the three (1,2)-hardwired joints are this file's census Scope paragraph and the arc entry (`sideways_spin_ladder`: `BlockPencil`, the `ClearedAtSectors` rate literal, the `AssertHoppingSymmetry` weights; what the literal would DO to a {−8,−16} block, silently clear nothing, is this generalization's own observation, recorded nowhere before); the layer machinery is `FoldResultantCertificate`, its committed (1,2) anchors being the pinned N=4 scout (52/24/[20,4], [`EvenNDiscLayerScoutTests`](../compute/RCPsiSquared.Core.Tests/F89PathK/EvenNDiscLayerScoutTests.cs)) and the certified N=6 degree/valuation 926/536 (the layers [124, 133] lived only as registry prose until now; the control below is the first place they are PINNED); the species/multiplicity dictionary is this file's census section (double vs quadruple away from μ=0, the μ=0 caveat); `docs/CAUGHT_ERRORS.md` B1 (2026-07-01) is why a squarefree part or sign count is the wrong read; `docs/proofs/` holds the fold lattice ([PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) §7, the source of the fold and gauge ingredients) and no disc-layer object; `experiments/` (null results included) holds the census and the walk sections of this file and no exact (1,3) read; `docs/ANALYTICAL_FORMULAS.md`, `docs/GLOSSARY.md` and `fw.Confirmations` hold no exact (1,3) object. No exact (1,3) read existed anywhere in the repo. Every structural number below is pinned in one gate class, [`Disc13LayerScoutTests`](../compute/RCPsiSquared.Core.Tests/F89PathK/Disc13LayerScoutTests.cs) (run `dotnet test compute/RCPsiSquared.Core.Tests -c Release --filter "Category=SLOW_EVEN_DISC13|Category=EVEN_DISC13_SCOUT"`, 16 gates, ~27 s + build; the runtimes print, they are not pins).

**The machinery, generalized with the trap removed.** `WeightSectorPencil(n, wKet, wBra, rOdd)` builds the exact ×2-cleared R-sector pencil of any all-2-cycle block (every reflection orbit a 2-cycle, guaranteed by one odd weight side at even N: a fixed mask needs mirror-paired bits; a fixed point throws). All orbits being 2-cycles, both parities carry the uniform metric BᵀB = 2·I, the sector operator BᵀLB has exact Gaussian-integer entries, and the hopping-symmetry guard runs unweighted. `ClearedAtSectorsFromPencil` DERIVES the AT rate list from the q=0 diagonal (distinct values, descending) instead of the (1,2) literal, so the silent-zero-sectors trap the arc recorded cannot recur, and the (1,2) path now delegates to it unchanged. Validation is the committed record itself: the generalized path reproduces the (1,2) scout pins at N=4 and N=6, both parities, and reads the disc-reality discrimination exactly (N=4 real, the antiunitary self-fold; N=6 genuinely complex: the two mod-p embeddings i ↦ r and i ↦ p−r disagree).

**The (1,3)@N=6 profile, both parities, two split primes (1073741833, 1073741857), identical at both, ~2.3 s per read:**

| sector | deg_q D | v_q | layers (mult 1, 2, 3, 4) | atDeg | resDeg | disc mod p |
|---|---|---|---|---|---|---|
| R-even | 1572 | 876 | [0, 108, 0, 120] | 18 | 42 | real (both embeddings agree) |
| R-odd | 2124 | 1188 | [0, 132, 0, 168] | 12 | 48 | real (both embeddings agree) |

Aggregation gated: 1572 − 876 = 2·108 + 4·120 and 2124 − 1188 = 2·132 + 4·168. Two theorems and a corollary become exact statements here, and the review rounds reshaped all three, twice each (below). **First, the polynomial fold**: EVERY odd Taylor coefficient of F_res at Λ = −12 vanishes EXACTLY over ℤ[i], so F_res is a polynomial in (Λ+12)², the within-sector fold not as a spectrum closure but as an algebra identity of the exact residual, no prime involved (holding at complex q too, where the census's spectrum-closure argument said nothing), available exactly where the gauge makes the coefficients real AND the fold acts within the sector; the N=4 (1,2) control (real disc, but gauge anticommuting with R at (p+q)(N−1) = 9 odd) reads no evenness, discriminating along the census's criterion. This gate was tightened twice in review: the first version read only the FIRST odd coefficient (∂F_res/∂Λ at −12, a₁ alone, which does not give evenness), the second checked all odd coefficients but only mod p; the landed gate is the exact one. **Second, the μ = 0 stratum carries no defective locus at all**: f(q) = F_res(−12, q) is a perfect square, f = c·u² with u squarefree (f-layers [0, 18] / [0, 24]). Two wrong reasons died in review before the right one stood. A first draft said "μ and −μ strands touch the line only together", circular given the evenness; the replacement said an odd-order f-root is the only defective signature, and the fix-diff round found ITS hole: an order-2 f-root has two sources, one transversal crossing of the line, or TWO coincident defective EPs meeting on it (each a simple branch, 1+1). The closing gate separates them: **u divides the multiplicity-2 layer** (deg gcd(u, L₂) = 18 / 24, gated both primes), equivalently gcd(u, disc_M G) = 1, and a double defective touch would make disc_M G vanish at that q and push the u-root into the multiplicity-4 layer instead. With that gated, all 18 / 24 μ = 0 coalescences are transversal (diabolic in the census's exponent sense: a disc read certifies root multiplicity, never geometric semisimplicity) and none is defective: the census's μ = 0 escape hatch, closed there only on the scanned grid by channel B, is now closed algebraically, window-free, complex q included. The N=4 control's f is squarefree ([8]), single strands crossing alone, their s-partners living in the other R-sector, and its u-gate returns the n/a sentinel, the evenness analysis being void there. **The corollary, the even-order layer profile**: the composition identity disc_Λ(F) = ±4^m·f·disc_M(G)² makes every odd-multiplicity root of D an odd-multiplicity root of f and conversely, so with f a perfect square, no multiplicity-1 or multiplicity-3 layer can exist: the census's Klein even-order statement (and the sign-change detector's structural blindness) derived rather than merely observed, and the identity chains all six numbers per parity against each other, LIVE off the production tuples (deg disc G = (1572−36)/2 = 768 and (2124−48)/2 = 1038, v = 876/2 = 438 and 1188/2 = 594, v_q(D) even being FORCED, and the stripped-layer identity 108 + 2·120 = 18 + 330, 132 + 2·168 = 24 + 444; gated). The layer accounting sharpens the census's floor, with the mod-p lift's direction stated rather than assumed: mod p, distinct true roots can only MERGE, so a measured layer DEGREE is a floor for the true one, and only the measured maximum MULTIPLICITY bounds the truth from above. With the u-gate above, exactly 18 / 24 of the measured multiplicity-2 roots are the μ = 0 diabolics, so the defective species live on the simple layer of stripped disc_M(G), **true degree 90 + 2k (R-even) / 108 + 2k′ (R-odd), k, k′ ≥ 0** (the two-prime agreement is evidence for k = k′ = 0, not proof; the only a-priori ceiling is the stripped degree 330 / 444, and the exact reconstruction the Sturm step needs anyway is what will pin the degree; closed at k = k′ = 0 the same afternoon, the real-root-count section below), the census's 8 + 5 real-q pins among them (most of the rest sit at complex q); the multiplicity-4 layer (measured 120 / 168) carries the doubled off-line diabolic crossings. A first wording here quoted the measured layer degree as a ceiling ("at most 90 / 108"), the one-way lift read backwards; a design review caught it the same day.

**Scope, upgraded to certificate-grade the same midday.** The scout reads above use the crude Bauer-Fike interpolation bound and two-prime agreement; the D-device itself (`CertifyDiscMultiplicity`'s lc-divisor and valuation certification) was then generalized as `WeightCertifyDiscMultiplicity` (same proof shape, nothing weakened: the leading-form m_D = 51 / 42 sharpens the bound to 1620 / 2172, the Hadamard lc-divisor bound caps the primes that could lie at 923 / 1209, and 924 / 1210 primes were sampled past it), and **both parities CERTIFY**: TrueDiscriminantDegree 1572 / 2124, TrueQValuationD 876 / 1188, the layer profile identical to the scout at the certifying prime (a mod-p read, its degrees being floors per the section above), MaxDiscMultiplicity = 4 (a one-way-upward mod-p bound; the "Klein-forced ceiling" reading is the sections above, not the device). An earlier clause here read "a Puiseux-3/2 being hideable inside the multiplicity-4 layer", and the Sturm design's review decided it with both disc orders on the table: that wording named the order-3 species for the order-2 hideout. The β-exotic and its exponent-3/2 twin carry disc order 3 ([F89_BETA_EXOTIC_GENERICITY](F89_BETA_EXOTIC_GENERICITY.md), "ord 3 each"), and the exact (Λ+12)²-evenness forces equal local order at the two fold-doubled collision points, so an order-3 point contributes order 6 > 4 to D off the μ = 0 line and is excluded outright by this very certificate, while on the line every u-root has order exactly 2 (u squarefree plus the u-gate). What the multiplicity-4 layer could genuinely hide was the 3×3 Jordan cubic branch (disc order 2, doubled to exactly 4); the psc device of the real-root-count section below has since closed that off the line, and on the line the u-gate closes it (a Λ = −12 multiplicity ≥ 3 needs M = 0 double in G, i.e. a common root of u and disc_M(G), excluded by gcd(u, disc_M G) = 1; parity alone would still admit a J₃ ⊕ J₁ inside multiplicity 4). Gate class [`Disc13CertificateTests`](../compute/RCPsiSquared.Core.Tests/F89PathK/Disc13CertificateTests.cs), Categories SLOW_DISC13_CERT (the N=6 members, ~7 s / ~14 s per (1,3) parity since the 2026-08-13 pair of speedups, against ~50 s / ~109 s before them; the certified values are unchanged, what changed is the sample count per prime, halved by the q-even law, and the prime loop, now parallel across cores) + EVEN_DISC13_SCOUT (the fast N=4 control); the weight path reproduces the committed (1,2) certificate values at N=4 and N=6 as its controls, and its per-point guard rebuilds the block fresh at q0 = 2, 3 (the AT factor stays single-route, the one leg narrower than the (1,2) original, recorded in the guard's summary). These certified invariants are exactly what the Sturm step will lean on, as the (1,2) gcd certificate leaned on the (1,2) device's. What the read does NOT yet deliver is the census's exact closing statement: the number of REAL roots of the multiplicity-2 layer in the physical window: that needs a real-root count (the Sturm route) on an exactly reconstructed layer polynomial, the same closing step the (1,2) story needed (and the once-planned N=8 ℚ(i) simple-layer split would have shared, before the 2026-08-12 certificate retired it unbuilt). The thirteen census loci stayed float-pinned until the same afternoon; the real-root-count section below makes every locus an exact root of the landed simple layer and closes the count at fifteen.

### The exact real-root count (2026-08-11 afternoon): the census closed at 9 + 6

The layer read's named missing step ran the same afternoon, and the census's floor is now an exact count of the codim-1 defective species (the residual hideout accounting is the psc paragraph below). The sweep first, stores by name: the input invariants are the certificate section above (deg/v of D certified 1572/876 and 2124/1188, the exact F_res with its ℤ[i]-evenness, the u-gate) and the arc entry (`sideways_spin_ladder`, which had recorded the gap verbatim, "no Sturm chain exists in the repo yet", and now carries the closed step with that phrase kept as the record); the design that shaped the pipeline is a three-round-reviewed local note (docs/superpowers/ is gitignored; its object chain, the §1a defectiveness lemma of order parity + Kato, and the route decision are summarized here because the note will not survive a clone); `docs/CAUGHT_ERRORS.md` B1 (2026-07-01) is why the count runs on the multiplicity-typed layer and never on a squarefree part of D; the repo's one committed exact-Sturm precedent is [`r90_frozen_divisor_gate.py`](../simulations/r90_frozen_divisor_gate.py)'s Fraction chain with the w = z² trick (the gate G13 of the R90 divisor entry in `docs/ANALYTICAL_FORMULAS.md`), the sympy precedent is [THE_EXCEPTIONAL_COUPLINGS](THE_EXCEPTIONAL_COUPLINGS.md); `docs/proofs/` holds the fold-resultant certificate prose and no real-root object; `fw.Confirmations` nothing; `docs/GLOSSARY.md` nothing beyond the q vs Q factor-2 book (all q below is the census header's octic q, quoted: "gamma = 1 per site, octic q, J = 2q"). No real-root count of any (1,3) object existed anywhere in the repo. Every number below is pinned in one gate class, [`Disc13SturmTests`](../compute/RCPsiSquared.Core.Tests/F89PathK/Disc13SturmTests.cs) (Categories EVEN_DISC13_SCOUT for the fast controls, SLOW_DISC13_STURM for the two (1,3) parities at ~10 / ~47 min plus the (1,2)@N=6 decline control; run `dotnet test compute/RCPsiSquared.Core.Tests -c Release --filter "Category=SLOW_DISC13_STURM"`), with two exceptions said now: the 10⁻⁷ root isolation of the two new loci is the recorded sympy oracle's, and the runtimes are measurements.

**The route, and the derivation that paid for it.** The one object needing reconstruction is disc_M(G) (G the exact compression F_res = G((Λ+12)², q), monic in M, deg 21/24; everything else is an exact function of the already-exact F_res). Route A, exact integer-node interpolation, was timed quiet at 870 / 2371 ms per node, which put the R-odd full grid (1039 nodes) over the design's budget; what brought it back under was the **q-evenness of disc_M(G), derived rather than scouted**: the census's bipartite gauge T = diag((−1)^(σ(a)+σ(b))) satisfies T·L(q)·T = L(−q) as a polynomial identity (a hop flips the bipartite parity, so the q-linear entries change sign; the q-free dissipator diagonal stays), and at (1,3)@N=6 the gauge acts within each R-sector ((p+q)(N−1) = 20 even), so the sector charpoly, the AT divisor (T pairs the strands a+bq ↔ a−bq), F_res, G's coefficients and disc_M(G) are all even in q. Gated exactly on F_res (every odd-q coefficient zero over ℤ), with the N=4 (1,2) control measuring False as the derivation demands (character 9 odd, the gauge swaps the sectors): the mirrored grid then takes the negative node half for free and the evaluations halve to 385 / 520. The landing runs entirely over ℤ (integer forward differences, the (2k)!-scaled falling-factorial Newton form, exact final division), and its gates: landed deg/v = 768/438 and 1038/594 (certified-backed through the composition identity disc_Λ(F_res) = (−4)^m·f·disc_M(G)², proven as algebra in the design and gated here at the POLYNOMIAL level mod both committed primes, sign included); fresh verification nodes including a negative one (the one end-to-end check of the mirroring); gcd(u, disc_M G) = 1 promoted to exact over ℚ; landed evenness exact. The **squarefree split is proven, not assumed**: primitive-PRS gcds over ℤ deliver stripped disc_M(G) = A·B² with the reconstruction identity P = A·B·B compared coefficient for coefficient over ℤ, sign included, plus gcd(A,B) = gcd(A,A′) = gcd(B,B′) = 1, so the layer-read's floor "true degree 90+2k / 108+2k′" closes at **k = k′ = 0**: deg A = 90 / 108, deg B = 120 / 168 exactly. The Sturm chain itself is the classical one built from sign-fixed pseudo-remainders (every element a positive-scalar multiple of the classical chain member, the one discipline a signed chain needs), evaluated at −∞, 0, +∞ and at rational brackets; that is a deliberate departure from the design's named Sturm–Habicht form, and what carries it in place of the subresultant normalization is the N=4 control reproducing the committed quartet plus the external oracle below.

**Controls, all committed shapes reproduced.** (1,2)@N=4 R-even end to end: D landed exactly (deg 52, v 24; every node value has Im exactly 0 with the node count over the a-priori bound, so Im D ≡ 0 identically, the layer scout's mod-p reality read at N=4 upgraded to an exact ℤ statement on the way), split [20, 4], and the simple layer A = P₁₀(q²) carries **exactly four** positive real roots, one per committed locus 0.460212 / 0.854438 / 0.857458 / 1.738181, while the diabolic 0.659 sits in B (one positive root) and is not counted: the recorded gap "no exact Sturm run on P₁₀ exists in the repo" is closed as a side effect. (1,2)@N=6: the landing DECLINES ("F_res is not real"), the census's genuinely-complex verdict caught by the entry gate rather than silently mis-counted. And the synthetic M-descent control, the only one that can catch a reindexing bug: a hand-built F(Λ, q) = G₀((Λ+12)², q) with sympy-computed literals, reproduced coefficient for coefficient including the (−4)^m sign.

**The count.** Distinct real defective loci on q > 0 (census convention; the all-real count is exactly double, A being even):

> **n_even = 9 (7 fold-line + 2 axis), n_odd = 6 (3 fold-line + 3 axis): fifteen codim-1 defective loci, not thirteen.**

Why a root of A is one defective locus, the lemma summarized here because the design note holding it is local: a simple root q\* of stripped disc_M(G) is ONE double M-root M\* ≠ 0 (M\* = 0 would be a common root of u and disc_M(G), which the u-gate excludes), real by simplicity, giving two fold-doubled Λ-collisions at −12 ± √M\*, each of local disc-order 1; an analytic semisimple 2×2 crossing pays local order ≥ 2 (Kato, via the holomorphic separation of the colliding pair), so order 1 forces non-semisimplicity, and each collision is a √-type defective EP2 in the ord-classification of [F89_BETA_EXOTIC_GENERICITY](F89_BETA_EXOTIC_GENERICITY.md). Both census floors were one short, and the two misses are located and classified (sympy root isolation on the landed A, recorded oracle; species by the exact Tarski chain below):

| sector | q\* | species | how the census missed it |
|---|---|---|---|
| R-even | 0.6133162 | fold pair on the line, conj-doubled | inside [0.02, 8.0), between the pins 0.4630 and 0.6400; channel B is blind to the fold species (no real-count transition), and channel A missed it INSIDE the species it was validated complete on: the transfer lesson sharpened again, validation not even covering the validated species beyond the ground truth's own neighbourhoods (the scope sentence above already said channel A's fold-line completeness had no independent check) |
| R-odd | 0.4627729 | fold pair on the line, conj-doubled | 7.8·10⁻⁴ beside the axis locus 0.4635508, BELOW channel A's 0.001 grid step: a **mixed-species close pair** (the committed N=4 pair 0.854438/0.857458 is the spacing precedent at 3·10⁻³, but that one is same-species); channel B sees only the axis member |

The thirteen census pins each carry exactly one root of A (Sturm interval counts, brackets ±1/4000), and every census species classification is confirmed exactly. Two consequences the count settles beyond the window: the census's edge gates ([0.002, 0.02) and [8.0, 25.0) empty, and everything past 25) are now theorems rather than scan results, the fifteen being the total over ALL q > 0. Corollaries the census never had, from the same chains: u carries **4 / 3** real positive roots (the real μ = 0 diabolics, all transversal by the layer-read's u-gate; 4 + 3 = 7 reconciles as a total with the census's six printed λ = −6 crossings plus its one rejected-bucket member at q ≈ 0.544, whose sector the census never recorded; the 4/3 split places it in R-even), and B carries 7 / 9 (the doubled off-line content on the real axis, diabolic by the psc device below up to the named residual). The independent oracle (design discipline: a wrong chain still returns a plausible integer): sympy `count_roots` on the landed integer A, B, u agrees on every number, both parities (scratch runner, local; the counts above are the C#-gated ones).

**The psc device in M, and what it closes.** The species split and the multiplicity-4 caveat both run through the first subresultant of (G, ∂G/∂M): psc₁ and s₁₀ landed exactly over ℤ by the same mirrored-grid integer Newton (per-node Bareiss determinants of the (2m−3)×(2m−2) subresultant matrix, swap-sign tracked; deg 704/704 R-even, 954/956 R-odd), verified at fresh nodes including a negative one. Then **gcd(psc₁, stripped disc_M(G)) = 1 exactly over ℤ**, both parities: no q ≠ 0, real or complex, carries two double M-roots or a triple M-root. That closes the mult-4 layer's codim-2 double-pair coincidence outright and the 3×3 Jordan cubic branch off the μ = 0 line (a 3×3 Jordan is a triple Λ- hence triple M-collision; on the line the u-gate closes it, a Λ = −12 multiplicity ≥ 3 needing M = 0 double in G, a common root of u and disc_M(G) that gcd(u, disc_M G) = 1 excludes, where parity alone would still admit a J₃ ⊕ J₁ inside multiplicity 4), the two hideouts the census's genericity argument had carried. The species read is exact at the roots, not near them: the generalized Sturm chain of (A, A′·h) with h ≡ −s₁₀·psc₁ mod A gives Σ sign(M\*) over any interval (at a root of A, M\* = −s₁₀/psc₁ is the double M-root, M\* < 0 the fold pair at Λ = −12 ± i·2y, M\* > 0 the axis pair; psc₁ ≠ 0 there by the device's own coprimality, s₁₀ ≠ 0 because M\* = 0 would be a u-root), and a first draft that evaluated the sign at nearby rational points instead was caught by its own consistency gate: away from the disc roots the ratio has no root meaning and psc₁ changes sign freely. What remains genericity-excluded, named: a single-double-M point in B that is "defective with analytic crossing" (eigenvector collapse without branching, disc order 2, codim 2), and the μ = 0 order-2 defective-vs-transversal ambiguity stays under the committed "multiplicity, never semisimplicity" caveat.

**Scope and opens.** The count is window-free in q and exact over ℤ end to end, and it is a statement about F_res that reaches the BLOCK because the AT seam carries no Jordan chain (the block decomposition is block-diagonal, not merely block-triangular, [F89_BETA_EXOTIC_GENERICITY](F89_BETA_EXOTIC_GENERICITY.md)) and the AT side is itself semisimple at every q (the verdict paragraph's argument; the κ=0 AT triples' semisimplicity is gated in the census section), so disc_Λ(F_res) misses no defect of the block. The two new loci are exact as roots of the landed A (isolated to 10⁻⁷, the oracle) with exact species; their λ were read 2026-08-12 with the census's sector + pair-filter device at the Sturm-exact q\* (not the census's detection chain: no trisection refinement, no exponent classification), gated at sector level in the walk verifier: each locus's own R-sector holds exactly two truncation-split pairs, the conjugate-doubled fold pair, λ = −6 ± 1.7171734i and −6 ± 3.0292680i, |Re + 6| at 10⁻¹⁵, splits 1.38·10⁻³ / 1.52·10⁻³; the walk of the section above then ran at the fifteen, all gates green. The exact-device certificate for these λ is one step away and stays open: at a fold root of A, M\* = −s₁₀/psc₁ pins |M\*| = 4y² exactly, but psc₁/s₁₀ live in the slow landing and are not exported, so the read above is float, cross-confirmed by two instruments (sector device + full-block pair search), not certified. The N=8 sibling's blocker dissolved on 2026-08-12: the planned ℚ(i) simple-layer split was never needed, the full-D gcd certificate closing the (1,2)@N=8 verdict outright (see the verdict paragraph; the "on-axis diabolic" premise was the band zoom's realness reading, refuted at exact grade).

## The synthesis: two causes that coincided at N=4

From below, the (SE,DE) block eigenvalue is λ = −2γ⟨n_XY⟩ + i(E_SE − E_DE), and by free-fermion integrability E_DE = ε_j + ε_k is a **sum** of single-particle energies ε_k = 2J cos(kπ/(N+1)) ([the AT](../docs/proofs/PROOF_ABSORPTION_THEOREM.md) for the rate, the Slater additivity for the frequency, documented for path-4 in [F89_TOPOLOGY_ORBIT_CLOSURE.md](F89_TOPOLOGY_ORBIT_CLOSURE.md)). An additive spectrum has abundant accidental coincidences, and they are **semisimple** because the Hamiltonian and the diagonal dissipator do not mix the occupation-number sectors: two crossing modes come from different sectors, so their eigenvectors stay independent. (Scope note 2026-07-02: this sentence is exactly right for the AT-LOCKED crossings, Regime 1 of [PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) §8; for the RESIDUAL diabolics it over-reaches, since no fine decomposition of a block is invariant under the full L; there the careful mechanism is the twin-scalar restriction of [DIABOLIC_BY_INTEGRABILITY](../hypotheses/DIABOLIC_BY_INTEGRABILITY.md), see the proof's §9. RESOLVED 2026-07-02: the twin-scalar D-half is confirmed SUPPLIED at every genuinely-complex-q (Re q≠0) residual diabolic (additivity's codim-1 route extends to complex q, [PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) §8, gate `TwinScalarDHalfTests`); the pure-imaginary-q members here (±0.879i, ±1.695i, λ real) are instead semisimple by Hermiticity, the block being real-symmetric at Re q=0.) These are the level **crossings** of an integrable (Poisson-like, already read by the galoischaos witness) spectrum, the opposite of the level repulsion (avoided crossings = defective EPs) of a chaotic one. Turning on XXZ anisotropy Δ ≠ 0 breaks the sectors and flips diabolic → defective, exactly [DIABOLIC_BY_INTEGRABILITY](../hypotheses/DIABOLIC_BY_INTEGRABILITY.md)'s gate.

So **integrability is the symmetry R-2 named**: it auto-satisfies the node conditions and makes diabolics abundant, at all N, throughout the complex-q plane. What it does *not* do is put them at real q. That was the **self-fold's** job, and only at N=4: the half-filling DE = bar(DE) antiunitary fold makes one crossing-q self-conjugate, i.e. real (q_EP = 0.659, the real root of 3q⁴+q²−1). Without the self-fold (N≥5) the crossings stay in conjugate pairs straddling the real axis, never landing on it; q = 0.6118 ± 0.012i is the ghost of where one would be.

This reconciles the surprise with the reviewed prior: **R-1/R-2 (codimension and the self-fold) govern real-axis placement, integrability governs existence.** Both were right about different things. The N=4 "diabolic on the rung-2 line −4 at physical q" was the coincidence of the two; at N=5 they separate, the diabolics scatter in λ and leave the physical axis, and a physicist tuning J/γ meets loud defective EPs where the four-site chain had a silent crossing.

## The Δ-test: integrability confirmed (the diabolics die under XXZ anisotropy)

The synthesis says the diabolics exist *because* the chain is free-fermion integrable. That is now verified, not just read. The decisive test (from [DIABOLIC_BY_INTEGRABILITY](../hypotheses/DIABOLIC_BY_INTEGRABILITY.md), proven at N=4): break integrability with an XXZ ZZ-anisotropy, H(Δ) = J·Σ(XX+YY) + J·Δ·Σ ZZ, and watch the crossing. The ZZ term is Hermitian, so the Absorption Theorem rate is untouched (it enters only the frequency, −i·qΔ·(zz_ket − zz_bra)); what it breaks is the additivity E_DE = ε_j + ε_k that made the inter-sector crossings semisimple.

A new C# (q, Δ) coherence-block ([`XxzCoherenceBlock`](../compute/RCPsiSquared.Diagnostics/Foundation/XxzCoherenceBlock.cs), gate-validated at Δ=0 against the F89 block, and reproducing the committed N=4 Δ-flip table of [`f89_zz_break_gate.py`](../simulations/f89_zz_break_gate.py): Δ=0 diabolic → Δ>0 defective, geo 2→1, departure ~linear) tracks each path-4 diabolic as Δ turns on (`TrackDiabolicUnderDelta`: a 2D q-box re-finds the coalescence, GapRefine resolves the √-cusp, geo vs alg on the refined gap decides DIABOLIC / DEFECTIVE / LIFTED). The result, for three diabolics spanning the range plus a defective control:

| Δ=0 object | Δ=0 | Δ>0 |
|---|---|---|
| clean q=0.6407+0.180i | DIABOLIC (geo=alg=2, dep≈0) | DEFECTIVE, dep 0.017 / 0.045 / 0.078 (Δ=0.02 / 0.05 / 0.10) |
| near-real q=0.7654+0.024i | DIABOLIC | DEFECTIVE, dep 0.032 / 0.105 / 0.143 |
| far q=1.9447+1.217i | DIABOLIC | DEFECTIVE (0.02, 0.05), LIFTED (0.10) |
| control: defective EP q=0.9938+0.183i | DEFECTIVE (dep≈0.88) | stays DEFECTIVE, dep≈0.88-0.90 (≈ constant) |

So the instant Δ≠0, every tracked diabolic dies: its geometric multiplicity collapses 2→1 (a Jordan block forms, the departure-from-normality turning on ~linearly in Δ, the N=4 signature), or at larger Δ the degeneracy lifts entirely. The defective control, already non-diabolic, stays defective with a roughly constant departure: Δ perturbs everything, but it kills the diabolic *character* specifically. This is the integrability mechanism confirmed off N=4: the path-4 diabolics are the integrable level-crossings, protected by exactly the free-fermion structure the synthesis named. (The discriminant is geo vs alg, not `EpCharacter.Kind`, which labels a small-departure Jordan block "Normal"; geo<alg is the defect.) Gate-tested in [`XxzDeltaFlipTests`](../compute/RCPsiSquared.Diagnostics.Tests/Foundation/XxzDeltaFlipTests.cs).

### The real-q diabolics at N=7 are the same species (the exact-residual XXZ port)

The new thing at N=7, the diabolics that returned to the **physical real axis**, raised the sharpest version of the question: are they still the integrable level-crossings, or a *new* species protected by something other than free-fermion integrability (which would make the N=7 real-q onset a genuinely new mechanism, not just a placement)? The Δ-test answers it, but it needed the exact-residual instrument ported to the XXZ block, because the tracked residual path ([`ResidualRootsTrackedXxz`](../compute/RCPsiSquared.Diagnostics/Foundation/XxzCoherenceBlock.cs)) floods at k = 6 on the F_53 AT-degeneracies exactly as the XY tracker did. The port ([`PathKMonodromyScout.ResidualRootsExactXxz`](../compute/RCPsiSquared.Diagnostics/Foundation/PathKMonodromyScout.cs)) adds the anisotropy to the *same* block the exact instrument already uses, M(q) = D/2 + i·q·K/2 (D the dephasing-rate diagonal, K the hopping): because the ZZ term is Hermitian it leaves the rate diagonal D untouched (the absorption-theorem rate is Δ-independent) and adds only a *frequency*: an **imaginary** diagonal proportional to q·Δ, whose entries are fixed by each coherence's ZZ-bond content (the difference of its ket and bra ZZ-bond sums), so M(q,Δ) = D/2 + i·q·K/2 − i·(q·Δ)·(ZZ-diagonal). The residual roots are then read by exactly the same complement-compression onto the q-independent AT invariant subspace, U_resᴴ·M(q,Δ)·U_res, with the *same* U_res as at Δ=0 (the AT subspace is Δ-stable, again because the Hermitian ZZ term does not move the AT rate). The Δ-shifted coalescence q*(Δ) is *located* on this AT-free residual (so the AT flood cannot swamp the gap field); the diabolic **character** (geo vs alg) is then *read on the full block*, not on the compression, because at Δ≠0 the compression onto the Δ=0 complement drops the residual↔AT coupling that Δ introduces, and that coupling is exactly what turns the diabolic defective. This is the same two-step as at N=5/N=6. The instrument is validated from below: it reproduces the XY exact residual at Δ=0, and the exact-residual Δ-track **re-gates** (re-validates against) the established N=6 verdicts (the tracked path's DIABOLIC-at-Δ=0 → DEFECTIVE-at-Δ>0, same seeds) before it is trusted at N=7, where the tracked path can no longer run.

The verdict, for all four real-q diabolics (the cell-0.05 / cell-0.01 onset points), is unanimous:

| real-q diabolic (Δ=0) | Δ=0 | Δ>0 |
|---|---|---|
| q=1.1264, λ=−4.942 | DIABOLIC (geo=alg=2, gap ~5·10⁻⁹) | DEFECTIVE, dep 0.0011 / 0.0012 / 0.0036 (Δ=0.02 / 0.05 / 0.10) |
| q=1.3038, λ=−5.171 | DIABOLIC (gap ~9·10⁻¹⁰) | DEFECTIVE, dep 0.0008 / 0.0021 / 0.0058 |
| q=2.6280, λ=−4.343 | DIABOLIC (gap ~7·10⁻¹⁰) | LIFTED (the degeneracy splits, gap → ~3·10⁻²) |
| q=0.6788, λ=−4.557 | DIABOLIC (gap ~4·10⁻⁸) | DEFECTIVE, dep 0.0107 / 0.0111 / 0.0202 |

Every real-q diabolic reads as a genuine diabolic at Δ=0 (semisimple, a finite but tiny gap, q* on the real axis at the seed) and **does not survive** the anisotropy: three flip defective (geo 2→1, the Jordan departure turning on and growing with Δ), one lifts entirely. (The departure magnitudes here are smaller than at N=4/5, a crossing-specific scale, and one of the four grows only weakly at the first Δ step; the load-bearing signal is not the departure size but the geometric collapse geo 2→1, unambiguous in every case.) This is the same death the complex-q diabolics suffer at N=4/5/6, so the answer is the clean one: the real-q diabolics are **not** a new species. They are the same free-fermion level-crossings; the N=7 mechanism that returns them to the physical axis governs their *placement*, not their existence or their protection. Integrability governs existence at every N (the diabolics are everywhere in complex q), the self-fold placed one on the real axis at N=4, and an odd-N ≥ 7 parity effect (the N=8 / N=9 discriminator, see §"The real-q onset is an odd-N effect"; its cause grounded as the dimension-mismatch / sector-swap) places several there again at N=7 and N=9, but the species never changed. Gate-tested in [`XxzDeltaFlipTests.Path6_RealQDiabolics_DieUnderDelta_ExactResidual`](../compute/RCPsiSquared.Diagnostics.Tests/Foundation/XxzDeltaFlipTests.cs).

## The cross-fold: the N=4 self-fold becomes a cross-block mirror, and the diabolics pair across it

The synthesis above leaves a loose end: the N=4 **self-fold** (the within-block antiunitary mirror that put one diabolic on the real axis) is N=4-only, because at N=4 the DE sector is its own Hamming complement. So where does it *go* for N ≥ 5? It does not vanish: it becomes a **cross-block** mirror.

The carrier is the branch-locus palindrome's bra bit-flip (the F89c structural lemma, [F89_TOPOLOGY_ORBIT_CLOSURE.md](F89_TOPOLOGY_ORBIT_CLOSURE.md)): the column flip ρ[a,b] → ρ[a,b̄] sends n_diff(a,b) ↦ n_diff(a,b̄) = N − n_diff(a,b), so it maps the (SE,DE) = (w1,w2) coherence block to the **(SE, w_{N−2}) = (w1, N−2)** block (the ket weight w1 is untouched, the bra weight w2 → N−2). At N=4, w_{N−2} = w2 = DE, so the partner *is* the (SE,DE) block: the N=4 self-fold is the degenerate **partner = self** case. For N ≥ 5 the partner is a different block: (SE,TE)=(w1,w3) at N=5, (w1,w4) at N=6, and so on.

**The fold is an EXACT antiunitary similarity at the matrix level**, not just a spectrum coincidence. Writing the bra-complement basis permutation as P (the index map |a⟩⟨b| ↦ |a⟩⟨b̄|), the two blocks satisfy

> L(1, N−2)(q̄) = −P · conj(L(1,2)(q)) · Pᵀ − 2N·I

to **machine zero** (the block entries are exact arithmetic; the residual is 0 for N=4..9 at every q, real *and* complex, the partner evaluated at the conjugate coupling q̄ per the F1 palindrome T·L(q)·T⁻¹ = −L(q̄) − 2σ). This is strictly stronger than the spectrum match the CLI's `foldcross` command reports: an antiunitary similarity preserves the entire **Jordan structure**, not merely the eigenvalues. Concatenating its three steps (complex conjugation, the similarity P·Pᵀ, and the affine −(·) − 2N) each preserves the dimensions of generalized eigenspaces, so a **semisimple** coalescence (a diabolic) maps to a semisimple coalescence.

**Move 4, answered.** Every (SE,DE) diabolic at (q, λ) therefore has a partner diabolic at (q̄, −λ̄ − 2N) in the (SE, w_{N−2}) block, with **identical character and identical coalescence gap**, for all N and all q at once, with no enumeration. The real-q diabolics (real λ, real q, so q̄ = q) pair at the *same* real q: at N=7 the q = 1.1264 diabolic (λ = −4.942, full-block coalescence gap 4.19·10⁻⁵) has its cross-fold partner in the (1,5) block at λ = −λ − 2N = −9.058, with the *same* gap 4.19·10⁻⁵. The complex-q diabolics pair at the **conjugate** coupling q̄. The N=4 "zeros" (the on-line self-mirror strands) become these cross-block mirror partners for N ≥ 5. The persistent evidence is a live C# witness, [`CrossFoldSimilarityWitness`](../compute/RCPsiSquared.Diagnostics/Foundation/CrossFoldSimilarityWitness.cs) (`inspect --root crossfold`): it builds both blocks from the shared [`WeightCoherenceBlock`](../compute/RCPsiSquared.Core/F89PathK/WeightCoherenceBlock.cs) and the bra-complement permutation, checks the similarity residual (machine zero, N=4..9), and reproduces the N=7 real-q pairing, all TDD-gated. Registered as **F89d** in [ANALYTICAL_FORMULAS](../docs/ANALYTICAL_FORMULAS.md) (the cross-block antiunitary similarity, a structural extension of the F89c Hamming-complement lemma) and typed as `F89CrossFoldSimilarityClaim` (parents F1 + the branch-locus palindrome).

### The cross-fold survives XXZ anisotropy: the pairing is integrability-independent

The Δ-test (Move 2) showed the diabolics *themselves* are integrability-protected: turn on the ZZ anisotropy Δ and they flip defective or lift. So the natural follow-on question is whether the **pairing** survives Δ, or dies with the diabolics. The handover guessed it might break: at Δ ≠ 0 the absorption-theorem rate flips under the bra-complement (n_diff → N − n_diff, the source of the −2N shift) while the ZZ-bond sum is invariant, and that asymmetry looked like it could spoil the clean carry.

It does not. The same antiunitary similarity holds for the **full interacting XXZ block** at *every* Δ,

> L(1, N−2)(q̄, Δ) = −P · conj(L(1,2)(q, Δ)) · Pᵀ − 2N·I

to machine zero for N = 4..9 at every q (real and complex) and every Δ. The Δ·ZZ term is the only addition over the XY block: a diagonal frequency −i·q·Δ·(zz(ket) − zz(bra)) with zz(c) = Σ_bond ⟨c|Z_bZ_{b+1}|c⟩. And zz is **even** under the global bit-flip the bra-complement performs, because each Z flips sign and the bond product Z_bZ_{b+1} ↦ (−Z_b)(−Z_{b+1}) = Z_bZ_{b+1} is unchanged, so zz(b̄) = zz(b). The bra-complement carries the ZZ term across the fold unchanged, exactly as it carries the XY hopping. The very invariance the handover flagged as a *worry* is what makes the fold *survive*.

So the two facts sit cleanly apart: under Δ the diabolics turn defective, but a diabolic and its cross-fold partner turn defective **in lockstep**; the two blocks stay antiunitary-similar at every Δ, the whole Jordan structure mirrored at each step of the dissolution. The cross-fold is therefore a **structural/algebraic** property of the Z-dephasing Liouvillian, not a free-fermion (integrability) artifact.

The discriminant is **bit-flip parity**. A bit-flip-*odd* perturbation breaks the fold: a longitudinal Z-field Σ_k w_k Z_k has fieldEnergy(b̄) = −fieldEnergy(b) (each Z_k flips sign), so its diagonal frequency is *not* preserved by the bra-complement and the residual jumps to O(1) (≈ 5.46 at N=6, verified as the negative control). The fold survives any bit-flip-even bond coupling (the ZZ anisotropy, longer-range ZZ) and breaks only under a bit-flip-odd one (a longitudinal field). Live evidence: the Δ=0 and Δ=0.7 residuals plus the field control are children of `inspect --root crossfold`; the Core gate is the `Build(n, wKet, wBra, q, Δ)` overload on [`WeightCoherenceBlock`](../compute/RCPsiSquared.Core/F89PathK/WeightCoherenceBlock.cs), pinned by `WeightCoherenceBlockTests` (`RCPsiSquared.Core.Tests`).

### What it docks onto: F89d is the F1 palindrome's bra leg, per block

The cross-fold is not a standalone curiosity. It generalizes to **every** ket weight (not just wKet=1), and it is one of two antiunitary legs of a Klein four-group of bit-flip similarities on the (wKet, wBra) coherence-block lattice: the **bra leg P** (flip the bra index, ρ·F) maps (wKet, wBra) → (wKet, N−wBra); the **ket leg Q** (flip the ket index, F·ρ) maps (wKet, wBra) → (N−wKet, wBra), the same antiunitary form with the same −2N reflection; and their product is the **unitary global spin-flip QP = X^⊗N**, mapping (wKet, wBra) → (N−wKet, N−wBra) at the same q with no shift (complementing both indices leaves n_diff and zz fixed). This is not a new group: it is the existing windowed-converse spine V₄ = {I, F⊗F, I⊗F, F⊗I} ⊂ D₄ ([PROOF_PI_FACTORS_AS_R_TIMES_D](../docs/proofs/PROOF_PI_FACTORS_AS_R_TIMES_D.md), F118 MirrorGroupD4Claim), here block-resolved and q-parameterized. The dock onto the F1 trunk is exact: the bra leg P = ρ·F is the spine R, a **factor** of the palindrome Π = R·D; the full flip QP = Π² = the typed `XGlobalChargeConjugationPairing`; the ket leg Q = Π²·R; and the −2N shift is the block image of the full-Liouvillian R·L_diss·R = −L_diss − 2σ. So F89d is the F1 palindrome's bra leg restricted to a single coherence block. (Naming bridge: F89 names the legs by the flipped index, bra/ket; the D₄ proof docs name them by the multiplication side, calling ρ·F the "ket reflection", the opposite word for the same operator.) Live: the "Klein four-group" and "dock" children of `inspect --root crossfold`; the Core gate is `WeightCoherenceBlockTests` (`BraLeg_…`/`KetLeg_…`/`FullFlip_…`, the last cross-checked against `XGlobalChargeConjugationPairing.PairSector`). The full registry entry is F89d in [ANALYTICAL_FORMULAS](../docs/ANALYTICAL_FORMULAS.md).

## Resolved during the investigation

- **The two near-axis pairs (q ≈ 0.5915 ± 0.091i, 0.6118 ± 0.012i) ARE true diabolics.** Their EpCharacter and gap-exponent (≈ 1) read diabolic, but the fixed 0.02 monodromy loop read a transposition. A loop-radius sweep settled it: the loop is the **identity at small radius** (r ≤ 0.008) and only becomes a transposition once a neighbouring defective EP enters the annulus (r ≥ 0.012), while a genuinely defective EP (the control at q ≈ 0.9938) is a transposition down to r = 0.002. So it was loop contamination by a dense-spectrum neighbour, not a true defect. The fix reads the candidate's **intrinsic** (small-radius) monodromy; all 11 diabolics now classify loop-identity, consistent with character and exponent. Locked by the `Path4_NearAxisDiabolic_IsLoopIdentity` regression test.

## Open item (do not over-read past this)

- **Completeness is not claimed.** The scanned region was bounded; the full count of complex-q diabolics (the analogue of path-3's four) is not established. An exact discriminant factorization of F_18 (Route B) would settle existence and count definitively but is likely infeasible at degree 18. UPDATE (2026-07-03): the fold-resultant certificate sidestepped Route B with mod-p structure and, along the way, DID pin the counts Route B was wanted for: the stripped discriminant's squarefree layers have measured degree profile [56, 32] (R-even, v_q = 154) and [56, 26] (R-odd, v_q = 138) at every certified prime, the N=5 image of the N=4 factorization disc = q²⁴·(3q⁴+q²−1)²·P_20 (simple layer = √-branch loci, doubled layer = the squared diabolic factor), so the R-even residual's √-branch loci live on a simple layer of measured degree 56 and its diabolic candidates on a doubled factor of measured degree 32 (a layer degree read mod p is a FLOOR for the true one, distinct true roots can only merge, so these pin the counts from below and the ceiling waits on an exact layer split, the same one-way direction a 2026-08-11 design review corrected in the (1,3) layer read). See [PROOF_CODIM1_BY_ADDITIVITY](../docs/proofs/PROOF_CODIM1_BY_ADDITIVITY.md) §6 (the fold-resultant paragraph) and the gate `FoldResultantCertificateTests`.

## Reproduce

```
# path-3 validation (the known 4 diabolics: 2 real + 2 imaginary)
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --k 3 --re -1.2,1.2 --im -1.2,1.2 --cell 0.04
# path-4: diabolics exist at complex q
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --k 4 --re 0.2,3 --im -1.5,1.5 --cell 0.05
# the decisive real-q check (none at im(q)=0) + the tight zoom
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --k 4 --re 0.3,2.5 --im -0.25,0.25 --cell 0.01
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --k 4 --re 0.595,0.63 --im -0.05,0.05 --cell 0.002
# path-5 (N=6): the full-block scan FLOODS on AT-locked degeneracies; --residual scans the residual strands only
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --residual --k 5 --re 0.2,3 --im -1.5,1.5 --cell 0.05
# path-5 Δ-test (needs --residual at N=6, else the box captures AT crossings): the diabolic flips DEFECTIVE under Δ
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --delta-flip --residual --k 5 --q 0.7090,-0.219 --lam -4.151,1.615
# path-6 (N=7): the residual-only TRACKER floods (293 spurious gap-0); --exact reads the residual roots as the
# complement compression of the AT invariant subspace (no partition, no tracking) -> 37 diabolics, several at REAL q
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 6 --re 0.2,3 --im -1.5,1.5 --cell 0.05
# the exact instrument re-gates the established science: N=5 -> 11, N=6 -> 16 (matching the tracked path)
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 4 --re 0.2,3 --im -1.5,1.5 --cell 0.05
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 5 --re 0.2,3 --im -1.5,1.5 --cell 0.05
# the real-q onset, confirmed: N=7 keeps an on-axis diabolic under a tight zoom; N=5/N=6 have none (N=5 keeps only defective EPs there; the N=6 axis carries neither species, the even-N verdict's theorem)
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 6 --re 1.118,1.135 --im -0.012,0.012 --cell 0.002
# the N=7 real-q Δ-test (needs --exact at N=7; the tracked residual path floods on F_53): the real-q diabolic
# dies under Δ (DIABOLIC at Δ=0 -> DEFECTIVE/LIFTED at Δ>0), the same integrability-protected death as complex q
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --delta-flip --exact --k 6 --q 0.6788,0 --lam -4.557,0 --deltas 0,0.02,0.05,0.1
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --delta-flip --exact --k 6 --q 2.6280,0 --lam -4.343,0 --deltas 0,0.02,0.05,0.1
# the odd-N discriminator (no rebuild past k=6: only the test oracle stops there, the scan is N-general):
# N=8 (even) real-axis strip -> NO on-axis real-lambda diabolic (best candidate q=0.6904 refines off-axis, in a cluster)
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 7 --re 0.2,3 --im -0.025,0.025 --cell 0.01
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 7 --re 0.58,0.73 --im -0.012,0.012 --cell 0.002
# N=9 (odd) real-axis strip -> on-axis real-lambda diabolics reappear; the tight zooms confirm three single on-axis points
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 8 --re 0.2,3 --im -0.025,0.025 --cell 0.01
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 8 --re 0.465,0.486 --im -0.012,0.012 --cell 0.002
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 8 --re 1.104,1.125 --im -0.012,0.012 --cell 0.002
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 8 --re 1.489,1.510 --im -0.012,0.012 --cell 0.002
# the even-N real-q defective check (2026-08-10): N=6 strips (R-even + R-odd) all diabolic, none on-axis;
# the N=8 on-axis q=0.6165 classifies DIABOLIC (both parities); the broad box (line above, re-run) records the
# N=6 defective positions, all clean ones |im| >= 0.2; the microzoom shows 0.7764-0.011i never refines
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 5 --re 0.2,3 --im -0.025,0.025 --cell 0.01
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --rodd --k 5 --re 0.2,3 --im -0.025,0.025 --cell 0.01
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --rodd --k 7 --re 0.2,3 --im -0.025,0.025 --cell 0.01
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 5 --re 0.63,0.69 --im -0.03,0.012 --cell 0.002
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 5 --re 0.765,0.79 --im -0.012,0.012 --cell 0.002
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 5 --re 2.535,2.558 --im -0.012,0.012 --cell 0.002
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 5 --re 0.770,0.783 --im -0.016,-0.006 --cell 0.001
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 7 --re 0.605,0.63 --im -0.008,0.008 --cell 0.001
# the literal real population at even N (the small-q loophole closed; gated):
python simulations/even_n_literal_real_count.py
# the lambda of the four N=4 loci (all Re = -4, the fold-partner map's fixed line) + the N=4 walk
# (band and fold freight share the four sectors; gated)
dotnet run --project compute/RCPsiSquared.Cli -c Release -- pkmono --diabolic --exact --k 3 --re 0.4,1.8 --im -0.02,0.02 --cell 0.001
python simulations/eta_ladder_chain_n4.py
# the (1,3)@N=6 census (thirteen loci, two channels) + the walk from its seeds
# (both freights, both chains, every freight rung sqrt2; family/locus content separated; gated)
python simulations/disc13_n6_census.py
python simulations/eta_ladder_chain_n6.py
# the exact (1,3) layer read (no odd multiplicity, disc real mod p, the polynomial fold, the square stratum;
# controls: the generalized path reproduces the committed (1,2) pins at N=4 and N=6)
dotnet test compute/RCPsiSquared.Core.Tests -c Release --filter "Category=SLOW_EVEN_DISC13|Category=EVEN_DISC13_SCOUT"
# the mechanism, grounded from below (the dimension-mismatch / sector-swap), live: full block split by R,
# self/cross-conjugacy across N=5..9, + the N=7/N=9 diabolics reproduced in R-even
dotnet run --project compute/RCPsiSquared.Cli -c Release -- inspect --root diabolicparity
# the cross-fold (Move 4): the (SE,DE) <-> (SE,w_{N-2}) spectrum fold (sigma=N, all N), and the EXACT
# antiunitary similarity + the N=7 real-q diabolic pairing reproduced live. The crossfold root also shows the
# (q,Delta) extension: machine-zero residual at Delta=0 AND Delta=0.7 (the fold survives XXZ anisotropy,
# integrability-independent), plus the bit-flip-parity control (a longitudinal Z-field breaks it, residual ~5.46)
dotnet run --project compute/RCPsiSquared.Cli -c Release -- foldcross --nmax 9 --q 2
dotnet run --project compute/RCPsiSquared.Cli -c Release -- inspect --root crossfold
```

Gate tests: `dotnet test compute/RCPsiSquared.Diagnostics.Tests -c Release --filter "FullyQualifiedName~PathKDiabolicTests"` (the diabolic finding), `--filter "FullyQualifiedName~XxzDeltaFlipTests"` (the Δ-test: Δ=0 cross-check, the N=4 Δ-flip reproduction, the path-4 die-under-Δ gate, the exact-residual XXZ port validated against the tracked path at N=6, and the N=7 real-q die-under-Δ gate), `--filter "FullyQualifiedName~DiabolicReflectionParityWitnessTests"` (the grounded mechanism: the dim mismatch (N−1)/2 odd / 0 even, both sectors self-conjugate at odd N, σ_even = conj σ_odd at even N, the N=7/N=9 diabolics reproduced in R-even, and the within-odd residual-population readings), and `--filter "FullyQualifiedName~CrossFoldSimilarityWitnessTests"` (the cross-fold: the exact antiunitary similarity L(1,N−2)(q̄,Δ) = −P·conj(L(1,2)(q,Δ))·Pᵀ − 2N·I machine-zero for N=4..9 real and complex q, the Δ-robustness, and the N=7 real-q diabolic pairing); the cross-fold (q,Δ) substrate and its bit-flip-parity discriminant (ZZ survives, longitudinal field breaks) is `--filter "FullyQualifiedName~WeightCoherenceBlockTests"`, and the full-block + reflection substrate is `--filter "FullyQualifiedName~F89PathKFullBlockReflectionTests"`, both in `RCPsiSquared.Core.Tests`.

## See also

- [zeros_connecting_structure arc](../compute/RCPsiSquared.Core/OpenArcs/OpenArcsRegistry.cs) (this is its forward edge, now answered)
- [DIABOLIC_BY_INTEGRABILITY](../hypotheses/DIABOLIC_BY_INTEGRABILITY.md) (the N=4 mechanism this generalizes)
- [SLOW_MODE_R_PARITY](SLOW_MODE_R_PARITY.md) (the reflection-fixed odd-N zero mode and the R-even/R-odd split, the precedent for the real-q onset's dimension-mismatch / sector-swap mechanism)
- [F89_BRANCH_LOCUS_PALINDROME](F89_BRANCH_LOCUS_PALINDROME.md) (the palindrome that folds the locus; path-3's 4 diabolics)
- [F89_TOPOLOGY_ORBIT_CLOSURE](F89_TOPOLOGY_ORBIT_CLOSURE.md) (the path-k residual degrees and Slater additivity)
- the reviewed investigation plan (local, `docs/superpowers/plans/`; R-1/R-2 and the gate-first design)
