# The Flow From a Sampled Handover to a Fixed Point

**Date:** 2026-05-30
**Authors:** Thomas Wicht, Claude (Opus 4.8)
**Status:** SEEN, not yet understood. Parked for future us. The ideal-model computations below
are verified to their stated numerical tolerances; hardware values are finite-shot raw marginals.
The *interpretation* is deliberately left open.
We showed what happens; we do not yet grasp why. This document holds the structure so a
later session can pick it up without re-deriving, and resists the temptation to sink into
guessing while the numbers are in front of us but not yet readable.

**Probes:** [`the_flow_endpoints.py`](../simulations/the_flow_endpoints.py),
[`at_the_target.py`](../simulations/at_the_target.py),
[`post_ep_dynamics_4d.py`](../simulations/post_ep_dynamics_4d.py),
[`f86_ep_through_the_clock.py`](../simulations/f86_ep_through_the_clock.py),
[`after_the_collapse.py`](../simulations/after_the_collapse.py),
[`memory_fate_across_the_takt.py`](../simulations/memory_fate_across_the_takt.py)

**Hardware:** IBM Kingston run, 2026-05-31 (Job `d8dr7dfd0j8c73f4man0`); data and notes in
[`data/ibm_ep_onset_may2026/`](../data/ibm_ep_onset_may2026/), runner `run_ep_onset.py` in the
IBM pipeline.

---

## What this document is about

Take a few qubits in a row and let them be gently jostled by noise, the kind that washes out
a quantum state's delicate phase relations (its "coherence"). Drop a single excitation on one
site and watch. Like a bead of ink in water, it spreads, loses track of where it began, and
settles into an even, featureless smear, the same amount on every site. That settled smear is
the system's *future*: where it ends up, and stays.

This document asks a plain question about that future. It is a single point, one settled
state. What happens *at* that point, and what, if anything, comes next?

Two things surprised us. You never quite arrive: the system approaches the settled state
exponentially, forever getting closer but reaching it only in the infinite limit, the way the
future is always just ahead of now. And, stranger, the settled state is *already there* at the
very start, hidden inside the initial state as a piece that never changes. Time does not build
the future; it erases everything that is *not* the future, until only the future remains.

We could only find this by *running* the system forward step by step (we call that "the
loop"), because here, unlike most of this project, there is no formula for the answer. That
turns out to be the lesson: a formula fixes the long-time endpoint, while the finite-time
population trajectories between preparation and endpoint must be propagated.

One honesty note. Everything with a number attached is computed and checked. The reading at
the close, that this settled point *is* "the future", is interpretation, the most speculative
tier in this project. We keep it because it is what drives the work, not because it is proven.

## Abstract

The setup: an open quantum system, a Heisenberg/XY chain of N qubits under local Z-dephasing
(noise that erases phase but conserves which-site occupation), prepared with one excitation.
Its evolution is generated by the Liouvillian L, the matrix that propagates the density matrix
including the noise. The measured F86-adjacent observation is a sampled SE-walk population handover.
The hardware runner labels its coherence decay Γ and Q_label=J/Γ; the repository's Lindblad jump
√γ Z gives γ=Γ/2, hence **Q_Lindblad = 2 Q_label**. The finite-time revival is near the 1/N reference through
Q_label=1.5 and larger at Q_label=2.5, equivalently Q_Lindblad=3→5. Its spectral character remains open. Here we
follow the dynamics to its long-time fixed point. The question
is dynamical, so we compute it in the loop (step-by-step RK4 time evolution), not from the
static spectrum.

The result: the flow ends at the equipartitioned state ⟨n⟩ = 1/N per site in its fixed
single-excitation sector (verified N=3..6). The finite Q scan and the long-time target are two
different readings: the first is a hardware sampled revival-amplitude handover; the second is
an ideal-model λ=0 stationary sector state. EP, Hopf, and Jordan character remain open for the handover.

At the target: it attracts the fixed-trace one-excitation sector's transient complement and is
reached only asymptotically. It is not an isolated global sink because the full kernel has N+1
stationary number-sector directions. The central finding is that the 1/N target is the
*conserved* λ=0 component of the prepared sector state, present
at full strength from t=0 (its coefficient is exp(0·t)=1), so the future is not built by time
but uncovered by it as the transient fades. Under the specific continuation that multiplies one
fixed positive per-site profile by a common scalar f, its transient stability changes from decay
(f>0) through neutral (f=0) to formal inverse-dephasing amplification (f<0). The f<0 generator is
not a physical CP dephasing channel or energy-gain bath, and this sign table does not classify
arbitrary mixed-sign site profiles or certify a Hopf bifurcation.

The ideal-model identities are computed to the stated tolerances; the hardware counts are not bit-exact.
The closing reading, the target as "the future", a computed face of
[The Other Side](../hypotheses/THE_OTHER_SIDE.md)'s "both sides exist simultaneously", is
Tier-5 interpretation, kept as the work's motor, not a claim.

---

## What we set out to see

A bit of background, made self-contained. The evolution of an open quantum system is set by a
matrix called the Liouvillian; each of its modes carries a decay rate and, sometimes, an
oscillation frequency. Picture each mode as a clock with two hands: a *beat* (how fast it
decays) and a *rotation* (how fast it turns). The exact two-mode clock has its own toy 2×2 EP,
worked out in [F86's Exceptional Point Through the Clock](F86_EP_THROUGH_THE_CLOCK.md). The
full SE-walk measurement here is different: through the lower sampled labels the return population
stays near the 1/N reference level; at higher labels a larger revival is visible. No spectral
coalescence or Jordan-rank test is performed by that population measurement, so it does not
certify an EP.

We then asked where the prepared population goes in the long run. A snapshot of the modes
(the static spectrum) does not by itself provide that state-dependent trajectory, so we ran the time
evolution forward step by step (a standard integrator, RK4), what we call the loop, and
watched.

## What the loop showed

**1. The population dynamics is a 4D object (Q × site × t).** The propagated traces change with
canonical Q = J/γ, and finite sampled traces may or may not contain resolved return turns. That
time-grid display reading is not a universal damping-regime or spectral-transition classifier.
Edge and bulk sites differ. The dimensionless knob is Q alone.

**2. The target is 1/N, the equipartitioned state.** For the connected chain at finite Q>0, trajectories end at
⟨n_site⟩ = 1/N (verified N=3,4,5,6 → 0.3333, 0.2500, 0.2000, 0.1667). This is the
fully-forgotten, uniform single-excitation state. It equals ¼ at N=4 *by coincidence*; it
is NOT the project's CΨ=¼ fold (a separate coherence threshold, flat in N over the measured N = 2-5 for the product state,
studied elsewhere in this project, a different quantity entirely; the steady coherence here is
just 0).

**3. A sampled handover and a fixed point bracket the executed flow** (`the_flow_endpoints.py`,
N=3..6):

| | the **SE-walk handover** | the **target** |
|---|---|---|
| where | runner Q_label=1.5→2.5; canonical Q_Lindblad=3→5 | state space, the fixed point |
| measured object | site-0 revival grows relative to the 1/N reference | λ=0 kernel within the single-excitation sector |
| character boundary | no coalescence/Jordan certificate; EP character open | kernel dimension and stationarity are computed separately |
| memory | visible revival switches on | transients die away |

The target sits in the kernel of L: `L · vec(1/N uniform) = 0` bit-exact. The kernel is
**(N+1)-dimensional**, one fixed point per particle-number sector; the single-excitation
trajectory lands on its own sector's fixed point.

## On hardware: the flow on a real chip (IBM Kingston, 2026-05-31)

The loop above is a simulation. We then ran the same single-excitation flow on a real quantum
computer, IBM Kingston, on a connected 3-site chain (qubits 13-14-15, T2 ~ 180-300 μs), reading
out only the per-site populations ⟨n_l⟩ (Z-basis, no tomography). Job `d8dr7dfd0j8c73f4man0`;
data in [`data/ibm_ep_onset_may2026/`](../data/ibm_ep_onset_may2026/).

The whole arc shows up in a single population trajectory:

| t (μs) | ⟨n_0⟩ | ⟨n_1⟩ | ⟨n_2⟩ | |
|---|---|---|---|---|
| 0 | 0.99 | 0.03 | 0.02 | excitation on site 0 |
| 1 | 0.08 | 0.17 | 0.77 | sloshed to the far end |
| 2 | 0.13 | 0.67 | 0.23 | back across the middle |
| 3 | 0.84 | 0.11 | 0.09 | revival on site 0 (the memory) |
| 6 | 0.72 | 0.17 | 0.17 | revival fading |
| 9 | 0.63 | 0.20 | 0.22 | |
| 12 | 0.52 | 0.25 | 0.28 | |
| 15 | 0.47 | 0.26 | 0.36 | |
| 18 | 0.43 | 0.29 | 0.38 | |
| 20 | 0.339 | 0.426 | 0.338 | finite-time marginals (sum 1.103) |

Two finite-time features are recorded on the real device: the excitation **sloshes** site to site
with a ~3 μs period, and the site-0 revival falls from 0.84 to 0.43 over 15 μs. By 20 μs the
three marginals have moved closer together, but their sum is 1.103; without readout/leakage
correction or a long-time fit they do not certify convergence to the ideal sector's 1/N state.

Honest on the rate: the revivals fade *faster* than the T1+T2-only model predicted (the
simulation kept the site-0 revival near 0.83 at t=18 μs; hardware gave 0.43). This is consistent
with accumulated circuit/gate cost, but the stored records contain no calibrated gate/readout/
leakage model that assigns the discrepancy uniquely.

### The sampled SE-walk handover (Part B): inject dephasing and scan Q

Part A has no calibrated canonical-Q value in this record. Part B adds dephasing and scans the
runner label Q_label downward, watching the finite-time revival change. On hardware the dephasing
is a random-Z twirl (after each Trotter step, RZ(φ) per qubit with φ ~ N(0, 2Γ·dt), averaged
over K=16 instances; the RZ gates are virtual on IBM, so the injection itself is error-free). The
reported observable is the *revival*, the best return of the excitation to site 0 (max ⟨n_0⟩ for
t ≥ 2 μs). Its proximity to 1/N is a scalar reference, not proof that all site populations have equipartitioned. Job
`d8drjbfd0j8c73f4mobg`:

| Q_label=J/Γ | Q_Lindblad=2Q_label | revival (max ⟨n_0⟩) | population reading |
|---|---|---|---|
| 0.5 | 1 | 0.30 | near 1/N |
| 1.0 | 2 | 0.36 | near 1/N |
| 1.5 | 3 | 0.34 | near 1/N |
| 2.5 | 5 | 0.49 | larger return |
| 5.0 | 10 | 0.56 | larger return |
| 20 | 40 | 0.70 | larger return |

The random-phase variance gives coherence decay exp(−Γt), whereas the canonical jump √γ Z gives
exp(−2γt), so γ=Γ/2 and Q_Lindblad = 2 Q_label. The revival is near the 1/N reference through
Q_label=1.5 and is larger at Q_label=2.5: **a sampled population handover on a real chip**, bracketed
at canonical Q_Lindblad=3→5. The validated exact-statevector twirl simulation runs 0.28 → 0.84
across the same scan. At Q_label=20 the measured 0.703 is below the exact-twirl simulation 0.842 (raw 0.8417853730254796); this is
consistent with accumulated circuit/gate cost but is not uniquely attributed by these records.
Part A is a finite-time sloshing record; Part B is the population-revival handover.
Population-only data do not identify an eigenvalue coalescence or its Jordan character; spectral character remains open.

## The structural picture (seen, not interpreted)

This document places two different readings beside each other: a finite-grid hardware population
handover as Q changes and an ideal-model λ=0 stationary sector state. Only the latter is a
computed spectral object, not a hardware measurement. The former has no threshold coalescence, branch-continuation, or Jordan-rank
certificate; its EP, Hopf, and Jordan character remain open. The time-domain loop describes the
middle without promoting a sampled revival threshold into a spectral singularity.

## At the target: a point, and then?

The target is one point. What happens there, and how it goes on, is computed in
[`at_the_target.py`](../simulations/at_the_target.py).

**It attracts the fixed-trace one-excitation sector's transient complement, asymptotically.**
Kernel-direction perturbations that change conserved number-sector weights remain stationary
and generally approach another stationary mixture, so the point is not an isolated global sink.
The global slowest non-kernel mode is not identified with the population-visible approach rate:
the winning odd number-changing mode has zero overlap with this even preparation/readout.

**The future is already present, and conserved.** Decompose ρ(0) (a localized excitation)
in L's eigenmodes: the λ=0 component already equals the 1/N target, bit-exact (‖difference‖
= 1.4e-15), at t=0. Its coefficient is exp(0·t) = 1, so it never grows and never decays. The
target is not built up over time; it is the invariant part of the state, present from the
start at full strength. What changes is everything else: the non-kernel (transient) part,
norm 0.866 at t=0, fades to zero and *reveals* the target that was always already there. The
loop does not carry us to the target; it dissolves the present to expose it.

**How it goes on under a common scalar continuation.** Multiply one fixed strictly-positive
dephasing profile by a real scalar f. The 1/N sector state remains stationary throughout this
formal continuation. At f<0 the map is not a physical CP dephasing semigroup: it is an
inverse-dephasing continuation that amplifies coherences, not an energy-gain bath. A statement
based only on Σγ would not cover arbitrary mixed-sign site profiles.

| common profile scalar f | max non-kernel Re λ | role |
|---|---|---|
| > 0 | < 0 | **sink** (the flow falls in; the end of history) |
| = 0 | = 0 | **neutral** (eternal oscillation; the mirror) |
| < 0 | > 0 | **source** (linear source instability under formal gain continuation) |

The point does not move; the arrows around it invert. And there, at the dynamical layer,
Tom's {−, 0, +} reappears here as a sign reading along this one-parameter formal continuation.

## Tier-5 reading: the target as the future (our motor)

*Below this point is interpretation, in the spirit of [The Other Side](../hypotheses/THE_OTHER_SIDE.md)
§14: the structure above is computed (Tier 1-2); the reading here is Tier-5. We keep it
because, as Tom put it on 2026-05-30, it is our motor and drive, not a truth-claim.*

The target has the shape of the future as we know it: a point you approach but never reach
(the moment you would arrive, it is the present, not the future), the thing the arrow of
time points at (the sink). And the computation says something stranger and lovelier, the
future already exists. It is the conserved λ=0 mode, present in ρ(0) at full strength from
t=0, invariant. We do not travel to it; the present fades and uncovers it. In the timeless
spectrum (the Compute engine) all modes, including the future, are there at once; the loop
(Propagate) is only the unfolding.

This is the computed face of the oldest wild thing in the project: "Both sides exist
simultaneously", R = CΨ² and Ψ = √(R/C) (the project's founding pair of relations, read there
as past→now and future→now), "neither first, both simultaneous"
([The Other Side](../hypotheses/THE_OTHER_SIDE.md) §3, §17). What was poetry with eigenvalues
is now a conserved fixed point we can touch. Two honest seams stay at the label, not the
structure: today's "future" is the *decided* equilibrium (the kernel), where THE_OTHER_SIDE
called the future the *undecided* coherences, opposite labels but the same simultaneity; and
the mirror-world reading itself stays Tier-5, where that document drew its own line. We
returned with new sight and the wild thing sharpened into something we could compute. We did
not invent it; we learned to see the structure that was already there.

## Seen again 2026-05-31: the flow is a walk across the memory axis

Returning with the absorption light in hand, the un-closed-form middle turns out to have a clean
reading after all: not a formula for the rate, but a picture of *what moves*.

[The View Onto the Memory](../reflections/THE_VIEW_ONTO_THE_MEMORY.md) sorts a Liouvillian's modes
by one axis, the **drain depth** popcount(i⊕j): the number of sites at which two basis
configurations differ, which is how many places the noise can grip. Depth 0 (the diagonal) never
fades, the stored past; high depth is flushed fastest, the quantum future. That axis is not a
separate object from the light this experiment runs on. For any basis coherence |i⟩⟨j|, the
differing sites are exactly the sites carrying X/Y content (a site where the bra and ket agree
gives an {I,Z} factor, a site where they differ gives {X,Y}), so **the drain depth popcount(i⊕j)
is the light n_XY of that coherence, bit-exact**. The depth axis and the absorption light are one
axis with two names.

Read the flow along it. The initial state, a localized excitation |1₀⟩⟨1₀|, is pure diagonal:
depth 0, n_XY 0, all stored past. The Hamiltonian opens it into off-diagonal coherences (depth
lifts, n_XY rises) and the noise drains them back down. Watched as a single number, the flow's
light ⟨n_XY⟩ is a *tide*: 0 → ≈0.6 → 0 (peak near t ≈ 0.3 at Q=1.5, N=5), rising into the future
and falling back into the memory. The 1/N target it lands on is **100% depth 0** (the reflection's
"steady state is pure stored past"): the flow does not travel to its future, it dissolves into its
own memory. That is the same "the future is already there" of the section above, now in the depth
axis's words: the target is the depth-0 floor, the decided past, and the tide of {X,Y} future is
what time drains away.

The depth axis also carries a **parity rail**. A state of
definite particle number is purely *even* depth (a number-conserving coherence |i⟩⟨j| within a
sector differs at an even count of sites); particle-number-difference parity equals depth parity.
Thus odd-Δn coherences such as |1-exc⟩⟨vacuum| sit on odd depth, while even-Δn number-changing
coherences can still sit on the even rail. The single-excitation flow lives entirely on the even
rungs, draining depth-2 → depth-0, and never touches the odd rungs. Verified: the odd-parity
weight of ρ(t) is 0.0000 at every t, and the Liouvillian's globally-slowest non-kernel mode, the
depth-1 vacuum coherence at rate 2γ, has overlap 5·10⁻¹⁷ with the flow. This parity fact does not
identify the N+1 number sectors with only two rails.

One correction this sharpened: the depth-1, rate-2γ mode is the unused *vacuum* coherence, not the
flow's own slowest mode. A population-visible relaxation rate requires an overlap-restricted
sector calculation; the global rate-2γ value cannot be substituted for it. An earlier note that
called the global rate-2γ mode "the flow's memory" conflated the two. (Probe: state-based parity
propagation, this session, ρ(t) decomposed by depth/n_XY into even versus odd weight.)

## Seen again 2026-06-03: the arc closed as one picture (and the scale made honest)

Returning with the telescope built (the six-axis dimension navigator), the two measurements sit
in one frame without being identified as the same spectral object.

The first is in parameter space, read through the runner ratio Q_label=J/Γ and converted by
Q_Lindblad=2Q_label. The return population stays near the 1/N reference through Q_label=1.5
and is larger at Q_label=2.5 (canonical Q_Lindblad=3→5). This brackets a
sampled SE-walk handover; it does not locate a unique threshold, track a specific eigenvalue pair,
or certify a coalescence. The exact angle and overlap formulas belong to the separate toy 2×2
clock. Transferring its EP character to this full SE-walk scan would require a full-operator
spectral and Jordan certificate that this experiment does not provide.

The second measurement is in state space. Fix Q in the oscillatory regime and let time run. The memory sloshes
site to site (site 0 to the far end and back), the revival fades, and the per-site occupation
spreads to the equipartitioned 1/N within the one-excitation sector. Globally λ=0 is an
(N+1)-fold semisimple kernel, one stationary state per number sector, reached only
asymptotically. The forgetting.

The measured journey changes regime in Q and relaxes in τ. A sampled population handover and a
stationary kernel are different mathematical objects; this document establishes no relation
between their local spectral characters.

A scale correction, and it matters. The hardware scan itself supplies Q_label=J/Γ; its
random-phase variance converts this to canonical Q_Lindblad=2Q_label. The unrelated γ₀=0.05
used by the plotted toy clock is illustrative, not a calibration extracted from this run, so
this document assigns no canonical-Q value to Part A and no natural chip Q. The observation is
the change in revival amplitude; it assigns no J_EP and does not place the chip on, above, or
below a certified EP.

The injected noise is a steering wheel across the sampled Q_label axis. The grid demonstrates
control of the finite-time population response, not a calibrated Part-A home value, the topology
of a spectral branch, or a guaranteed subsequent hardware flow to 1/N. [The illustrative control map and the
family of journeys it opens](../simulations/results/journey_between_singularities/journey_control.png).
The interpretive picture is held in
[On the Whirlpool You Steer To](../reflections/ON_THE_WHIRLPOOL_YOU_STEER_TO.md).

The telescope reads the exact toy clock through `--axis ep` and the occupation relaxation through
`--root flow`; the former is not a character certificate for the hardware SE-walk. [The journey,
both hardware halves in one
view](../simulations/results/journey_between_singularities/journey.png): Part B's raw population
revival against canonical Q_Lindblad is on the left; an independent ideal-model propagation
toward its one-excitation-sector 1/N state is on the right.

What is closed here is a two-panel comparison: the sampled hardware population handover in Q
and a separate ideal-model relaxation to 1/N in time.
EP, Hopf, and Jordan character remain open for the handover, as does any structural relation
between that handover and the fixed point.

## What we do NOT yet understand (for future us)

- What spectral mechanism produces the sampled SE-walk handover?
- Does the handover carry a coalescence, and if so, what are its algebraic and geometric multiplicities?
- Does the handover-to-target flow carry an additional conserved quantity or geometry?
- The kernel is (N+1)-dimensional, one fixed point per number sector. Does that per-sector
  structure connect to anything beyond number conservation? The N+1 number sectors and the two
  even/odd drain-depth parity rails are distinct decompositions: parity constrains which
  Δn-depth rungs a flow can visit, but it does not resolve or identify the N+1 kernel sectors.
  Any stronger connection remains open.
- **Tom's polarity thread (2026-05-30), recorded as an open viewpoint.** The {−0, 0, +0}
  polarity layer at d=0 and its d=2 reading {−½, 0, +½} (the
  [PolarityLayerOrigin](../compute/RCPsiSquared.Core/Symmetry/Pi2KnowledgeBaseClaims.cs)
  0.5-shift, deepened in [Polarity Coordinates](../reflections/POLARITY_COORDINATES.md) and
  [The Polarity Layer](../hypotheses/THE_POLARITY_LAYER.md)) may touch this problem at the
  **target**: the target (1/N, uniform) is the *unpolarized* state, and the flow to it is a
  *depolarization*, the localized excitation (polarized, "alive") relaxing back to the
  uniform 0 (the d=0 layer, the "forgotten"). So the {−½, 0, +½} → {−0, 0, +0} collapse
  reads as the return to the unpolarized layer. The honest seam: there appear to be *two
  different zeros*. The **polarity-0** (unpolarized = the target, reached at fixed Σγ>0) and
  the **Σγ-mirror-0** of [Zero Is the Mirror](../hypotheses/ZERO_IS_THE_MIRROR.md) (Σγ=0, no
  decay, the eternal standing wave, reached by *sliding* Σγ) are orthogonal axes, not the
  same point. The sampled handover is not placed on the polarity triple. Whether the operator-level polarity triple (the content
  of the residual operator M under Π, the project's palindrome/mirror operator) and the
  state-level polarization (localized vs uniform) are the same structure seen from two sides is
  itself open. Pursue as a viewpoint; do not force a single
  three-point map.
- The continuation past the target is now partly seen as the role-flip above
  (sink/neutral/source); the memory's three fates along Σγ are in
  [`memory_fate_across_the_takt.py`](../simulations/memory_fate_across_the_takt.py) and the
  [The Fragile Bridge](../hypotheses/FRAGILE_BRIDGE.md). What stays open is whether the three
  fates and the sampled handover are one structure.

## For future us

Start by re-running the six probes above. The verified facts are the sampled population handover,
the 1/N target, the future-already-present (the conserved λ=0 mode), and the linear
sink/neutral/source role-flip. The population scan does not certify a defective EP, and the
formal gain-side source is not a Hopf certificate. The open work is the spectral character of
the handover and the relation, if any, between it and the target.

---

*Seen 2026-05-30; watched on IBM Kingston hardware 2026-05-31. The numbers are on the table;
reading them is still ahead.*
