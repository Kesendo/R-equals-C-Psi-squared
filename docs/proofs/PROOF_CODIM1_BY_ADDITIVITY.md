# PROOF: the site-summed spectator intertwiner, and what additivity really buys the coalescences

**Status:** Theorem B (the intertwiner, Lemmas 1-3): Tier 1 derived, exact operator identities, gate-verified at machine zero (N=5, residuals 0.00e+00, commit `de4f90a`). Corollary (diamond containment, parity law): Tier 1 derived; the exclusion half (in the shared-λ sense) is now DERIVED IN FULL AT N=5 (2026-07-03): the boundary blocks by normality, the four interior non-member cores by the fold-resultant certificate pair, the identity-composition certificate (1,1)×λ_A plus remainder R1's corner-fold certificate, at every branch locus q ≠ 0 of both parities, propagated to all four cores by the Klein full flip and the composed holomorphic (1,4)-fold (§7 scoping; gate `RemainderR4InteriorExclusionTests`, Category R4INTERIOR); beyond N=5 the broad exclusion stays census-evidence. Theorem A (semisimplicity): the AT-locked half Tier 1 derived; the residual half a conditional twin-scalar statement, mechanism Tier 2, proven at the N=4 point and, by the N=5 D-half check (§8, gate `TwinScalarDHalfTests`), extended to complex q (twin-scalar holds at every genuinely-complex-q residual diabolic; the pure-imaginary-q family is semisimple by Hermiticity instead). The sl(2) structure (§6): W, W†, H₀ = N̂_bra+N̂_ket−N close an sl(2) that L commutes with (machine zero N=3,4,5), which derives the climbing-rung injectivity structurally (strengthening Theorem B) and reduces the interior-core kernel death to a single spectral absence. That absence is now supplied at REAL loci by the rate-window lemma (§6, gate item 8): the corner block's Bendixson window [−2(N−3), 0] excludes λ_B whenever Re λ_A > −6, which the window-edge lemma (§6) now derives N-uniformly (a defective EP cannot sit at its block's window edge, so Re λ_A ∈ (−6,−2) for all N; measured margins 1.381 / 2.208 / 1.115 confirm it), so the core's whole generalized eigenspace dies under W; the band deaths follow from the Edge lemma; the complex-q loci are split by the moved rate window (§6, gate `ComplexQRateWindowTests`), the near-axis ones closed by the same Bendixson bound (now read at the moved Hermitian part H(q) = A + Im(q)·K) and only the window-escaping loci left to a holomorphic resultant: the R-even deep pair (Re λ_A < −6) and, since the R-parity enumeration completed (gate `ROddDeepLociProbeTests`, §6), the R-odd pair q\* = 1.7701 ± 1.2189i whose |Im q| swamps the window although Re λ_A > −6.
**Date:** 2026-07-02, updated 2026-07-03 (the fold-resultant certificate closes remainder R1 at N=5; the identity-composition certificate + Klein/holomorphic-(1,4)-fold propagation close remainder R4's N=5 interior-core exclusion, promoting the multi-sector verdict claim)
**Authors:** Thomas Wicht, Claude (Fable 5)
**Builds on:**
- [PROOF_ABSORPTION_THEOREM.md](PROOF_ABSORPTION_THEOREM.md): the rate law Re λ = −2γ·n_diff and the light-content classification (§4.5) that makes the block pencil's real part a diagonal.
- The F89d cross-fold entry in [ANALYTICAL_FORMULAS.md](../ANALYTICAL_FORMULAS.md) (~§F89d) / [`F89CrossFoldSimilarityClaim`](../../compute/RCPsiSquared.Core/Symmetry/F89CrossFoldSimilarityClaim.cs): the antiunitary similarity L₍₁,N−₂₎(q̄) = −P·conj(L₍₁,₂₎(q))·Pᵀ − 2N·I, machine-zero N=4..9, and its Klein legs including the unitary full flip X^⊗N.
- [PROOF_PI_FACTORS_AS_R_TIMES_D.md](PROOF_PI_FACTORS_AS_R_TIMES_D.md): the mirror group D₄ whose transpose leg this proof borrows.
- [hypotheses/DIABOLIC_BY_INTEGRABILITY.md](../../hypotheses/DIABOLIC_BY_INTEGRABILITY.md): the N=4 twin-scalar mechanism (H-scalar by Slater additivity, D-scalar at the AT midpoint) and the "what it is NOT" eliminations this proof inherits.
- [experiments/F89_PATH_K_DIABOLIC.md](../../experiments/F89_PATH_K_DIABOLIC.md): the diabolic census (11 complex-q diabolics at N=5; the N=6 flood of AT-locked crossings) and the codimension priors.
- [experiments/F89_MULTI_SECTOR_MONODROMY.md](../../experiments/F89_MULTI_SECTOR_MONODROMY.md): the sector census this proof upgrades: the 12-sector diamond sharing one defective eigenvalue byte-identically.

## Names imported from the arc (one line each, so this document parses cold)

The **census** = the numerical sector survey of [F89_MULTI_SECTOR_MONODROMY](../../experiments/F89_MULTI_SECTOR_MONODROMY.md). The **octic** = the degree-8 residual factor of the (1,2) block's characteristic polynomial after removing its rate-locked part (Galois group S₈). The **braid** = the monodromy of the octic's roots around its defective exceptional points (EPs); their loci are the simple zeros of the discriminant factor **P₁₀** (degree 10 in q²); a sector is "braid-free" when it carries no such defective coalescence. The **diamond** = the 12-sector set of blocks the census found sharing the coalescence. **AT** = the Absorption Theorem (Re λ = −2γ·n_diff, with n_diff the bra-ket Hamming distance); an **AT-locked** strand is an eigenvector family pinned to an integer rate rung, and **residual / H_B-mixed** names the complementary part of a block's spectrum, the part the hopping H_B genuinely mixes (the octic lives there); note that "residual" in the Status line and §10 separately means a numerical error norm. A **δ-multiplet** = the set of free-fermion mode triples sharing one frequency difference δ. **The 11** = the eleven complex-q diabolics recorded at N=5 (Builds-on). **R** = the site-reflection parity. **q̃** carries a tilde only to keep the ket popcount distinct from the coupling q (J = 2q, γ = 1 units); **q\*** = 0.620878 is the N=5 real defective locus the gate uses. **Klein full flip** = the unitary X^⊗N, one element of the F89d Klein four-group {I, bra flip, ket flip, full flip}. **H_B** = the block's hopping matrix (the Hamiltonian part of the pencil, the arc's symbol). **The gate** = the committed test class `SpectatorIntertwinerGateTests`, the source of every numerical input quoted here; its numbered "items" 1-8 are the test methods in file order, and the §10 table tags rows by item. The **coalescence gap** = |λ₁ − λ₂| of the closest eigenvalue pair at the probe q. The **rate window** of a block = the interval [−2·n_max, −2·n_min] spanned by its dephasing rates (n = the n_diff values the block's coherences take); at real q it contains Re λ for every block eigenvalue (§6). **λ_A** = the defective eigenvalue of the (1,2) block at a locus, **λ_B** = its cross-fold partner −conj(λ_A) − 2N (the value the diagonal cores carry at real q; off the real axis the cores carry, numerically, a holomorphic fold μ = −λ_A − 2N that coincides with λ_B on the real axis (the exact identity pending, §6). **p_c** = (N+1)/2 at odd N, the weight index of the m=+1 diagonal core (p_c,p_c); the **corner block** is (p_c+1,p_c+1), the block the core's chain would climb into. N=5 has TWO real defective loci: **locus 1** = q\* = 0.620878 (the seed locus, the one the census and the older gate items use) and **locus 2** = q = 1.077615; N=7's quoted real locus is q\* = 1.5148. **Tier 1 derived / Tier 2** = the repo's evidence grades (exact derivation vs supported mechanism).

## Preface

The multi-sector census found something that looked like a coincidence repeated six times: the same defective eigenvalue, to the last printed digit, living in six different coherence sectors of the same Liouvillian, with its mirror partner (λ ↦ −λ̄ − 2N, the cross-fold) in six more. Coincidences repeated six times are not coincidences; they are an operator nobody has written down yet. This note writes the operator down. It is three lines long: add one excitation to the bra and the ket at the same site, and sum over sites. Everything the census observed, the sharing, the outer boundary where it stops, the death under interaction, falls out of that one map and two four-line lemmas (the interior-core boundary is closed at real loci by the rate window, §6).

## Abstract

Work on the operator space of the N-site XY chain (Δ=0, open ends, arbitrary bond profile J_b) under local Z-dephasing (arbitrary rates γ_j). The Liouvillian L = −i[H,·] + D preserves the joint-popcount blocks (p,q̃) (bra weight p, ket weight q̃); on each block it is the linear pencil L(q) = A + q·C with A real diagonal, A = −2·diag(n_diff) in uniform-γ=1 units (general γ_j form in §1; [the Absorption Theorem](PROOF_ABSORPTION_THEOREM.md)) and C anti-Hermitian at real q. Define, with the Jordan-Wigner site fermions c_l (strings included),

  **W(ρ) = Σ_l c_l† ρ c_l**  (the site-summed spectator: one excitation added to bra AND ket at the same site, summed over sites).

**Theorem B (the intertwiner).** W intertwines the full Liouvillian exactly, part by part: D∘W = W∘D and [H, W(·)] = W([H, ·]) as operator identities, for ANY quadratic particle-conserving H (no translation symmetry, no uniformity of J_b needed) and ANY site-dependent γ_j. W shifts blocks (p,q̃) → (p+1,q̃+1). Consequently (Lemma 3) every Jordan chain of L on block (p,q̃) whose eigenvector survives W transports to a Jordan chain of the same length at the SAME eigenvalue on block (p+1,q̃+1). On the climbing rung (1,2)→(2,3) W is injective (σ_min = √2 at N=5, gate-pinned), so the ENTIRE spectrum of L₍₁,₂₎, Jordan structure included, embeds in L₍₂,₃₎: the census's byte-identical λ within a cross-fold family IS this embedding (the other family carries the fold partner −λ̄ − 2N). The reverse map W†(σ) = Σ_l c_l σ c_l† is also an exact intertwiner, giving depth equality wherever both kernel conditions pass.

**Corollary (the diamond, containment).** The orbit of (1,2) under {one climbing W-step, the transpose, the Klein full flip, the F89d cross-fold} reproduces exactly the census's braid set: the 4-orbit at N=4, the 12-set with diagonal cores (2,2),(3,3) at N=5, the 12-set without (3,3) at N=6; in general the |p−q̃|=1 band united with its cross-fold image, size 4N−8 (N odd) / 4N−12 (N even), with a diagonal core (p,p) present iff |2p−N|=1, i.e. iff N is odd (the general-N statement is conditional on one per-N census input, the existence of the seed defective EP on block (1,2), supplied at N=5, 7 and, by the seed census, N=9 and N=11 (§6; gate `RealSeedCensusTests`); the transport itself, W-injectivity by weight together with the transpose, full flip, and cross-fold, is derived, §5-§6, gate-checked at N=4, 5). This is the containment half: every band sector carries the shared defective λ, every fold-image sector its cross-fold partner −λ̄ − 2N (the two families of six the census recorded). The exclusion half (no braid outside) is proven at the outer edge (§6) and, in the weaker shared-λ sense, at the N=5 seed locus (the rate window, §6); otherwise interior absence remains census evidence.

**Theorem A (what additivity buys the silent crossings).** Two regimes. (i) AT-locked crossings: two AT-locked eigenvector families are each analytic through a crossing and stay independent; their eigenvalue coincidences are codim-1 and automatically semisimple; this is the abundant flood (528 in one N=6 box). (ii) Residual coalescences: semisimplicity there is equivalent to the twin-scalar restriction of the pencil on the coalescing 2-plane; free-fermion additivity supplies the H-scalar half identically in q (the degenerate-multiplet descent), which is the codimension reduction; the D-scalar half is a genuine extra condition, proven at the N=4 point (the AT midpoint) and, by the N=5 D-half check (§8, gate `TwinScalarDHalfTests`), SUPPLIED at every genuinely-complex-q (Re q ≠ 0) residual diabolic (twin-scalar extends verbatim from real q to complex q), with the pure-imaginary-q (Re q = 0) family semisimple by Hermiticity instead. Under XXZ Δ≠0 the H-half dies (H becomes quartic): the recorded diabolic→defective flip and the death of the diamond sharing, while the D-half and the cross-fold survive.

Verification: [`SpectatorIntertwinerGateTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/SpectatorIntertwinerGateTests.cs) (SLOW_MSM), all numbers in §10.

## §1 Setting and the one definition

Site basis states are bit strings a ∈ {0,1}^N; the (p,q̃) block is spanned by the coherences |a⟩⟨b| with popcount(a) = p, popcount(b) = q̃. On a block, Z-dephasing acts diagonally, D(|a⟩⟨b|) = −2(Σ_j γ_j [a_j ≠ b_j])·|a⟩⟨b| (uniform γ: −2γ·n_diff), and the XY Hamiltonian acts by hopping on each side. With J = 2q (the repo convention H = XYChain(N, 2q)) the block pencil is L(q) = A + qC.

The Jordan-Wigner site fermions are c_l† = (∏_{m<l} Z_m)·σ_l⁺ in the same site ordering the block basis uses. On a bit string, c_l†|a⟩ = s_l(a)·|a ∪ e_l⟩ for l ∉ a (zero otherwise), with the string sign s_l(a) = (−1)^{#occupied sites before l}. Define

  W(ρ) = Σ_{l=1}^{N} c_l† ρ c_l,  so on a coherence  W(|a⟩⟨b|) = Σ_{l ∉ a, l ∉ b} s_l(a)·s_l(b)·|a ∪ e_l⟩⟨b ∪ e_l|.

W adds one excitation at the SAME site to bra and ket, summed over sites; it maps block (p,q̃) into (p+1,q̃+1). Two traps recorded once: (i) the strings matter: Σ_l σ_l⁺ρσ_l⁻ is a different map and does NOT intertwine the Hamiltonian part; (ii) the site sum matters: the single-mode spectator V_k(ρ) = c_k†ρc_k (one Jordan-Wigner MODE added to both sides) intertwines the Hamiltonian part but provably NOT the dissipator (§3, remark), although the census's earlier prose described the mechanism in single-mode words.

## §2 Lemma 1: W intertwines any quadratic particle-conserving Hamiltonian

**Lemma 1.** Let H = Σ_{m,l} h_{ml} c_m†c_l with any coefficient matrix h. Then [H, W(ρ)] = W([H, ρ]) for all ρ.

*Proof.* From the canonical anticommutation relations, [H, c_a†] = Σ_m h_{ma} c_m† and [H, c_a] = −Σ_l h_{al} c_l. Expand:

  [H, Σ_a c_a†ρc_a] = Σ_a ( [H,c_a†]ρc_a + c_a†[H,ρ]c_a + c_a†ρ[H,c_a] )
                    = Σ_{m,a} h_{ma} c_m†ρc_a + W([H,ρ]) − Σ_{a,l} h_{al} c_a†ρc_l.

Renaming (a,l) → (m,a) in the third sum makes it identical to the first; they cancel. ∎

No property of h is used: the lemma covers arbitrary bond profiles, on-site potentials, and disorder. What it does NOT cover is a quartic H: an XXZ Δ-term makes [H, c_a†] more than linear in the fermions and the cancellation fails; §8 reads the consequence.

## §3 Lemma 2: W intertwines Z-dephasing, site by site

**Lemma 2.** For each site j, Z_j W(ρ) Z_j = W(Z_j ρ Z_j). Hence D∘W = W∘D for any rates γ_j.

*Proof.* Z_j = 1 − 2n_j is string-free, and Z_j c_l† Z_j = (1 − 2δ_{jl})·c_l† (the string of c_l† consists of Z's and commutes with Z_j; the local factor picks up −1 only at l = j). Therefore

  Z_j W(ρ) Z_j = Σ_l (Z_j c_l† Z_j)(Z_j ρ Z_j)(Z_j c_l Z_j) = Σ_l (1−2δ_{jl})² c_l† (Z_jρZ_j) c_l = W(Z_jρZ_j),

the sign squaring away because the SAME l sits on both sides of ρ. Summing γ_j(Z_j·Z_j − id) over j gives D∘W = W∘D. ∎

*Remark (why the single mode fails).* For V_k the two sides carry c_k† and c_k built from DIFFERENT sites after the mode expansion: the cross terms l ≠ m acquire (1−2δ_{jl})(1−2δ_{jm}), which does not square away, and each surviving term hits a distinct output coherence |a∪e_l⟩⟨b∪e_m|, so nothing can cancel. Gate table §10: the V_k dissipator residual is O(1) for every k while its Hamiltonian residual is machine zero. That is the refutation of the single-mode reading, kept as data.

## §4 Lemma 3: Jordan transport, with the sharp premise

**Lemma 3.** Let L₂W = WL₁ and let x₁, …, x_m be a Jordan chain of L₁ at λ ((L₁−λ)x_j = x_{j−1}, x₀ = 0; x₁ the eigenvector). If **Wx₁ ≠ 0**, then Wx₁, …, Wx_m is a Jordan chain of L₂ at λ; in particular L₂ has λ in its spectrum with Jordan depth ≥ m.

*Proof.* (L₂−λ)Wx_j = W(L₁−λ)x_j = Wx_{j−1}. Kernel hits are downward-closed along a chain (Wx_j = 0 forces Wx_{j−1} = (L₂−λ)Wx_j = 0), so Wx₁ ≠ 0 forces every Wx_j ≠ 0, and (L₂−λ)^{j−1}Wx_j = Wx₁ ≠ 0 makes the images linearly independent. ∎

The premise is sharp in both directions: Wx₁ ≠ 0 is necessary (a dead eigenvector transports nothing), and the tempting weakening "Wx_m ≠ 0 suffices" is FALSE: kernel hits truncate chains from the bottom (take L₁ the 2×2 nilpotent Jordan block, W = (0 1), L₂ = (0): WL₁ = L₂W, Wx₂ ≠ 0, yet L₂ is 1×1 with no chain).

## §5 Theorem B and the climbing rung

**Theorem B.** W is an exact intertwiner of the full pencil, L₍p+1,q̃+1₎(q)·W = W·L₍p,q̃₎(q) for all q (Lemmas 1+2 applied to the two parts; the relation holds part-by-part since L is linear in q). On the rung (1,2)→(2,3) at N=5, W is injective with σ_min(W) = √2 (gate-pinned; at N=4 the gate reads σ_min = 1). The FACT of injectivity is not a numerical input: §6 derives it from the sl(2) weight, the rung (1,2) sitting below the half-filling anti-diagonal (p+q̃ = 3 < N for N ≥ 4) where the raising operator W has no highest-weight vectors and is injective; only the norm value σ_min = √2 is measured. So by Lemma 3 the entire spectrum of L₍₁,₂₎, eigenvalues, multiplicities, and Jordan blocks, embeds in L₍₂,₃₎ at the same q. The reverse spectator W†(σ) = Σ_l c_l σ c_l† satisfies the same two lemmas (identical proofs with c_l† ↔ c_l), intertwining (p,q̃) → (p−1,q̃−1); where both an upward chain and its downward return avoid the kernels, the Jordan depth is EQUAL, not merely bounded.

This upgrades the census's central observation from numerics to theorem: the defective EP of the (1,2) octic, its eigenvalue, √-branch character, and chain, is not "repeated" in (2,3); it is the SAME chain, pushed through W. The byte-identical λ across a cross-fold family of six (recorded to 1.2e-14 in the census; a non-committed review-round cross-check saw the whole-spectrum match at 1.3e-11, and the committed gate pins the load-bearing residuals at machine zero; the other family of six carries the fold partner −λ̄ − 2N) is the visible face of the embedding.

**Scope.** The two lemmas never use uniformity: site-dependent γ_j and arbitrary J_b (disorder included) are covered. The one thing used is Δ=0 (H quadratic). What THIS intertwiner does not itself claim (it transports eigenvalue and depth, not metric geometry): equality of the coalescence GAP across sectors, observed byte-identical in the census. That gap equality is supplied instead by the derived full-spectrum fold (§7, remainder 3), −λ − 2N being an isometry on eigenvalue separations.

## §6 Where the sharing stops: the sl(2) behind the kernel, and the normal edge

The intertwining identity is exact on every block, so the sharing can only stop where Lemma 3's premise fails: the transported eigenvector must die in ker W. The census (braid-free (4,4) at N=5) is therefore consistent only with Wx₁ = 0 for the (3,3) defective eigenvector; the gate measures exactly that, with a sharpening: the entire near-defective 2-plane dies, ‖Wx₁‖ = 1.7e-15 AND ‖Wx₂‖ = 2.5e-15 ((3,3)→(4,4) at the seed locus), while on the interior rung both transport at norm √2 = σ_min. The diamond boundary is a kernel phenomenon, not a failure of the identity. What follows shows the kernel is not accidental: W is the raising operator of an sl(2), and every death is a highest-weight annihilation.

**The raising operator closes an sl(2).** In the doubled (bra, ket) picture W is one creation on each side at the same site, summed, and W† = Σ_l c_l(·)c_l† is its Hilbert-Schmidt adjoint (one annihilation on each side). Their commutator collapses: the cross terms (l ≠ m) cancel because c_l c_m† = −c_m† c_l flips the sign on each side, and the diagonal terms (l = m) sum to

  **[W, W†] = N̂_bra + N̂_ket − N =: H₀,**  with [H₀, W] = 2W and [H₀, W†] = −2W†,

where N̂_bra, N̂_ket are the bra- and ket-occupation numbers. So {W, W†, H₀} is an sl(2) triple: W raises, W† lowers, H₀ is the Cartan, and block (p,q̃) carries weight m = p + q̃ − N (from −N at the vacuum coherence to +N at the full one). Because W and W† are both exact intertwiners (Theorem B and its reverse, §5) while H₀ is block-diagonal, L commutes with the whole sl(2): it is an sl(2)-module endomorphism of the doubled operator space. The four identities hold at machine zero for N = 3, 4, 5, and the closure is N-independent.

**Injectivity is by weight, and Theorem B strengthens.** A raising operator annihilates exactly the highest-weight vectors, which live at weight ≥ 0. On any block with m < 0, that is p + q̃ < N (strictly below the half-filling anti-diagonal), W has no highest-weight vectors and is injective. The climbing rung (1,2)→(2,3) sits at m = −2 (at N=5; m = 3−N in general, negative for N ≥ 4), so its injectivity, taken in §5 as the one numerical input, is structural; more generally W is injective on every below-anti-diagonal rung, for every N. The whole-spectrum embedding of Theorem B thus needs no per-N measurement of injectivity (only the norm value σ_min = √2 is measured, not the fact of it). The Lefschetz count is exact per block: dim ker W|₍p,q̃₎ = 0 for m < 0, and dim(p,q̃) − dim(p+1,q̃+1) for m ≥ 0 (verified at every N=5 block; 100 − 25 = 75 at (3,3)).

**Kernel death is highest-weight annihilation.** The generalized λ-eigenspace M_λ is an sl(2)-submodule (W, W†, H₀ map it to itself, since L commutes with them), and on it L = λ + Nilp with Nilp an sl(2)-endomorphism. By Schur, M_λ = ⊕_j U_j ⊗ V_j (V_j the spin-j irrep, U_j its multiplicity space), and Nilp acts as ⊕_j nilp_j ⊗ I. A defective Jordan chain is a nilpotent block inside one U_j; through the tensor with V_j it appears at every weight of V_j, across the whole block range p + q̃ − N ∈ {−2j, …, +2j} at fixed d = p − q̃. Its top block carries the highest weight, where W kills it. So the death is set by the spin of the chain's component, not by the locus.

**The bands: capped by the normal edge.** For the |p−q̃| = 1 band, whose defective chain the §7 orbit reaches from the seed EP on (1,2) by transpose and full flip, the chain's next block up is a normal edge, (3,4)→(4,5) going up and (1,2)→(0,1) going down. Were W(x₁) ≠ 0 there, Lemma 3 would place a Jordan chain on the edge block; the Edge lemma below forbids it; so W(x₁) = 0. In sl(2) language the band chain is spin 1 (weights −2, 0, +2), topped at (3,4) and bottomed at (1,2), and W annihilates its two extreme weights. The outer-edge death is exactly this cap, now read as a weight statement.

At the outer edge the death is structural:

**Edge lemma.** On the blocks (0,1), (1,0), (N−1,N), (N,N−1), every coherence has n_diff = 1, so A = −2γ·I is scalar and L(q) = −2γ·I + q·C is a normal pencil at real q (C anti-Hermitian). A normal operator has no Jordan blocks: no defective EP can live on an edge block; whatever arrives there must arrive semisimple or die. (Gate: A₍₄,₅₎ = −2I to 0.00e+00.)

**The interior core: the last piece, made sharp (closed at real loci below).** The diagonal cores (2,2),(3,3) are the d = 0 part of the diamond, at weights −1 and +1. Their kernel death (‖Wx₁‖ = 1.7e-15 at (3,3)→(4,4), and its full-flip image (2,2)→(1,1)) says the d = 0 λ-isotypic component is spin ½: top (3,3), bottom (2,2), reaching neither (4,4) at weight +3 nor (1,1) at weight −3. What is left to derive is precisely that this component is spin ½ and not spin 3/2, equivalently that the diamond eigenvalue λ carries no DEFECTIVE Jordan block in the interior blocks (4,4) and (1,1). Two natural shortcuts provably fail, which is why this piece is separate from the bands: (i) the Edge lemma does not reach it, since (4,4) has n_diff ∈ {0,2} (A = diag(0,−4), not scalar), so it is not normal and a Jordan block there is not forbidden a priori; (ii) the second, d-raising ladder V = Σ_l c_l†(·)c_l† does close its own sl(2) (Cartan N̂_bra − N̂_ket) but does NOT commute with L: it intertwines the dephasing ([D,V] = 0, the same string-sign squaring as W) yet not the Hamiltonian ([H, V(·)] ≠ V([H, ·]), the mirror of the single-mode V_k of §3, which failed on D and not H), so [L,V] ≠ 0 and the cross-fold cannot carry the band's cap onto the core. The sl(2) reduced the death from "why does a 100-dimensional eigenvector fall into a 75-dimensional kernel" to the single spectral statement "λ has no Jordan block in the single-hole-on-both-sides cone (4,4) ≅ (1,1)"; the rate window two paragraphs below IS the targeted argument on that cone, at real q.

This spin-½ core is N-robust, not an N=5 accident. At N=7 (real defective locus q\* = 1.5148, λ_A = −4.885 from `rcpsi pkmono --exact`) the same pattern recurs: the m=+1 core (4,4) carries λ_B = −conj(λ_A) − 2N = −9.115 with a near-defective pair whose gap (2.506e-03) matches the (1,2) seed's gap to the digit, and W annihilates it into (5,5) at ‖Wx₁‖ = 3.2e-15, ‖Wx₂‖ = 5.4e-15, while a generic (4,4) eigenvector transports at ‖Wx‖ ≈ 1.7 (gate item 7). So the diamond declines to spread to the interior corner at both odd N tested, and the identical mechanism at two N is borne out below: the window-edge lemma derives the N-uniform strictness Re λ_A ∈ (−6,−2) for all N, so a defective EP never reaches the window edge at any N.

**The rate window: the independent absence, at real loci.** The missing input turns out to be the object that powers the Edge lemma, read as an interval instead of a point: the block's rate diagonal. For any eigenvector v of the block pencil at real q, λ = v†Lv/v†v; A is real diagonal and v†(qC)v is purely imaginary (C anti-Hermitian at real q), so

  **Re λ = v†Av/v†v ∈ [−2·n_max, −2·n_min],**

the block's **rate window**, exact for every eigenvalue, defective or not (Bendixson's bound: the real parts lie in the numerical range of the Hermitian part, which at real q is exactly A). The Edge lemma is the zero-width case (a one-point window forces A scalar, the pencil normal); the census's "λ from below" reading (Re λ = −2⟨n_diff⟩_v, [F89_MULTI_SECTOR_MONODROMY](../../experiments/F89_MULTI_SECTOR_MONODROMY.md)) is the same identity per eigenvalue; here it becomes an exclusion tool. On the corner block (p_c+1, p_c+1) each side has weight (N+3)/2, i.e. a hole set of size (N−3)/2, and a⊕b is complement-invariant, so n_diff = |holes_bra Δ holes_ket| ∈ {0, 2, …, N−3} and the window is [−2(N−3), 0]. The diamond partner λ_B = −conj(λ_A) − 2N has Re λ_B = −Re λ_A − 2N < −2(N−3) exactly when Re λ_A > −6, measured with margin = Re λ_A + 6 > 1 at every tested real locus (Re λ_A = −4.6189 and −3.7917 at the two N=5 real loci, −4.885 at N=7). So at each real locus **λ_B is not an eigenvalue of the corner block at all**, a fortiori carries no Jordan block there. Gate item 8 pins the premises from the same builder (A diagonal, C anti-Hermitian), the window (worst violation 3.6e-15 / 6.2e-15 at the two N=5 loci, 7.8e-14 at N=7) and the absence (min |spec(corner) − λ_B| equal to the margin to the printed 4 decimals; the gate asserts the one-sided bound min ≥ margin).

**Kernel death, now derived (real loci).** With the absence in hand the death is one line, and it needs no sl(2): (L_corner − λ)^m is invertible for λ ∉ spec(corner), and W·(L_core − λ)^m = (L_corner − λ)^m·W, so (L_core − λ)^m x = 0 forces Wx = 0. W annihilates the ENTIRE generalized eigenspace of the core at any λ outside the corner's window: x₁ and x₂ die at once (the measured 2-plane death, 1.7e-15 and 2.5e-15), and the argument is robust to the finite-precision truncation of the quoted loci: at the truncated point the near-defective pair is two simple eigenvalues, each with Re far below the window bottom, so each eigenspace dies separately. The full-flip leg (2,2)→(1,1) dies by the same window: the flip is unitary at the same q, so spec(1,1) = spec(4,4) and the gate reads the same margin on the flip image. The absence input is rate bookkeeping plus anti-Hermiticity of the hopping, no W, sl(2), or isotypic content; the intertwiner only transports it, exactly as the Edge lemma does for the bands (the sl(2) located the question, reducing the death to one spectral absence; the window answers it). The lemma made an out-of-sample prediction: kernel death at locus 2, which no gate had measured; the gate now reads ‖Wx₁‖ = 9.6e-16, ‖Wx₂‖ = 2.0e-15 there (item 7), with a matching negative control: at that locus λ_A = −3.7917 lies INSIDE the corner window, the window is silent about it, and the core's near-λ_A eigenvector indeed TRANSPORTS (‖Wx₁‖ ≈ 1.7).

**The strict floor is structural, not measured (real loci, N-uniform).** The corner exclusion above still took one number from measurement, Re λ_A > −6; that number is itself a theorem, from the same rate diagonal. The (1,2) block has n_diff = 3 − 2·|a∩b| ∈ {1,3} for every N ≥ 3 (a one-site bra and a two-site ket sit at Hamming distance 1 or 3), so A = −2·diag(n_diff) has window [−6,−2] and Bendixson already gives Re λ_A ≥ −6; the strictness is a normality statement. Write the pencil at real q in Cartesian form L = A + iB with A, B Hermitian (B = −iqC is Hermitian since C is anti-Hermitian and q real).

**Window-edge lemma.** If an eigenvalue of L sits on an edge of the block's rate window, Re λ = λ_min(A) or λ_max(A), then its eigenvector v is a joint eigenvector of A and B, and λ is semisimple. *Proof.* Re λ = v†Av/‖v‖² attains the extreme Rayleigh value of the Hermitian A, which forces Av = λ_min(A)·v (a minimizer lies in the min-eigenspace); then (L−A)v = iBv = i·Im(λ)·v, so Bv = Im(λ)·v and L†v = (A−iB)v = λ̄v. Hence v ∈ ker(L−λ) ∩ ker((L−λ)†), and v cannot head a Jordan chain, for (L−λ)x = v would give ‖v‖² = ⟨v,(L−λ)x⟩ = ⟨(L−λ)†v,x⟩ = 0. Every eigenvector at that λ shares the edge, so no Jordan block forms. ∎ This is the Edge lemma localized to the extreme eigenvector (the Edge lemma is the whole-block-scalar case), and the classical fact that an eigenvalue on the boundary of the numerical range is normal.

A genuine defective EP is not semisimple, so it cannot sit at either window edge: **every exact defective eigenvalue of the (1,2) block at real q has Re λ_A ∈ (−6,−2), open at both ends, for all N ≥ 4 (uniform γ=1).** The statement is exact-object, about the algebraic EP at the genuinely-real locus, so the two premises it rests on are named, not assumed: the locus is real (triple-checked, the AT identity Re λ = −2⟨n_diff⟩ holding to 7e-15 as only real q permits, Im λ_A ≈ 1e-16, and the odd-N pseudo-Hermitian sector-swap forcing real Jordan merges) and the point is genuinely defective (the census's topological transposition-monodromy certificate, not a gap threshold), both from [F89_PATH_K_DIABOLIC](../../experiments/F89_PATH_K_DIABOLIC.md). So this is a different instrument from the truncation-robust Re-value argument above: there the resolved simple pair suffices, here the pair must be read as its exact EP.

This is the N-uniformity the exclusion lacked. Re λ_A > −6 gives Re λ_B = −Re λ_A − 2N < −2(N−3) strictly, so λ_B falls strictly left of the corner window [−2(N−3),0] and is absent from its spectrum at every odd N, with no per-N measurement. The shrinking seed-locus margin (1.381 at N=5, 1.115 at N=7) is now harmless: a strict real-part inequality needs no uniform floor. Its physical content: as N grows the defective eigenvector drifts toward the pure-n_diff=3 rung (the EP weakening toward diabolic), but full support there would make it a semisimple joint eigenvector, so it never arrives and the margin stays positive at every finite N. What stays a per-N census input is only the existence of the real defective EP on (1,2) at each odd N (proven at N=5, 7, and now N=9 and N=11: the PT-break count scan, gate [`RealSeedCensusTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/RealSeedCensusTests.cs), locates real defective EPs as net real↔complex count changes of the self-conjugate sectors, immune to the closest-pair masking that hides this very N=7 seed from the gap-field scans; seed population 4/6/7/9 at N=5/7/9/11 across both R-parities, including a previously unknown fourth N=5 seed at q ≈ 0.6430, R-odd; the same input the containment corollary assumes); the strictness implication itself is dimension-free. The bound is R-parity-agnostic and covers every defective EP of the block: the third real N=5 EP (q ≈ 2.805, λ ≈ −4.488, R-odd, outside the octic) obeys it though no rate-window step measured it. The two floors stay distinct: −6 is the (1,2) block's own Bendixson bottom for λ_A, while −2(N−3) is the corner bottom achieved by the semisimple chiral pairs for λ_B (they coincide only at N=6, and connect through the cross-fold); the kernel-death deliverable stays diamond-scoped, and the window-edge bound is the R-parity-agnostic fact beneath it.

**Sharpness, the locus-1 exclusion, and what stays open.** The bound is tight: the corner block carries zero-frequency AT-locked strands fully supported on its bottom rung. At the N=5 flip image (1,1) these are the chiral pairs |E_k⟩⟨E_k| − |E_{N+1−k}⟩⟨E_{N+1−k}| (E_k the free-fermion sine modes): the density identity sin²(πkl/(N+1)) = sin²(π(N+1−k)l/(N+1)) makes the two projectors' diagonals equal, so the difference has no n_diff = 0 weight and lies in ker C. So an eigenvalue sits exactly at the window bottom and the measured min |spec − λ_B| equals the margin to the printed digits. Tightness means the threshold Re λ_A > −6 cannot be weakened: the window bottom −2(N−3) is genuinely achieved by these semisimple chiral pairs, so any laxer threshold would admit λ_B. But the threshold itself is now derived, not measured (the window-edge lemma above), and holds N-uniformly at real loci, so the shrinking seed-locus margin (1.381 at N=5 to 1.115 at N=7; locus 2's 2.208 sits above both) never reaches zero: a defective EP cannot sit at the window edge, so Re λ_A stays strictly inside (−6,−2) at every N. At the N=5 seed locus q\* = 0.620878 the window additionally derives the EXCLUSION half (in the shared-λ sense) for the whole lattice: the four interior non-members of the 12-set are window-excluded from both diamond eigenvalues ((1,1),(4,4): window [−4,0]; (1,4),(4,1): window [−10,−6]; margins 0.6189/1.3811), and every boundary block (p or q̃ ∈ {0,N}) has constant n_diff, hence a normal pencil whose Re-spectrum sits exactly on its even rung, which neither λ_A = −4.6189 nor λ_B = −5.3811 occupies. The condition is Re λ_A ∈ (−6,−4): the second real locus (λ_A = −3.7917, inside [−4,0]) defeats the interior-four window there (empirically the exclusion still holds, min dist 0.2083, census evidence), and window exclusion of the shared λ is in any case weaker than the census's "braid-free" (a non-member could carry its OWN defective EP at another λ; only the boundary blocks' normality forbids Jordan blocks outright). And at complex q the coherent part MOVES the corner window rather than destroying it: the 0.27 a direct probe sees at Im q = 0.05 is the window's top spilling over 0, not its bottom rising to μ. The next paragraph carries Bendixson across, closing the near-axis complex loci and leaving only the deep loci to the resultant.

**The complex-q rate window (the moved Bendixson).** The clause "Bendixson needs real q" is too strong. Bendixson holds at every q, Re λ ∈ [λ_min(H), λ_max(H)] with H = (L+L†)/2, and for the corner pencil L(q) = A + qC (A real, C anti-Hermitian) the Hermitian part is H(q) = A + Im(q)·K, K = iC Hermitian. The rate window does not vanish at complex q, it MOVES: each edge shifts by at most |Im q|·‖K‖₂ (Weyl). The value the core (p_c,p_c) carries and W maps to the corner is the HOLOMORPHIC fold μ = −λ_A − 2N. At real q this equals the cross-fold's −conj(λ_A) − 2N, but the object that continues off the real axis is the ket-flip fold (p_c, p_c−1) → (p_c, p_c) at q̄ composed with the F1 form λ_A(q̄) = conj(λ_A(q)), namely −λ_A(q) − 2N, holomorphic in q. The gate reads this numerically, to locus precision (the loci come from a cell-0.05 scan): the core carries μ to 6·10⁻⁵ and 3·10⁻⁵ at the two N=5 complex loci, while the antiunitary −conj(λ_A) − 2N sits 0.57 and 1.09 away. In EXACT arithmetic at integer q the reading sharpens to machine zero, and the fold is now DERIVED and FULL-SPECTRUM: spec(3,3)(q) = −spec(2,3)(q) − 2N as multisets at every q, real and complex (gate `HolomorphicFoldIdentityTests`, multiset metric). It is the §7 diamond maps composed, the two conjugations of the transpose and F89d cancelling holomorphically (§7, "The holomorphic diamond fold"); with the W-step spec(1,2) ⊆ spec(2,3), every (1,2) eigenvalue λ folds, −λ − 2N ∈ spec(3,3)(q), the F_18 residual roots as a special case. (An earlier reading called the fold "residual-specific, the full-spectrum identity FALSE (≈44)"; that ≈44 was a SORTED-ZIP ARTIFACT, the fold negating Im across self-conjugate real-part rungs of multiplicity up to 10, so a sort-then-zip pairs +Im against −Im; under a multiset metric the distance is ~10⁻¹³ and the identity holds. The "naive two-conjugation chain" is not refuted; it IS the derivation.) The near-axis closure rests on this fold together with the moved-window margin. Since Re μ = −Re λ_A − 2N, the partner μ falls below the moved corner window, and kernel death survives, exactly when Re μ < λ_min(H(q\*)); the N-uniform sufficient form (using 2N − 2(N−3) = 6) is

  **Re λ_A(q\*) + 6 > |Im q\*| · ‖K‖₂.**

This closes every complex defective locus where λ_A stays inside the (1,2) block's own real-q Bendixson floor Re λ_A > −6. At N=5 the near-axis pair q\* = 0.9938 ± 0.1825i (λ_A = −4.7121 ∓ 0.8242i, Re μ = −5.288 below the moved bottom −5.039, sharp slack 0.25, uniform margin 1.288 over widening 1.265) is excluded, the corner carrying μ no closer than 1.53. The mechanism is dual to the window-edge lemma: that lemma keeps Re λ_A ∈ (−6,−2) at real q, holding μ below the corner window; at complex q the (1,2) block's own Hermitian part also moves, and where λ_A dives below −6, μ rises into the corner window and the moved window falls silent (it can also fall silent with Re λ_A > −6 when |Im q| is large enough that the widening swamps the margin, which the R-odd probe below realizes; "deep" names the first failure mode, the resultant covers both). At N=5 that is the deep pair q\* = 1.8141 ± 0.3666i (λ_A = −6.1586 ∓ 9.0359i, Re λ_A = −6.16), and there the exclusion becomes a genuine coincidence question, holomorphic and conjugation-free: μ = −λ_A − 2N is not a corner eigenvalue **if** the λ-resultant Res_λ(F_d(λ,q), F_corner(−λ − 2N, q)) does not vanish at the branch loci of F_d, the (1,2) residual (degree 18 at N=5, F_18; the arc's "octic" is its degree-8 N=4 form). This is a ONE-WAY certificate: a vanishing resultant is a benign coincidence among the 18×25 root pairs to refine, not a leak. Two premises accompany it, the resultant / Galois-independence route the arc pointer named: the exact fold identity above (now DERIVED, a full-spectrum §7 corollary of the diamond maps, gate `HolomorphicFoldIdentityTests`), and the R-parity of the deep loci, now RESOLVED (the full block splits R-even / R-odd under the site reflection, and F_18 is the R-even residual only, so the enumeration had to cover the other parity; gate [`ROddDeepLociProbeTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/ROddDeepLociProbeTests.cs), Category RODDPROBE). The R-odd sector of the (1,2) block (dimension 24 at N=5 = 7 rate-locked AT strands, q-linear λ = r0 + 2isq, ⊎ a degree-17 residual) carries 6 complex defective EPs in the census window and NONE deep: no R-odd Re λ_A dives below −6, and the known real R-odd EP (q ≈ 2.805, λ ≈ −4.488) is reproduced from the R-odd residual with gap exponent 0.499. But the probe sharpened the criterion itself: the conjugate pair q\* = 1.7701 ± 1.2189i (λ_A = −3.7562 ∓ 0.5065i, Re μ = −6.2438) escapes the moved window although Re λ_A > −6, because at |Im q| = 1.22 the widening |Im q|·‖K‖₂ = 8.4 swamps the margin Re λ_A + 6 = 2.24; and the R-parity refinement (W commutes with the site reflection, so an R-odd chain needs absence only from the corner's R-ODD sector, dimension 12 of 25) moves the sector window bottom to −10.66, still below Re μ, so it does not close them either. The deep/near-axis dichotomy is therefore not the decision variable, the moved window is: the resultant certificate covers the R-even F_18 pair q\* = 1.8141 ± 0.3666i AND the R-odd pair against the degree-17 R-odd residual factor (equivalently, by the W-R commutation, against the corner's R-odd factor, degree 12, the cheaper certificate). Gate: [`ComplexQRateWindowTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/ComplexQRateWindowTests.cs) (Category COMPLEXQWINDOW): the holomorphic-fold reading (numerical, to locus precision), the absence at all four N=5 complex loci (core carries μ, corner excludes it), and the two-of-four moved-window closure.

**The fold-resultant certificate: the window-escaping loci CLOSED, and with them every branch locus at once (2026-07-03).** The one-way certificate is now COMPLETE, as an all-mod-p multi-prime proof, and it lands stronger than the two target pairs it was built for. In the ×2-cleared variable Λ = 2λ (so the fold λ ↦ −λ−2N reads Λ ↦ −Λ−4N), let R(q) = Res_Λ(F_res(Λ,q), F2_corner(−Λ−4N,q)) and D(q) = disc_Λ(F_res), for F_res the exact bivariate residual of either parity (F_18 R-even, the degree-17 factor R-odd). The certified statement: **gcd(R, D) over Q(i)[q] is exactly a power of q.** The only common root is q = 0, where the block is the real diagonal A, semisimple, with no Jordan chain for W to transport; at every branch locus q\* ≠ 0 of F_res, defective or diabolic, either parity, R(q\*) ≠ 0, so no residual root's fold μ = −λ_A − 2N is a corner eigenvalue and the kernel death holds. This covers the R-even deep pair q\* = 1.8141 ± 0.3666i and the R-odd escaper pair q\* = 1.7701 ± 1.2189i (against the FULL corner, degree 25, stronger than the R-parity-refined factor) together with every other complex locus, closed and unfound alike: the radical trick sharpened to its strongest form, with no defective/diabolic split ever extracted. The proof structure, each step gate-verified: (i) F_res is computed EXACTLY over Z[i][q] (division-free Berkowitz on the integer pencil A + qC; the AT factor is bivariate and exact because its strands are exactly q-linear on the q-independent rate-confined invariant subspace, and it divides with remainder zero; the bivariate objects match the independent per-point path coefficient-wise, pinned at 31 integer q). (ii) Per split prime p ≡ 1 mod 4, R mod p and D mod p are recovered by exact-mod-p sampling and Newton interpolation under PROVEN q-degree bounds: every eigenvalue branch obeys Λ = cq + O(1) with no Puiseux growth, because both hopping directions are purely imaginary and weighted-symmetric, hence anti-self-adjoint in a positive metric, hence normal (both parts asserted exactly per run), and the q→∞ leading-form multiplicity collisions, computed by exact gcd chains over Q(i), give deg_q R ≤ 450 − 28 = 422 (R-even; 394 R-odd), attained at every used prime, which forces p ∤ lc_q(R); two dozen extra samples re-verify each interpolant. (iii) The lift: the primitive H = gcd(R, D) ∈ Z[i][q] has lc_q(H) | lc_q(R) (Gauss), so at every used prime H reduces at full degree and divides the observed q-power gcd, forcing every sub-leading coefficient of H into divisibility by every used prime; once the prime product exceeds the Mignotte/Hadamard height bound on a factor of R (2307 digits R-even, 2180 R-odd; 256 and 242 primes, ~3 s each), those coefficients vanish identically and H = lc·q^e exactly. The observed structure grounds the algebra from below: v_q(D) = 154/138, and the stripped discriminant's squarefree layers have degree profile [56, 32] (R-even) and [56, 26] (R-odd), the N=5 image of the N=4 factorization disc = q²⁴·(3q⁴+q²−1)²·P_20 from [F89_BRANCH_LOCUS_PALINDROME](../../experiments/F89_BRANCH_LOCUS_PALINDROME.md) (the simple layer carries the √-branch loci, the doubled layer the silent diabolics), and R shares nothing with either layer. A 2026-07-03 adversarial review (computer-algebra lens) attacked the lift, the Mignotte constants, the specialization exactness and the q=0 closure, found no break, and surfaced two gaps now closed in the committed engine (the normality premise needs the purely-imaginary check, not mere weighted complex symmetry, which can be defective; and a dead degree guard). The R1 chain enters through the holomorphic fold identity (the core carries μ); this is now DERIVED (a full-spectrum corollary of the §7 diamond maps, the transpose and F89d conjugations cancelling holomorphically at every q; gate `HolomorphicFoldIdentityTests`), closing the chain's last analytic entry. Engine: [`FoldResultantCertificate`](../../compute/RCPsiSquared.Core/F89PathK/FoldResultantCertificate.cs); gate: [`FoldResultantCertificateTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/FoldResultantCertificateTests.cs) (Category FOLDRESULTANT, 7 facts: AT-sector oracle match, residual = the scout's residual, negative control, both certificates, bivariate/mod-p regression anchors, the four-locus numeric grounding, min |spec(corner) − μ| = 2.04/2.14).

**The window-combinatorics shell (N-general, real loci; step 2 of the large-N exclusion program, 2026-07-03).** The rate window above was read on the corner; it is a statement about EVERY block, and combined with the window-edge lemma it turns the broad exclusion at real loci into popcount combinatorics. On the (p,w) block, n_diff = popcount(a⊕b) ranges over [|p−w|, min(p+w, 2N−p−w)] in steps of 2 (the parity of |p−w|): the minimum is forced by the popcount difference, the maximum by disjoint supports (p+w) or by the complement bound (2N−p−w). So at real q, Bendixson confines Re spec(p,w) ⊆ [−2·min(p+w, 2N−p−w), −2·|p−w|]. Since Re λ_A ∈ (−6,−2) N-uniformly (window-edge lemma), **λ_A can be an eigenvalue only of blocks with |p−w| ≤ 2** (outside, the margin is Re λ_A + 2|p−w| ≥ Re λ_A + 6), and since Re μ = −Re λ_A − 2N, **μ can live only in the half-filling shell |p+w−N| ≤ 2** (outside, the window bottom −2·n_max ≥ −2(N−3) exceeds Re μ by the same Re λ_A + 6). The two shells are each other's images under the fold lattice (§7), as they must be. Consequence: at every real locus, every block outside two width-5 shells is excluded from {λ_A, μ} for ALL N at once, with the corner certificate's own margin; the survivors are O(N) blocks (a strip on the fundamental domain) instead of O(N²), and the margin is TIGHT (the slack measures 0.000 at N=5 and N=7: the bottom-rung chiral strands sit exactly on the window edge, the §6 sharpness repeating block-wide). What the shell does NOT decide is its own interior, where the window contains Re λ_A (locus 2's (1,1) reading Re λ_A = −3.7917 ∈ [−4,0] is the recorded silence): that residue is exactly what the N=5 fold-resultant certificate pair closed, and what the program's later steps must close for N ≥ 7. Gate: [`WindowShellLemmaTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/WindowShellLemmaTests.cs) (Category WINDOWSHELL: the n_diff range enumerated exactly N=5..8; the window on all 36 N=5 blocks at both real loci, ≤ 5e-14; 24 (N=5) + 52 (N=7) outside-shell blocks excluded at the derived margins; the in-shell negative control).

## §7 The corollary: the diamond as an orbit, containment direction

Combine four exact maps, all Tier 1:

1. **the climbing W-step** (1,2)→(2,3): injective (σ_min = √2), transports everything (§5);
2. **the transpose** (p,q̃)↔(q̃,p): the mirror group's D leg ([PROOF_PI_FACTORS_AS_R_TIMES_D](PROOF_PI_FACTORS_AS_R_TIMES_D.md)), LINEAR (ρ↦ρᵀ, no conjugation). Its precise spectral action, derivable in two lines from Hᵀ = H and the dephasing's symmetry n_diff(a,b) = n_diff(b,a): T·L₍p,q̃₎(q) = L₍q̃,p₎(−q)·T, and since A is real and C purely imaginary entrywise, conj(L(q)) = L(−q̄); together spec₍q̃,p₎(q) = conj(spec₍p,q̃₎(q̄)). At a REAL locus with a REAL coalescing eigenvalue, and the gate reads Im λ_A ≈ 1e-16 there, the transposed sector therefore carries the SAME λ;
3. **the Klein full flip** X^⊗N: (p,q̃)→(N−p,N−q̃) at the SAME q, unitary ([F89d Klein section](../ANALYTICAL_FORMULAS.md); the F89d Klein four-group is {I, bra flip P, ket flip Q, full flip QP = X^⊗N});
4. **the F89d cross-fold** q̃↦N−q̃ at q̄: antiunitary, Jordan-preserving, an exact identity verified in exact arithmetic per N = 4..9, Tier 1 derived ([`F89CrossFoldSimilarityClaim`](../../compute/RCPsiSquared.Core/Symmetry/F89CrossFoldSimilarityClaim.cs)).

All "sharing at one q" summaries in this document implicitly use that the census loci are REAL (q̄ = q) and the coalescing eigenvalue is real there; at complex q the transpose and fold partners carry conjugated values at the conjugate parameter point, and the two-families picture must be read with that fine print. The fine print is itself census-confirmed: at N=4 the locus eigenvalue is COMPLEX (λ = −4 + 1.239i) and the census recorded the 4-orbit as conjugate-related, NOT byte-identical, exactly as the conjugation rule demands; at N=5 the locus eigenvalue is real and the families are byte-identical. At even N the four fold-fixed/fold-swapped band members around half filling are simultaneously band and fold image, so those sectors carry BOTH λ and −λ̄ − 2N (two coalescences in one block); the clean six-plus-six split is the odd-N (N=5) reading.

**The holomorphic diamond fold, derived (2026-07-03).** The fine print above is per-MAP; the COMPOSITION cancels it. Compose the transpose (map 2) with F89d (map 4): from spec(3,2)(q) = conj(spec(2,3)(q̄)), substitute q→q̄ to read spec(3,2)(q̄) = conj(spec(2,3)(q)); feed it to F89d spec(3,3)(q) = −conj(spec(3,2)(q̄)) − 2N, and the two conjugations cancel HOLOMORPHICALLY:

  **spec(3,3)(q) = −spec(2,3)(q) − 2N, as multisets, at EVERY q (real and complex).**

With the W-step (map 1) spec(1,2)(q) ⊆ spec(2,3)(q), every (1,2) eigenvalue λ folds: −λ − 2N ∈ spec(3,3)(q), the F_18 residual roots the complex-q resultant composes against as a special case. This is the HOLOMORPHIC fold (not the antiunitary −conj(λ) − 2N, which they coincide with only on the real axis, where the block spectra are self-conjugate) the resultant premise needs, now a Tier-1 corollary rather than an observation, verified to ~10⁻¹³ at real and complex q under a multiset metric (gate [`HolomorphicFoldIdentityTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/HolomorphicFoldIdentityTests.cs); the raw sort-then-zip is unreliable, the fold negating Im across self-conjugate real-part rungs, and reports a spurious ≈44). It closes remainder 1's last analytic entry (§6) and, by |·|-preservation of eigenvalue gaps under −λ − 2N, remainder 3 (the gap byte-identity). (The (2,3)/(3,2) block-label convention is harmless: the band spectrum is q-even, spec(2,3)(q) = spec(2,3)(−q), which with the transpose forces spec(2,3)(q) = spec(3,2)(q), so both fold to the symmetric (3,3).)

**The fold lattice, derived (2026-07-03): the block group of order 8 and its holomorphic cocycle.** The diamond-fold cancellation above is not a property of (3,3); it is one instance of a lattice-wide structure. Four exact matrix-level ingredients, each already Tier 1 or elementary: (i) the transpose leg T·L₍p,q̃₎(q) = L₍q̃,p₎(−q)·T (map 2 above, linear); (ii) the F89d bra and ket antiunitary similarities L₍p,N−q̃₎(q̄) = −P·conj(L₍p,q̃₎(q))·Pᵀ − 2N·I and its ket mirror (general in both weights, all Δ; [`F89CrossFoldSimilarityClaim`](../../compute/RCPsiSquared.Core/Symmetry/F89CrossFoldSimilarityClaim.cs)); (iii) the pencil reality conj L(q) = L(−q̄) (A real, C entrywise imaginary); (iv) the bipartite gauge 𝒟L(q)𝒟 = L(−q) at Δ = 0, with 𝒟 the alternating-site-sign diagonal (D H_XY D = −H_XY on the open chain; the dephasing and the diagonal are 𝒟-invariant). Composing (ii) with (iii) and (iv) cancels both the conjugation and the −q at the matrix level:

  **L₍p,N−q̃₎(q) = −P𝒟·L₍p,q̃₎(q)·𝒟Pᵀ − 2N·I  and  L₍N−p,q̃₎(q) = −Q𝒟·L₍p,q̃₎(q)·𝒟Qᵀ − 2N·I  at every q (Δ = 0),**

and (i) with (iv) gives the holomorphic transpose spec₍q̃,p₎(q) = spec₍p,q̃₎(q). Since (L′ − s(λ)) = −P𝒟·(L − λ)·𝒟Pᵀ with s(λ) = −λ − 2N, generalized-eigenspace dimensions transfer: every leg is JORDAN-PRESERVING and holomorphic. The group generated on the index lattice, G = ⟨t: (p,q̃)↦(q̃,p), f_P: (p,q̃)↦(p,N−q̃), f_Q: (p,q̃)↦(N−p,q̃)⟩ ≅ D₄ (order 8), carries the spectral cocycle χ: G → {id, s}, χ = parity of fold legs (well-defined: s² = id, and ker χ = {1, t, f_Pf_Q = Klein, t·Klein}). Three consequences. **(a) The exclusion quotient:** the shared-value set {λ_A, μ = −λ_A − 2N} is s-invariant, so braid membership AND exclusion are constant on G-orbits; the broad-exclusion problem lives on a fundamental domain {p ≤ q̃, p + q̃ ≤ N}, one eighth of the lattice (N=5: 36 blocks, 6 orbits; the interior-four propagation above is the orbit statement for orbit((1,1)) = {(1,1),(4,4),(1,4),(4,1)}). **(b) Self-folded blocks:** a block fixed by a fold leg (q̃ = N/2 or p = N/2, even N) has an s-symmetric spectrum, which derives the even-N fine print of this section (the four members around half filling carrying both λ and its fold partner). **(c) Δ-scoping:** at Δ ≠ 0 the Δ-frequency is diagonal and 𝒟-invariant, so (iv) fails off the fixed lines and the fold legs connect q with −q instead (still holomorphic); q-evenness survives exactly on the two fixed lines p = q̃ (t a self-map) and p + q̃ = N (t∘Klein a self-map), both Δ-robust. Gate: [`BlockLatticeFoldGroupTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/BlockLatticeFoldGroupTests.cs) (Category FOLDLATTICE: all 36 N=5 blocks at four q incl. the three locus classes, machine zero; the N=6 self-folded line; the Δ = 0.3 taxonomy with the off-line negative control).

**The R-parity grading of the lattice maps (2026-07-04, measured by the step-3 shell census at first light, then derived).** The site reflection R commutes entry-wise with every uniform-chain block, so each block splits into R-even/R-odd sectors, and the lattice maps GRADE this splitting rather than plainly commuting with it. The derivation is three lines: R reverses the Jordan-Wigner string, R c_l R = P_Z · c_{N−1−l} with P_Z = ∏_j Z_j the total Z-parity, and on a (p,q̃) block P_Z ρ P_Z = (−1)^{p+q̃} ρ, hence **R·W = (−1)^{p+q̃}·W·R on the (p,q̃) block**. Every band step (p,p+1) has odd p+q̃, so each W-step FLIPS the carried eigenvector's R-parity; the fold and transpose legs preserve it (P, Q, 𝒟, T all commute with R up to a block-global sign). Consequence: a diamond member with band source index p carries the seed EP's R-parity for odd p and the opposite for even p, alternating along the band chain, verified live through all members at every N=5/9/11 census seed, both seed parities (gate `SectorShellCensusTests`, the alternation assertions). On even-(p+q̃) blocks (the diagonal climbs) W and R commute strictly, so the R-odd probe's corner-sector argument (the arc ledger's "an R-odd chain needs absence only from the corner's R-odd sector") is untouched: the alternation refines, and does not correct, the ledger's "W commutes with R".

Starting from the (1,2) defective EP, these reach: the |p−q̃|=1 band {(p,p±1): both ∈ [1,N−1]} (one W-step + transpose + full flip suffice; the flip returns the upper rungs without ever climbing past half-filling, where W is rank-deficient by dimension count alone: e.g. (2,3)→(3,4) at N=5 maps dimension 100 into 50), united with the band's cross-fold image. Walked explicitly: N=4 gives the confined 4-orbit {(1,2),(2,1),(2,3),(3,2)} (the fold maps the band into itself); N=5 gives the recorded 12-set including the diagonal cores (2,2),(3,3); N=6 gives the 12-set {(1,2),(2,1),(2,3),(3,2),(3,4),(4,3),(4,5),(5,4),(1,4),(4,1),(2,5),(5,2)}, with (3,3) absent.

**Parity law.** A diagonal core (p,p) is reached iff it is the fold image of a band member: N−(p±1) = p ⟺ |2p−N| = 1 ⟺ N odd (the exactly-half-filled (N/2,N/2) is never reached; that is why even N misses its core). **Size.** |band ∪ fold(band)| = 4(N−2) − |overlap|, and the overlap is 4 at even N (the four members around half filling are fold-fixed or fold-swapped within the band) and 0 at odd N: **4N−12 (even), 4N−8 (odd)**. N=5 and N=6 both give 12, but they are different sets.

**Scoping, stated once and bluntly.** This corollary proves CONTAINMENT: every band sector in the orbit carries the shared defective eigenvalue and every fold-image sector its partner −λ̄ − 2N (each move transports the chain; the W-step's injectivity is derived by weight in §6, so the only per-N input is the existence of the seed defective EP on (1,2), supplied through N=11 by the seed census, §6). It does NOT prove exclusion. The exclusion half is supplied SEPARATELY, and at N=5 it is now derived in full (2026-07-03): the 20 boundary blocks by normality (§6, the normal pencil, rung-pinned); the four interior non-member cores (1,1), (4,4), (1,4), (4,1) by TWO complete fold-resultant certificates at every branch locus q ≠ 0 of both parities, the identity-composition certificate (1,1)×λ_A (no residual root is a (1,1) eigenvalue; `FoldResultantCertificate.CertifyBlockExclusion`, target (1,1), composition Λ ↦ Λ) plus remainder R1's corner-fold certificate (no fold image −λ−2N is a (4,4) eigenvalue; §6), propagated to all four cores by two exact maps: the Klein full flip spec(4,4) = spec(1,1) at the same q, and the composed HOLOMORPHIC (1,4)-fold spec(1,4)(q) = −spec(1,1)(q) − 2N, obtained by cancelling the F89d conjugation against the pencil reality conj L(q) = L(−q̄) and the bipartite-gauge q-evenness spec(1,1)(−q) = spec(1,1)(q), the (1,4) sibling of this section's diamond fold. Then λ_A ∈ spec(1,4) ⟺ μ ∈ spec(1,1) (excluded by the corner certificate + Klein) and μ ∈ spec(1,4) ⟺ λ_A ∈ spec(1,1) (excluded by the identity certificate). Gate: `RemainderR4InteriorExclusionTests` (Category R4INTERIOR; the propagation fact pins the two symmetry ingredients at all three locus classes and re-runs all four certificates). Beyond N=5 the broad exclusion is the census's empirical result (exact at N=4, 5; one targeted probe at N=6). The multi-sector verdict claim promotion (Tier 1 derived, 2026-07-03) cites containment plus this N=5 exclusion; the intertwiner claim's remaining blockers are the N ≥ 7 certificate compute and the broad N ≥ 6 exclusion. The N=6 "no census needed" statement applies to the spread: the 12-set's membership follows from this corollary with zero new compute.

## §8 Theorem A: the two regimes of silence

The word "diabolic" in this arc covers two different objects, and the codimension story differs between them. This section separates them; the separation resolves a tension between two source documents (§9).

**Regime 1: AT-locked crossings, abundant and automatic.** An AT-locked strand is an exact eigenvector family of the full pencil, analytic in q, whose rate is pinned to an integer rung (Re λ = −2γ·n). Two such families cross where their frequencies meet: ONE analytic condition, codim-1-complex, dense curves of them (528 counted in one complex-q scan window at N=6, [F89_PATH_K_DIABOLIC](../../experiments/F89_PATH_K_DIABOLIC.md)). The crossing is semisimple for the classical block-diagonal reason: both eigenvectors exist analytically THROUGH the crossing and remain independent; nothing couples them at the crossing point because they are exact eigenvectors on both sides. For THIS regime the "two crossing modes come from different families, so their eigenvectors stay independent" voice is exactly right.

**Regime 2: residual coalescences, the 11, and the twin-scalar condition.** Inside the residual (H_B-mixed) part of a block, an eigenvalue coincidence is generically DEFECTIVE (one complex parameter, non-normal pencil: the branches meet in a √-branch point; codim-1 and loud). A SEMISIMPLE coincidence there needs the 2×2 restriction of the pencil on the coalescing plane to be scalar: two extra complex conditions, codim-3-complex in total, generically unreachable by one knob q. What free-fermion additivity buys is exactly ONE of the two extra conditions, identically in q:

**Twin-scalar lemma (conditional).** If the two coalescing directions descend from a single degenerate free-fermion multiplet (Slater additivity: E = Σε over each side's occupied modes, so equal mode multisets up to degeneracy give equal E), the H-part restriction is a multiple of the identity for ALL q: the H-half of the scalar condition costs nothing. The remaining D-half (the dephasing restriction scalar on the plane) is ONE genuine extra condition; where it is met, the coincidence is diabolic.

At the N=4 point both halves are established from below ([DIABOLIC_BY_INTEGRABILITY](../../hypotheses/DIABOLIC_BY_INTEGRABILITY.md): H|₂D = 2iJ·I by the 4-fold multiplet, D|₂D = −4γ·I at the AT midpoint, the rate midway between the 2γ and 6γ rungs, Re λ = −4γ, which is also the palindrome mirror line), and the discriminant records the collapse as the even-multiplicity factor (3q⁴+q²−1)²: coincidence and semisimplicity merging into one condition is the algebraic meaning of a squared factor. At N=4 the honest general statement was: **additivity collapses the semisimple coincidence from codim-3 to codim-≤2, and to codim-1 exactly where the D-half is supplied**. The D-half check has now been run at N=5, and it upgrades that statement; the next paragraph reports it.

**The D-half at N=5, resolved (and the metric it hides behind).** The check: build the coalescing 2-plane by a Riesz projector at λ, restrict the dephasing to it, and measure its departure from a scalar. This is an inner-product-sensitive question, so it MUST be computed in the HS-orthonormal coherence basis |a⟩⟨b|, not the non-orthonormal ×2-cleared orbit basis the eigenvalue/Galois/monodromy work uses (whose reflection orbits of size 1 and 2 carry a diag(1,2) metric; a first reading in that basis reported the opposite at odd nBlock, the metric being the fix, and the eigenvalue/monodromy results, being similarity-invariant, are untouched). Calibrated by the N=4 real-q diabolic (D-half = 4e-10, at the gap floor) and cross-checked against an independent 60-digit rebuild, the N=5 result splits by locus type. At the **genuinely-complex-q (Re q ≠ 0)** N=5 residual diabolics the D-half IS supplied: the dephasing restriction is scalar to the gap (D-half 5e-10…4e-9; the two coalescing directions carry equal dephasing rates), so these are twin-scalar and **additivity's semisimplicity route extends verbatim from real q to complex q**, codim-1 exactly as at N=4. The lone exception is the **pure-imaginary-q (Re q = 0, λ real)** family: there the block is Hermitian (the anti-Hermitian coherent part q·C turns real-symmetric when q = iy; gate-checked ‖L − Lᴴ‖ < 1e-14), so every coincidence is semisimple automatically, and its restriction is genuinely non-scalar (distinct dephasing rates), semisimple by Hermiticity, a mechanism distinct from twin-scalar that needs no additivity. So the general statement upgrades: **the residual semisimple coincidence is codim-1 wherever the coalescence is twin-scalar (established from below at N=4 real-q and at every N=5 genuinely-complex-q diabolic), with the imaginary-q Hermitian-locus coincidences semisimple for the separate reason above**: no unexplained residual coincidence remains at N=5. (The census's "11" are the ± conjugate pairs of these families; the survey covered every family, the 4 genuinely-complex-q and the 4 pure-imaginary-q representatives it located, plus 2 defective EPs it correctly rejected, the conjugate branches following by the cross-fold symmetry.) Gate: [`TwinScalarDHalfTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/TwinScalarDHalfTests.cs) (the check is `PathKMonodromyScout.CharacterizeCoherencePencilAt`, restricting the dephasing to the Riesz 2-plane of the raw HS-orthonormal coherence block via `EpCharacter.CharacterizePencil`).

**Δ-tightness (both regimes).** XXZ Δ≠0 makes H quartic: Lemma 1's cancellation dies (gate: W's H-residual jumps to 2.4e-1 at Δ=0.3 while the D-residual stays 0.00e+00), the multiplet descent breaks (E ≠ Σε), and the recorded phenomenology follows at once: diabolics defect instantly, the diamond sharing dies, while the F89d cross-fold (bit-flip parity, integrability-independent) survives. The theorem's name is honest: everything here is BY additivity, and only the transpose/flip/fold legs outlive its removal.

## §9 Bookkeeping: defective vs diabolic, and the two voices

- **The defective braid EPs (the P₁₀ simple zeros) need NO codimension collapse.** Codim-1 is the GENERIC count for a defective coalescence of a non-normal one-parameter family. What Theorem B explains about them is not their existence but their SHARING across sectors (the intertwiner) and where the sharing stops (kernel death, the normal edge).
- **The diabolics (the squared factor, and the complex-q census) are the codim story.** Regime 1 needs only analyticity of the AT-locked families; Regime 2 needs the twin-scalar lemma, of which additivity supplies half.
- **The two voices reconciled.** [F89_PATH_K_DIABOLIC](../../experiments/F89_PATH_K_DIABOLIC.md)'s synthesis ("the Hamiltonian and the diagonal dissipator do not mix the occupation-number sectors: two crossing modes come from different sectors") is the Regime-1 truth, over-applied in that sentence to Regime 2, where [DIABOLIC_BY_INTEGRABILITY](../../hypotheses/DIABOLIC_BY_INTEGRABILITY.md)'s "Not a commuting-symmetry separation" (both modes R=+1, same sector, twin-scalar instead) is the careful voice. Within one block no fine decomposition is invariant under the FULL L: the site sectors are dephasing-rigid but hopping-mixed, the mode sectors hopping-rigid but dephasing-mixed. That non-commuting pair is the whole game; where one of them goes scalar on a plane, the game goes silent.

## §10 Verification

[`SpectatorIntertwinerGateTests`](../../compute/RCPsiSquared.Diagnostics.Tests/Foundation/SpectatorIntertwinerGateTests.cs) (`SLOW_MSM`; run `dotnet test "compute/RCPsiSquared.Diagnostics.Tests" --filter "FullyQualifiedName~SpectatorIntertwinerGateTests"`), N=5, γ=1, H = XYChain(N, 2q), residuals normalized ‖X₂W−WX₁‖_F/(‖X₁‖₂·‖W‖_F), X ∈ {A, C} the pencil parts (the L₁, L₂ of §4 split part-by-part); λ_A = the (1,2) defective eigenvalue at q\*, λ_B its cross-fold partner:

| Measurement | Value | Reading |
|---|---|---|
| W (1,2)→(2,3): A-part / C-part residual | 0.00e+00 / 0.00e+00 (both q) | Lemmas 1+2, machine-exact |
| V_k, k=1..5: A-part residual | 3.8-4.1e-01 | single-mode dissipator failure (kept as refutation data) |
| V_k, k=1..5: C-part residual | ≤ 2e-16 | single-mode Hamiltonian half is exact |
| σ_min(W) on (1,2)→(2,3) | √2 to 1e-14 | injectivity of the climbing rung |
| interior transport ‖Wx₁‖, ‖Wx₂‖ at q*=0.620878 | 1.414, 1.414 | the chain crosses the rung at full norm |
| boundary (3,3)→(4,4): ‖Wx₁‖, ‖Wx₂‖ | 1.7e-15, 2.5e-15 | kernel death of the whole 2-plane (§6) |
| (3,3) pair at λ_B = −conj(λ_A)−2N | gap 4.771e-04 = (1,2) gap; \|mean−pred\| 5.5e-15 | cross-fold + embedding, byte-level |
| edge: A₍₄,₅₎ vs −2I; rank W (3,4)→(4,5) | 0.00e+00; 5/5 | edge lemma (§6) |
| Δ=0.3 (XXZ): W H-part / D-part residual | 2.4e-01 / 0.00e+00 | Δ-tightness (§8) |
| [W,W†] − (N̂_bra+N̂_ket−N), every block | 0.00e+00 (N=3..7) | sl(2) closure: W is the raising operator, Cartan H₀ (§6) |
| ker W per block vs Lefschetz dim(p,q̃)−dim(p+1,q̃+1) | exact, every block N=3..7 (75 at (3,3); 0 below the anti-diagonal) | injectivity by weight; kernel death = highest-weight (§6) |
| [L,W†] (2,3)→(1,2): A-part / C-part residual | 0.00e+00 / 0.00e+00 | L commutes with the whole sl(2), reverse spectator too |
| interior-core death (p_c,p_c)→(p_c+1,p_c+1): ‖Wx₁‖ | 1.7e-15 (N=5, core (3,3)), 9.6e-16 (N=5 locus 2, q=1.077615), 3.2e-15 (N=7, core (4,4)) | the spin-½ core is N-robust across the odd N tested; locus 2 = item 8's out-of-sample prediction (item 7) |
| corner rate window ((4,4) N=5 / (5,5) N=7): worst Re-violation; A off-diag; ‖C+C†‖/‖C‖ | 3.6e-15 / 6.2e-15 (N=5 loci 1, 2), 7.8e-14 (N=7); 0; ≤1e-12 | rate-window lemma, premises pinned from the builder (§6, item 8) |
| min \|spec(corner) − λ_B\| vs margin Re λ_A + 6 | = 1.3811 / 2.2083 / 1.1154 (N=5 loci 1,2; N=7), equal to the printed 4 decimals (gate asserts min ≥ margin) | λ_B absent from the corner; kernel death derived at real loci; tight (AT-locked bottom strands) (§6, item 8) |
| N=5 locus-1 exclusion sweep: interior four + boundary rungs | margins ≥ 0.6189; rung deviation ≤ 8.9e-15 | exclusion of {λ_A, λ_B} derived at q\*=0.620878 (Re λ_A ∈ (−6,−4) required; locus 2 defeats the interior-four window) (§6, item 8) |

The single-mode refutation and the W identity were additionally derived and verified from scratch (independent numpy build) by two independent reviewers on 2026-07-02; that review round is recorded in the local planning layer (deliberately not part of the repository), and its load-bearing numbers are reproduced by the committed gate above. The §6 sl(2) closure, its L-commutation, and the Lefschetz kernel dimensions were likewise independently re-derived the same day (convention-free, with a four-way stacking/sign probe and a disorder-general H), and are pinned by item 6 of the committed gate.

## What remains open

1. The interior-core kernel death ((3,3)→(4,4) and its full-flip image (2,2)→(1,1)) is CLOSED at real loci, per locus. The history in one breath: §6's sl(2) reduced it to the single spectral statement "the diamond eigenvalue carries no defective Jordan block in the cone (4,4) ≅ (1,1)"; a 2026-07-02 attack ruled out the eigenvector-structure routes (in the mode basis W kills |A⟩⟨B| iff A ∪ B = all modes, but the core eigenvector lives mostly on A ∪ B ≠ all: the death is a genuine cancellation, not a support fact) and identified the residue as an ABSENCE the intertwiner cannot supply (a facet of remainder 4); the rate-window lemma (§6) then supplied that absence the same day, from the Absorption Theorem's own diagonal: at real q the corner block's spectrum is confined to [−2(N−3), 0] while Re λ_B = −Re λ_A − 2N sits below it whenever Re λ_A > −6, so λ_B is not in the corner spectrum at all and the whole generalized eigenspace dies (gate items 7+8: both N=5 real loci, N=7, one an out-of-sample prediction). Remainder 1's complex-q face is now CLOSED at N=5 (2026-07-03): the moved rate window (H(q) = A + Im(q)·K) closes the near-axis loci (q\* = 0.9938 ± 0.1825i), and the fold-resultant certificate (§6) closes every remaining branch locus q ≠ 0 of BOTH parities at once, including the R-even deep pair q\* = 1.8141 ± 0.3666i and the R-odd escaper pair q\* = 1.7701 ± 1.2189i: gcd(Res_Λ(F_res, F2_corner(−Λ−4N)), disc_Λ(F_res)) over Q(i)[q] is exactly a q-power (gate `FoldResultantCertificateTests`, a complete multi-prime proof), and q = 0 is the diagonal, semisimple block. What remains of remainder 1 is only the N ≥ 7 extension of the certificate (the corner (5,5) bivariate layer): the holomorphic fold identity through which the chain enters is now DERIVED (a full-spectrum corollary of the §7 diamond maps W + transpose + F89d, the two conjugations cancelling holomorphically at every q, real and complex; gate `HolomorphicFoldIdentityTests`, multiset metric), so the chain's former "one underived analytic entry" is closed, and the fold maps are N-general (only the certificate's per-N compute is N-specific). The strictness Re λ_A > −6 is likewise no longer open: the window-edge lemma (§6) derives it N-uniformly at real loci (a defective EP cannot sit at either edge of its block's rate window, so Re λ_A ∈ (−6,−2) for all N ≥ 4), so the shrinking seed-locus margin (1.381 / 2.208 / 1.115) never reaches zero and the exclusion needs no uniform floor, only the strict inequality the lemma supplies at every finite N.
2. Theorem A's D-half: RESOLVED at N=5 (§8, gate `TwinScalarDHalfTests`). The D-half is SUPPLIED (twin-scalar, dephasing-restriction departure → 0 to the gap, D-half 5e-10…4e-9) at every genuinely-complex-q (Re q ≠ 0) N=5 residual diabolic, so additivity's codim-1 route extends from real q to complex q; the pure-imaginary-q (Re q = 0, λ real) diabolics are semisimple by Hermiticity (the block is real-symmetric there, ‖L−Lᴴ‖ < 1e-14), a separate mechanism. So no unexplained residual coincidence remains at N=5. What remains: a from-first-principles (non-numerical) proof that the genuinely-complex-q coalescences descend from a single free-fermion multiplet, and the N ≥ 7 extension. NOTE (metric): the test must run in the HS-orthonormal coherence basis; the ×2-cleared orbit basis is non-orthonormal (diag(1,2) metric) and gives a spurious non-scalar reading at odd nBlock, which cost a false first result before the metric was corrected.
3. Gap byte-identity across sectors: now IMPLIED by the derived full-spectrum fold (item 1, §7). The fold −λ − 2N preserves the eigenvalue separation of the near-defective pair (|(−λ₁−2N)−(−λ₂−2N)| = |λ₁−λ₂|), and the (1,2) seed pair sits in spec(3,3) via the W-step + fold, so the core gap equals the seed gap identically (byte-identical at N=5, 4.771e-04, gate `HolomorphicFoldIdentityTests`; N-general by the maps). What the intertwiner alone could not supply, the metric geometry of the near-defective gap (§5 scope), the full-spectrum fold does.
4. The EXCLUSION half of membership beyond the outer edge (§7's scoping): CLOSED AT N=5 (2026-07-03). The history: the rate window first derived it at the N=5 seed locus q\* = 0.620878 only (the four interior non-members window-excluded from {λ_A, λ_B}, the boundary blocks normal and rung-pinned, §6), under the condition Re λ_A ∈ (−6,−4), with everything else census evidence; the moved rate window (§6) then closed the near-axis complex loci for the corner facet, and the fold-resultant certificate (§6, gate `FoldResultantCertificateTests`) proved μ ∉ spec(corner) at every branch locus q ≠ 0 of both parities. The final step (2026-07-03) is the identity-composition sibling certificate (1,1)×λ_A (the engine parameterised over the target block and the Λ-composition; the degree guard upgraded from the tightness assumption deg R = rBound, which fails for the identity composition, true 412 < 422 R-even / 384 < 394 R-odd, to the CERTIFIED empirical degree: more distinct split primes are sampled than the Hadamard-bounded count of prime ideals that can divide lc_q(R), so max_p deg(R mod p) = deg_q R exactly, adversarially reviewed) plus the Klein/holomorphic-(1,4)-fold propagation (§7 scoping): all four interior cores exclude {λ_A, μ} at every branch locus q ≠ 0, both parities. Gate: `RemainderR4InteriorExclusionTests`. "No defective EP at ANY λ in a non-member block" (the census's stronger braid-free reading) remains derived only where normality holds (boundary blocks); the shared-λ exclusion, which is what membership means, is complete at N=5. Beyond N=5 the broad exclusion stays census evidence, and since 2026-07-04 THROUGH N=9 and (2026-07-06) N=11 IN FULL: the step-3 σ_min shell census (per-block, both shifts {λ_A, μ}, R-parity split, window-gated; instrument `SectorShellCensus`, gate `SectorShellCensusTests`, results in [F89_MULTI_SECTOR_MONODROMY](../../experiments/F89_MULTI_SECTOR_MONODROMY.md)) finds membership = this corollary's diamond at all 7 real N=9 seeds and all 9 real N=11 seeds, every window-silent in-shell strip block excluded in the shared-λ sense with member/non-member separation ×3.7e8..×4.6e11 (the N=9 and N=11 seeds combined; members read the Jordan pseudospectrum depth ~(gap/2)² ≈ e-13; the σ_min reading converges from above (rigorously for the members via the witness residual, as an estimate for the non-member cores via sparse LSQR), so this is census evidence, not a certificate). The six N=11 blocks the dense managed-LP64 wall had deferred, namely the members (4,5),(5,6)×λ_A, (4,6),(5,5)×μ and the non-member cores (4,4),(4,7), are resolved by the sparse σ_min two-instrument path (the large-N program's Tasks 5-6: `sparse-witness` carrying the member's parity-witness bound from above, `sparse-invit` honestly excluding the cores), verdict PASS (witness-assisted) at every seed. With the N ≥ 7 certificate compute it is the intertwiner claim's remaining blocker, beyond N=11 (N=13/15 matrix-free, and the complex loci).
