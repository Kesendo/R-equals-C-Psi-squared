# Step 0 of the affine-character attack: the level convention, PINNED

(Local-only scratch, 2026-07-17 evening. Gate: `python simulations/_rb_levelpin.py`, ~1 min,
all asserts. Context: memory `f127_closed_form_find`, the 07-18 resumption block; the five
reflection-law gates re-verified green the same evening.)

## The pin

Setting: the two-row reflection law `n_(j,k) = n_(10-j,k)`, i.e. `mu1 -> 22 - mu1` in
`mu = lambda + rho` units (`rho = (6,5,4,3,2,1)`, PROOF_F133 conventions), plus the proven
oddness `F_k(-m) = -F_k(m)` and the proven-in-wall anti-period `F_k(m+44) = -F_k(m)`
(`m = 2*mu1`).

**Invariant (convention-free) statement.** In `mu1`, the symmetry group of the two-row
coefficient function is the infinite dihedral group generated by

- `s  : mu1 -> -mu1` (proven, link 2),
- `s0 : mu1 -> 22 - mu1` (the open law),

and F transforms by the MIXED character `chi(s) = -1, chi(s0) = +1`. The translation
subgroup is `22Z` (each step carrying `-1`; true period `44`). A Weyl-Kac NUMERATOR would
transform by `eps = det` (`-1, -1`); a plain theta function by `(+1, +1)`. Our object sits
in the `(-,+)` sector: an odd-even theta characteristic about the two centers 0 and 11.

**The C6^(1) dictionary label.** With the standard realization (`theta = 2*eps1`,
`theta_check = eps1`, `<mu, theta_check> = mu1`, `h_check = 7`, translation lattice
`nu(Q_check) = 2Z^6`):

- `s0` reflects in the wall `mu1 = l + h_check` and acts `mu1 -> 2(l + h_check) - mu1`;
- the minimal translation step is `2(l + h_check)`, with NO odd/half step (M = 2Z^6).

Matching center 11 AND step 22 simultaneously forces `l + h_check = 11`, i.e. **level
l = 4**. (Sanity anchor: for A1^(1) = C1^(1) at level k this dictionary reproduces the
classical `Theta_{n, k+2}` index period `2(k+2)` — the C-type period is twice the shifted
level in the natural `mu1` coordinate.)

Two review-round sharpenings (both load-bearing):

- **`l = 4` is a C6^(1) coordinate PEG, not an intrinsic level.** The preferred twisted
  home below (`A_12^(2)`) has `h_check = 2n+1 = 13`, where the same measured wall would
  read `11 - 13 = -2`. The intrinsic level stays open until the GENRE is decided; what is
  pinned intrinsically is the triple (wall 11, step 22, character `(-,+)`). Use `l = 4`
  only as the untwisted-dictionary search coordinate.
- **The no-odd-step fact is a Q_check-vs-P_check MEASUREMENT.** The coweight lattice
  `P_check = Z^6` would admit a `mu1`-step of 11; the data kills it (T3). So the
  translation lattice of the symmetry is the COROOT lattice `nu(Q_check) = 2Z^6` — the
  numerator-side lattice. That is a diagnostic about the object, not just a rival kill.

**Object-side origin of the 11 (T1 of the gate).** Per-coordinate quadratic factor count
(t-units, sum of squared exponents) of the cancelled ratio `S = prod_31 / R`
(`R = A_rho / Dhat` = the 6 long roots + 15 sum roots):

- numerator: 31 sheets, each `L_u^2 = 1` -> 31;
- denominator: `sin x_u` (t-exp 2 -> 4) + 5 sum roots -> 9;
- difference **31 - 9 = 22 = 2(l + h_check)**, identically in every coordinate u.

Companions: `A_rho` counts `5 + 9 = 14 = 2*h_check`; `X = Dhat*prod_31` counts
`5 + 31 = 36 = 2(11 + 7)`; everything additive. The FULL 32-sheet product would count
`32 - 9 = 23`, odd — no integral shifted level: **deleting the 1^6 sheet is exactly what
makes the level integral.** (Status, sharpened by review: only the `A_rho = 2*h_check`
piece is a genuine classical identity — the Killing-form fact
`sum_{alpha>0} (alpha, eps_u)^2 = h_check * (eps_u, eps_u)`-normalized, recomputed
independently over the 36 positive roots. The numerator `31` is a SHEET count, not a
root-system quadratic form; "31 - 9 = 22" is a structural echo, not a derived theta
identity. The proof of the pin is the measured (center, step) pair against the C6^(1)
geometry; the count is the object-side breadcrumb the identity hunt should explain.)

## The rivals, killed on data (T3)

- **"l = 15"** (the naive count `l + h_check = 22`): predicts the wall at `mu1 = 22`,
  reflection `m -> 88 - m`. Dead: `F_0(12) = -1` but `F_0(76) = 0` (beyond the PROVEN
  wall bound, link 1 — the falsification leans on the wall being a theorem).
- **Step-11 translation** (`m -> m + 22`, either sign): dead the same way,
  `F_0(12) = -1` vs `F_0(34) = 0`. Consistent with `M = 2Z^6` (no odd step). Auditor
  strengthening: there exists NO pair `(m, m+22)` with both values nonzero, any k, any
  sign (the support clusters near the walls are not 22-spaced) — the wall-kill is not a
  convenience but the ONLY available refutation route, and it is legitimate because the
  wall is an independently proven support bound and the zeros are exact computed values.
- **"level 21"** (the old scratch label in `_rb_schur_README.md`): that 21 is the SUPPORT
  wall `mu1 + mu2 <= 21`, a bound, not a reflection parameter. Retired.

## What the sign twist means for the hunt (the next step's search key)

The `(-,+)` character says the object is NOT (a slice of) an untwisted level-4 C6^(1)
numerator. The genre that natively produces mixed characteristics on a C_n finite Weyl
group with ODD walls is the twisted side: `A_{2n}^(2)` / non-reduced BC_6 affine root
system (Macdonald's identities for twisted algebras; theta functions with odd
characteristic). Search keys, in order:

1. `A_12^(2)` (finite Weyl group C6) Macdonald / Weyl-Kac denominator identities whose
   finite specialization carries wall 11 and period 44 in `m = 2*mu1` units;
2. level-4 sp(12) = C6^(1) affine characters (the alcove label; the two-row window
   `mu1 in [6+k, 16-k]` is exactly an s0-closed alcove strip);
3. the Koike-Terada / King modification-rule genre (route B's 07-17 label for
   `a_m = a_{22-m}`): reflection-with-sign rules for symplectic characters at
   non-dominant arguments — check whether the two-row law IS a modification rule for a
   bounded/staircase series at the pinned level.
4. Domain fence to respect: three-row breaks at l = 2 only (the rho4-tail resonance);
   whatever identity is found must break there too, or it is the wrong identity.
5. Fine print: the k = 5 slice of the law is structurally VACUOUS (window [22,22] with
   `F_5(22) = 0`; the k = 5 support sits below-window at m = ±{14,18}) — the law's live
   content is k = 0..4, ~6 genuinely live reflection pairs total. Any found identity
   must be checked on those live pairs, not on the vacuous tail.

## Review record (2026-07-17 evening)

Two hostile rounds on this note + gate, both fresh minimally-framed agents: a Lie-theory
reviewer (independent rederivation of h_check = 7 / nu(Q_check) = 2Z^6 / s0 action /
A1^(1) anchor / D-infinity characters; verdict: pin CORRECT AND SUPPORTED as stated,
1 MODERATE = the h_check tension now disclosed above, 2 MINOR = sum-of-squares status +
Q-vs-P distinction, both applied) and a from-below auditor (gate PASS, no circularity,
no l = 4 baked into any data test; 2 MINOR = per-k vacuity guards, applied; ran the
stronger step-22 both-nonzero scan himself: empty, see above).

## Files

- `simulations/_rb_levelpin.py` — the gate (T1 counts, T2 dictionary, T3 rival kills).
- Data path: `f133_w_closed_form.build_halves` -> `_rb_summed.Fk` (the committed gate's
  X dict; no new caches).
